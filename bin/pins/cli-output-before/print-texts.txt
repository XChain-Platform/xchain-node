console.error('Restore rewinds to the newest bootstrap at or before that block, then the service re-parses forward.')
console.error('\nERROR: ' + e.message + '\n')
console.error('\nERROR: ' + e.message + '\n')
console.error('\nERROR: ' + e.message + '\n')
console.error('\nERROR: ' + e.message + '\n')
console.error('`rollback` is not yet implemented; nothing was rolled back.')
console.error('recreate failed: ' + redactSecrets(err && err.message ? err.message : err))
console.error('recreate failed: nothing was recreated' + (why ? ' - ' + why : ' (no requested service can be recreated from the config map)'))
console.error('uninstall failed: ' + redactSecrets(err && err.message ? err.message : err))
console.error('update failed: ' + redactSecrets(err && err.message ? err.message : err))
console.error('update failed: ' + redactSecrets(err && err.message ? err.message : err))
console.error('update failed: nothing was updated' + (why ? ' - ' + why : ' (no requested service matched an installed container)'))
console.error('xchain-node: command failed: ' + detail)
console.error('xchain-node: command failed: ' + detail)
console.error(` xchain-node bootstrap restore ${service} ${chain} ${network}`)
console.error(` xchain-node reset ${service} ${chain} ${network}`)
console.error(`To recover ${service} (${chain} ${network}) to block ${blockIndex}, use reset followed by a bootstrap restore:`)
console.error(`recreate: ${nextModule} (${nextCoin} ${nextNetwork}) FAILED: ${why}`)
console.error(`uninstall: ${nextModule} (${nextCoin} ${nextNetwork}) FAILED: ${why}`)
console.error(err.message)
console.error(redactSecrets(err.message))
console.error(redactSecrets(err.message))
console.log(" The indexer tracks reorgs by a decoder event id. Wiping the decoder restarts")
console.log(" committing blocks until both are rebuilt together.")
console.log(" those ids, so the indexer would abort with a reorg-cursor error (RE-1) and stop")
console.log("")
console.log("")
console.log("")
console.log("")
console.log("")
console.log("")
console.log("")
console.log("####" + container["name"] + " LOGS####")
console.log("####" + moduleName + " LOGS####")
console.log("Bye!")
console.log("Checking if Docker is installed")
console.log("Checking xchain-node structure")
console.log("Checking/Creating directories")
console.log("Checking/Creating xchain_node access for current user")
console.log("Checking/Installing hub module")
console.log("Checking/Installing mariadb container")
console.log("E2E tests finished with exit code " + exitCode)
console.log("E2E tests finished with exit code " + exitCode)
console.log("Following only " + moduleContainerIds[0]["name"] + "; omitted: " + omitted)
console.log("Getting all remote project versions")
console.log("Getting modules status")
console.log("Logs saved to: " + logFile)
console.log("Logs saved to: " + logFile)
console.log("No service was selected")
console.log("Opening MariaDB connection")
console.log("Running e2e tests, please wait...")
console.log("The shell command can't be used for multiple containers, 'all' is invalid")
console.log("There was a problem installing the node")
console.log("Warning: Docker's data-root is on a separate disk (" + relo.dockerRootDir + "), " + "but containerd's store is still on the root filesystem (" + relo.containerdRoot + "). " + "The Docker data-root setting does NOT move containerd's content/snapshot store, so it " + "keeps growing on `/` and can fill the root disk. Relocate containerd too: point its `root` " + "(in /etc/containerd/config.toml, or bind-mount " + relo.containerdRoot + ") onto the same disk " + "and restart the containerd + docker services. Set XCHAIN_NODE_CONTAINERD_ROOT to silence a false positive.")
console.log("Warning: couldn't fetch remote versions (GitHub unreachable or rate-limited); continuing without version check: " + redactSecrets(err))
console.log("Warning: the xchain-hub is not answering, so its config push was skipped. " + "This command can repair the hub, so it continues; the next command that runs against a " + "healthy hub pushes the config.")
console.log("Xchain-Node ver 0.0.0")
console.log("clear-reorg-halt takes one chain and one network; 'all' is invalid")
console.log("recreate does not apply to " + nextModule + remedy)
console.log(' Capability drift : xchain-node validator drift (compares this against the indexer)')
console.log(' DOGE signer : ' + (process.env.XCHAIN_NODE_HUB_SIGNER_DIR ? process.env.XCHAIN_NODE_HUB_SIGNER_DIR + ' (operator-supplied, XCHAIN_NODE_HUB_SIGNER_DIR)' : (getSignerMountDir() || '(missing; re-run validator init)')))
console.log(' DOGE wallet : ' + w.dogeAddress + ' (' + coins.dogeCoin + ' for price rounds and anchors)')
console.log(' Fix the volume problem and re-run the same reset command.')
console.log(' On-chain membership: xchain-node validator stake (dry run shows balances and the stake)')
console.log(' Set XCHAIN_NODE_DATA_DIR to this stack\'s data root (or make docker reachable so the')
console.log(' The node data for this stack WAS already cleared; the decoder/indexer')
console.log(' capabilities : ' + ((s.capabilities || []).join(', ') || '(none)'))
console.log(' caps config : ' + (getCapabilityConfigHostPath() || '(missing; re-run validator init)'))
console.log(' databases around a chain that would stay untouched.')
console.log(' databases were NOT touched, and the stopped services are left down.')
console.log(' full_node : not active on this network yet (tier turns on with a flag day; nothing to configure)')
console.log(' keys file : ' + WALLETS_FILE + ' (mode 0600; back it up)')
console.log(' network : ' + (s.network || '(unset; set HUB_NETWORK in .env)'))
console.log(' node container can be inspected) and re-run. Refusing rather than resetting the')
console.log(' oracle epoch : ' + (s.ORACLE_EPOCH_START || '(unset, required before oracle runs)'))
console.log(' p2p address : ' + s.P2P_VALIDATOR_ADDR)
console.log(' pubkey : ' + s.pubkey)
console.log(' roll call absences (BTC): ' + rollcall.absences.streak + (rollcall.absences.evictedNow ? ' EVICTED - dropped from every capability set' : ''))
console.log(' roll call absences (BTC): 1 (warning shot; one more consecutive miss evicts this stake)')
console.log(' roll call absences (BTC): none on record')
console.log(' roll call absences (BTC): unavailable (' + (rollcall.absences.error || rollcall.absences.reason || 'indexer read failed') + '); check the explorer before assuming this key is safe')
console.log(' seed nodes : ' + ((s.SEED_NODES || []).join(', ') || '(none)'))
console.log(' stake wallet : ' + w.stakeAddress + ' (' + coins.stakeCoin + ' for fees, holds the XCHAIN stake)')
console.log(' wallets : (none; re-run validator init, or run your own signer via XCHAIN_NODE_HUB_SIGNER_DIR)')
console.log('')
console.log('Aborted: MariaDB container not found; install the database first. No data was touched.')
console.log('Aborted: reset was not confirmed. No data was touched.')
console.log('This node is a validator; checking its config for anything a newer version added...')
console.log('Validator enabled.')
console.log('clear-reorg-halt: --reason must say, in at least 8 characters, why this database is known good; it is recorded with the clear.')
console.log('clear-reorg-halt: no xchain-decoder container is installed for ' + nextCoin + ' ' + nextNetwork)
console.log('clear-reorg-halt: no xchain-decoder matched the given chain and network')
console.log('install: nothing to do for ' + outcome.skipped .map(s => `${s.module} (${s.coin} ${s.network})`).join(', ') + ' - already installed. Use `update` to rebuild.')
console.log(JSON.stringify(due, null, 2))
console.log(` (XCHAIN_NODE_DATA_DIR in this shell would have pointed at ${resolved.configuredPath})`)
console.log(` Container ${resolved.containerName} reported no /root/.${coin} bind mount ` + '(it is absent, or docker is unreachable from here).')
console.log(` Or the whole stack (also re-syncs the chain): xchain-node reset all ${coin} ${network}`)
console.log(` Reset the pair: xchain-node reset ${XChainService.XCHAIN_DECODER} ${coin} ${network} --with-indexer`)
console.log(` Restarted ${stoppedModules.length - restartFailures.length} of ` + `${stoppedModules.length} already-stopped service(s).`)
console.log(` STILL DOWN, start by hand: ${restartFailures.join(', ')}`)
console.log(` The configured path ${resolved.configuredPath} does not exist either.`)
console.log(` XCHAIN_NODE_DATA_DIR is ${envState}.`)
console.log(`Aborted: ${reason}. No data was touched.`)
console.log(`Aborted: ${reason}.`)
console.log(`Aborted: cannot reach the external MariaDB at ${probe.host}:${probe.port}` + ` (${probe.error}). No data was touched.`)
console.log(`Aborted: cannot read the ${XChainService.XCHAIN_INDEXER} registry row ` + `(${failureReason(err)}), so it is not known whether resetting ` + `${XChainService.XCHAIN_DECODER} alone would strand it. No data was touched.`)
console.log(`Aborted: cannot resolve the ${coin} ${network} node datadir. No data was touched.`)
console.log(`Aborted: resetting ${XChainService.XCHAIN_DECODER} alone would leave ` + `${XChainService.XCHAIN_INDEXER} incoherent. No data was touched.`)
console.log(`Clearing Docker volume ${utxoVolumeName}...`)
console.log(`Clearing node data at ${nodeDataPath}...`)
console.log(`Clearing relocated node data at ${relocated}...`)
console.log(`Installing XChain ${target.tag} (${target.resolvedFrom}); every component is manifest-pinned.`)
console.log(`Installing from branch '${target.ref}' (UNRELEASED: tracking install, no version pinning).`)
console.log(`Marked ${marked.length} bootstrap combo(s) for republish after this reindex: ${marked.join(', ')}`)
console.log(`No ${NODE_MODULE_NAME} container is installed for ${coin} ${network}; there is no node data to clear.`)
console.log(`No Docker volume ${utxoVolumeName} to clear.`)
console.log(`No TTY to prompt on; restoring the newest bootstrap (${preselected}). Pass --file to choose another.`)
console.log(`Node datadir resolved from ${resolved.resolvedFrom}: ${nodeDataPath}`)
console.log(`Restarting ${coin} ${network} services...`)
console.log(`Stopping ${coin} ${network} services...`)
console.log(`This node tracks branch '${target.ref}'${target.inferred ? ' (classified from its checkouts)' : ''}; updating to its newest commits. Name a release (e.g. \`update all v0.15.2\`) to move it onto a release.`)
console.log(`update: ${nextModule} (${nextCoin} ${nextNetwork}) already runs the pinned daemon version; left running.`)
console.log(`update: skipped ${absent.length} service${absent.length === 1 ? '' : 's'} not installed on this node.`)
console.log(added === 0 ? "Nothing to add (already in sync)" : `Registered ${added} module(s)`)
console.log(combo)
console.log(entry.combo)
console.log(err)
console.log(err)
console.log(err)
console.log(err)
console.log(err)
console.log(execStdOut)
console.log(isInitialized() ? 'Validator is initialized but disabled.' : 'No validator configured. Run: xchain-node validator init')
console.log(line)
console.log(out)
console.log(redactSecrets(err))
console.log(redactSecrets(err))
console.log(redactSecrets(err))
console.log(redactSecrets(err))
console.log(redactSecrets(err))
console.log(redactSecrets(err))
console.log(redactSecrets(err))
console.log(redactSecrets(err))
console.log(redactSecrets(err))
console.log(redactSecrets(err))
console.log(redactSecrets(err))
console.log(redactSecrets(err))
console.log(redactSecrets(err.message))
console.log(redactSecrets(err.message))
console.log(redactSecrets(firstErr))
console.log(redactSecrets(firstErr))
console.log(rollcall.broadcast.exportsBroadcast ? ' roll call : this signer exports broadcast, so it can publish roll calls' : ' roll call : NO broadcast export in ' + rollcall.broadcast.file + ' - it can SIGN a roll call but never PUBLISH one, silently. Add broadcast(payload) ' + 'or use the CLI-generated signer.')
console.log(rollcall.doge.unavailable ? ' DOGE runway : unavailable (could not read the DOGE wallet balance' + (rollcall.doge.error ? ': ' + rollcall.doge.error : '') + ')' : ' DOGE runway : ' + rollcall.doge.balance + ' ' + coins.dogeCoin + ' confirmed, ~' + rollcall.doge.rollcalls + ' roll call(s) of runway (~0.006 ' + coins.dogeCoin + ' each: two ~0.003 ' + coins.dogeCoin + ' transactions)')
console.log(text || ('clear-reorg-halt: failed for ' + nextCoin + ' ' + nextNetwork + ': ' + (err && err.message)))
console.warn(" DELETE FROM price_ingest_watermarks WHERE source_chain = '" + (CoinTickerSymbol[coin] || coin) + "' AND network IN ('" + String(network || '').trim().toLowerCase() + "', '');")
console.warn(" Keep the network clause: it is what leaves every OTHER network's fence for this chain in place.")
console.warn(" Run on the hub DB before the indexer catches up:")
console.warn(' ' + statement)
console.warn(' A fresh indexer will mirror the dead chain\'s matches back in. Run on the hub DB:')
console.warn(' The published archives for these combos are now from the PRE-reset lineage and')
console.warn(' This forces a full resync afterward. There is no undo.\n')
console.warn(' nothing will force a republish. Republish by hand once the stack has caught up:')
console.warn('WARNING: clearing the hub price ingest fence failed: ' + (err && err.message ? err.message : err))
console.warn('WARNING: could not record this reindex in the bootstrap republish ledger: ' + ((err && err.message) ? err.message : err))
console.warn('WARNING: purging the hub cross-chain relic rows failed: ' + ((err && err.message) ? err.message : err))
console.warn('install: continuing anyway (XCHAIN_NODE_ALLOW_DEGRADED_EXPLORER is set).')
console.warn('install: could not attach the explorer to the new coin networks: ' + err)
console.warn('install: could not push config to the hub: ' + err)
console.warn('install: the xchain-explorer is still not serving coin data.' + ' The stack is installed; the explorer either cannot reach the hub or the hub' + ' has no config for these coins yet. Check it before running anything that reads it.')
console.warn(` Affected stores: ${targets.join(', ')}`)
console.warn(` Then restart the hub: xchain-node restart ${HUB_MODULE_NAME}`)
console.warn(` xchain-node bootstrap create ${module} ${coin} ${network}`)
console.warn(`Could not repair the validator config (${err && err.message ? err.message : err}); the hub is updated on its existing config.`)
console.warn(`Failed to start ${module} (${firstErr && firstErr.message}); retrying in 3s...`)
console.warn(`\nWARNING: this will IRREVERSIBLY destroy ${coin} ${network} data.`)
console.warn(`recreate: ${nextModule} (${nextCoin} ${nextNetwork}) has no container; nothing to recreate. Install it first.`)
console.warn(`uninstall: keeping ${s.module}; it is ${reason}.`)
console.warn(`update: ${module} (${coin} ${network}) was not rebuilt; nothing changed for it.`)
console.warn(`update: ${nextModule} (${nextCoin} ${nextNetwork}) has no registered container; nothing to update (install it first if you expected it here).`)
console.warn(`update: ${nextModule} (${nextCoin} ${nextNetwork}) is not rebuilt by update; the database container must be removed manually and reinstalled.`)

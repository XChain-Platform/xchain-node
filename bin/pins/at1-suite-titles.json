{
  "titleSets": {
    "013fb4868c83ee37": [
      "CredentialsService XCHAIN_NODE_DB equals \"xchain_node\"",
      "CredentialsService generatePassword() accepts custom byte length",
      "CredentialsService generatePassword() returns a non-empty base64url string",
      "CredentialsService generatePassword() returns different values on successive calls",
      "CredentialsService getCredentialsDir() returns ~/.xchain-node",
      "CredentialsService getCredentialsPath() returns ~/.xchain-node/credentials.json",
      "CredentialsService getOsUserDbName() returns xchain_node_<sanitized-username>",
      "CredentialsService getOsUserDbName() sanitizes username with special chars",
      "CredentialsService getOsUserDbName() truncates username at 60 chars",
      "CredentialsService hasCredentials() returns false when existsSync throws",
      "CredentialsService hasCredentials() returns false when file does not exist",
      "CredentialsService hasCredentials() returns true when credentials file exists",
      "CredentialsService hasExternalDbConfig() returns false when externalDb block is absent",
      "CredentialsService hasExternalDbConfig() returns false when host is not a string",
      "CredentialsService hasExternalDbConfig() returns false when port is not a number",
      "CredentialsService hasExternalDbConfig() returns false when readFileSync throws",
      "CredentialsService hasExternalDbConfig() returns false when root_password is not a string",
      "CredentialsService hasExternalDbConfig() returns false when root_user is not a string",
      "CredentialsService hasExternalDbConfig() returns true when credentials.json has a well-formed externalDb block",
      "CredentialsService loadCredentials() returns null when file contains null",
      "CredentialsService loadCredentials() returns null when file is not valid JSON",
      "CredentialsService loadCredentials() returns null when parsed object is missing password field",
      "CredentialsService loadCredentials() returns null when parsed object is missing user field",
      "CredentialsService loadCredentials() returns null when readFileSync throws",
      "CredentialsService loadCredentials() returns parsed object with user and password",
      "CredentialsService loadDbRootPassword() / saveDbRootPassword() loadDbRootPassword returns null when the file or key is absent",
      "CredentialsService loadDbRootPassword() / saveDbRootPassword() loadDbRootPassword returns the stored value and rejects non-string/empty shapes",
      "CredentialsService loadDbRootPassword() / saveDbRootPassword() saveDbRootPassword creates the 0700 dir when missing",
      "CredentialsService loadDbRootPassword() / saveDbRootPassword() saveDbRootPassword read-modify-writes with mode 0600, preserving sibling keys",
      "CredentialsService loadExternalDbConfig() returns null when externalDb is absent",
      "CredentialsService loadExternalDbConfig() returns null when file cannot be parsed",
      "CredentialsService loadExternalDbConfig() returns the externalDb block when present",
      "CredentialsService sanitizeForMariaDb() passes through alphanumeric + underscore unchanged",
      "CredentialsService sanitizeForMariaDb() replaces all special chars with underscores",
      "CredentialsService sanitizeForMariaDb() replaces hyphens and spaces with underscores",
      "CredentialsService sanitizeForMariaDb() replaces non-alphanumeric chars (including !) with underscores",
      "CredentialsService sanitizeForMariaDb() returns \"user\" for empty input (zero-length cleaned string)",
      "CredentialsService saveCredentials() calls chmodSync on file after writing",
      "CredentialsService saveCredentials() creates dir if it does not exist",
      "CredentialsService saveCredentials() does not create dir when it already exists",
      "CredentialsService saveCredentials() preserves the externalDb block written by saveExternalDbConfig",
      "CredentialsService saveCredentials() silently ignores chmodSync errors (Windows compat)",
      "CredentialsService saveCredentials() writes JSON to credentials file with mode 0600",
      "CredentialsService saveExternalDbConfig() coerces types: host/root_user/root_password to string, port to number",
      "CredentialsService saveExternalDbConfig() creates dir if it does not exist",
      "CredentialsService saveExternalDbConfig() merges externalDb into existing credentials.json",
      "CredentialsService saveExternalDbConfig() silently ignores chmodSync errors",
      "CredentialsService saveExternalDbConfig() writes file with mode 0600"
    ],
    "0226ac96a43d54d4": [
      "ValidatorStakeService planMints() flags a shortfall above the per-address cap",
      "ValidatorStakeService planMints() flags a token with no open mint",
      "ValidatorStakeService planMints() mints only the difference when partly funded",
      "ValidatorStakeService planMints() needs no mint when the balance already covers the stake",
      "ValidatorStakeService planMints() never mints on mainnet",
      "ValidatorStakeService planMints() splits the shortfall into per-transaction cap-sized mints",
      "ValidatorStakeService stakeTiming() gives no duration on regtest, where blocks are mined on demand",
      "ValidatorStakeService stakeTiming() glosses both counts as the same durations on every chain",
      "ValidatorStakeService stakeTiming() reads both clocks from the coin registry, not from the CLI",
      "ValidatorStakeService stakeTiming() tracks the per-chain values for litecoin and dogecoin",
      "ValidatorStakeService stakeValidator() --no-wait returns as soon as the STAKE is broadcast",
      "ValidatorStakeService stakeValidator() --serialize keeps the old block-per-action behaviour",
      "ValidatorStakeService stakeValidator() calls explorer methods that the published SDK actually exposes",
      "ValidatorStakeService stakeValidator() does nothing when the pubkey already carries a valid stake",
      "ValidatorStakeService stakeValidator() dry run prints the plan and sends nothing",
      "ValidatorStakeService stakeValidator() funds each action from the previous one, forcing the in-block order",
      "ValidatorStakeService stakeValidator() names the missing gas token and the bootstrap instead of a raw explorer 404",
      "ValidatorStakeService stakeValidator() on mainnet never mints and blocks on a shortfall",
      "ValidatorStakeService stakeValidator() passes --fee-per-kb through to the encoder",
      "ValidatorStakeService stakeValidator() refuses a WIF that does not control the recorded stake address",
      "ValidatorStakeService stakeValidator() refuses when the address has no coin for fees",
      "ValidatorStakeService stakeValidator() refuses without a validator",
      "ValidatorStakeService stakeValidator() reports pending mints instead of staking when they never index",
      "ValidatorStakeService stakeValidator() sends everything back to back and waits only on the STAKE",
      "ValidatorStakeService stakeValidator() skips minting when already funded",
      "ValidatorStakeService stakeValidator() the plan states the escrow and the cooldown that frees it",
      "ValidatorStakeService stakeValidator() the post-broadcast summary takes the activation delay from the registry",
      "ValidatorStakeService stakeValidator() waits for the mints to index when no chain can be formed, rather than racing the STAKE",
      "ValidatorStakeService stakeValidator() warns and keeps going when the validator set cannot be read, instead of reading the failure as \"not staked\"",
      "ValidatorStakeService stakeValidator() with --broadcast mints the shortfall in order, then stakes",
      "ValidatorStakeService unstakeValidator() does nothing when the pubkey carries no valid stake",
      "ValidatorStakeService unstakeValidator() dry run reports the active stake and sends nothing",
      "ValidatorStakeService unstakeValidator() never claims the XCHAIN is spendable once the stake leaves the set",
      "ValidatorStakeService unstakeValidator() omits the duration on regtest, where block spacing is meaningless",
      "ValidatorStakeService unstakeValidator() refuses a WIF that does not control the recorded stake address",
      "ValidatorStakeService unstakeValidator() refuses when the validator set cannot be read",
      "ValidatorStakeService unstakeValidator() refuses without a validator",
      "ValidatorStakeService unstakeValidator() the plan prints BOTH clocks: 6 blocks to leave the set, 1000 to unlock",
      "ValidatorStakeService unstakeValidator() the post-broadcast summary repeats both clocks, not just the delay",
      "ValidatorStakeService unstakeValidator() with --broadcast sends UNSTAKE v0 for this pubkey"
    ],
    "0b0e062a08751d6c": [
      "ReleaseSignatureService assertDigestMatches() REFUSES a tampered artifact",
      "ReleaseSignatureService assertDigestMatches() REFUSES an artifact the digest file does not list",
      "ReleaseSignatureService assertDigestMatches() passes when the artifact matches its signed digest",
      "ReleaseSignatureService parseSha256sums() REFUSES a duplicated filename rather than letting the last win",
      "ReleaseSignatureService parseSha256sums() REFUSES a malformed line instead of skipping it",
      "ReleaseSignatureService parseSha256sums() REFUSES an empty digest file",
      "ReleaseSignatureService parseSha256sums() lowercases digests so comparison never depends on case",
      "ReleaseSignatureService parseSha256sums() reads coreutils text and binary mode lines",
      "ReleaseSignatureService parseSha256sums() tolerates CRLF, which a digest file round-tripped through Windows carries",
      "ReleaseSignatureService the pinned trust anchor is NOT the wallet key (the named confusion hazard)",
      "ReleaseSignatureService the pinned trust anchor pins the PLATFORM key, and it is the key the shipped file contains",
      "ReleaseSignatureService the pinned trust anchor ships the release key inside this repo",
      "ReleaseSignatureService verifyDetachedSignature() REFUSES a fingerprint pin that is not 40 hex",
      "ReleaseSignatureService verifyDetachedSignature() REFUSES a signature over different bytes",
      "ReleaseSignatureService verifyDetachedSignature() REFUSES a signature whose key is not in the shipped anchor at all",
      "ReleaseSignatureService verifyDetachedSignature() REFUSES a valid signature made by a DIFFERENT key",
      "ReleaseSignatureService verifyDetachedSignature() REFUSES when gpg itself is unavailable rather than passing",
      "ReleaseSignatureService verifyDetachedSignature() REFUSES when the trust anchor is missing",
      "ReleaseSignatureService verifyDetachedSignature() accepts a fingerprint written with the spaced grouping people copy from docs",
      "ReleaseSignatureService verifyDetachedSignature() accepts a signature by the pinned key",
      "ReleaseSignatureService verifyGitTagSignature() accepts a tag signed by the pinned key",
      "ReleaseSignatureService verifyGitTagSignature() refuses a tag signed by another key, even a valid one",
      "ReleaseSignatureService verifyGitTagSignature() refuses a tag that does not exist",
      "ReleaseSignatureService verifyGitTagSignature() refuses an unsigned (lightweight) tag",
      "ReleaseSignatureService verifyGitTagSignature() refuses when the pinned key file is missing",
      "ReleaseSignatureService verifyManifestForTag() REFUSES a digest file signed by an impostor key",
      "ReleaseSignatureService verifyManifestForTag() REFUSES a manifest whose digest is not the signed one",
      "ReleaseSignatureService verifyManifestForTag() REFUSES a release that publishes no digest file",
      "ReleaseSignatureService verifyManifestForTag() REFUSES a release that publishes no signature",
      "ReleaseSignatureService verifyManifestForTag() REFUSES when the asset fetch itself fails",
      "ReleaseSignatureService verifyManifestForTag() names the opt-out in every refusal, so the airgapped path is discoverable",
      "ReleaseSignatureService verifyManifestForTag() the opt-out downgrades a refusal to a LOUD warning",
      "ReleaseSignatureService verifyManifestForTag() the opt-out is only the explicit falsy values, never any set value",
      "ReleaseSignatureService verifyManifestForTag() verifies signature THEN digest, and reports the key"
    ],
    "113421dff9a3e19e": [
      "ui/menu exports mainMenu as a function",
      "ui/menu exports modulesSelectionInterface as a function",
      "ui/menu exports restoreBootstrapInterface as a function",
      "ui/menu exports startInterface as a function",
      "ui/menu per-module action labels are all handled ACTION_INSTALL_LOCAL_IN_CONTAINER (\"Install Local Version in Container\") reaches a handler",
      "ui/menu per-module action labels are all handled ACTION_REINSTALL_CONTAINER (\"Reinstall\") reaches a handler",
      "ui/menu per-module action labels are all handled ACTION_REINSTALL_REMOTE (\"Reinstall from remote\") reaches a handler",
      "ui/menu per-module action labels are all handled ACTION_UPDATE_CONTAINER (\"Update Container\") reaches a handler",
      "ui/menu per-module action labels are all handled ACTION_UPDATE_LOCAL (\"Update local version\") reaches a handler",
      "ui/menu per-module action labels are all handled every offered per-module action label has a handler branch",
      "ui/menu requiring the module has no throwing side effects",
      "ui/menu restoreBootstrapInterface non-interactive resolution falls back to the newest archive when there is no TTY to prompt on",
      "ui/menu restoreBootstrapInterface non-interactive resolution rejects a --file that is not present instead of silently picking another",
      "ui/menu restoreBootstrapInterface non-interactive resolution restores exactly the named archive on --file",
      "ui/menu restoreBootstrapInterface non-interactive resolution restores the newest archive on --latest without prompting",
      "ui/menu restoreBootstrapInterface non-interactive resolution says so plainly when there is nothing to restore",
      "ui/menu restoreBootstrapInterface non-interactive resolution surfaces a failed restore as an error rather than a bare false"
    ],
    "12558605c0d0415c": [
      "crypto-node build scaffold (XCHAIN_NODE_CRYPTO_NODES_DIR) covers every coin and network the installer supports",
      "crypto-node build scaffold (XCHAIN_NODE_CRYPTO_NODES_DIR) creates the build dir when it does not exist yet",
      "crypto-node build scaffold (XCHAIN_NODE_CRYPTO_NODES_DIR) fails closed when the provisioned credentials are missing",
      "crypto-node build scaffold (XCHAIN_NODE_CRYPTO_NODES_DIR) injects the provisioned credentials, leaving no placeholder in the built conf",
      "crypto-node build scaffold (XCHAIN_NODE_CRYPTO_NODES_DIR) is a no-op copy when the build dir IS the source tree",
      "crypto-node build scaffold (XCHAIN_NODE_CRYPTO_NODES_DIR) names the missing path when the scaffold is not in the source tree",
      "crypto-node build scaffold (XCHAIN_NODE_CRYPTO_NODES_DIR) never writes credentials back into the tracked template",
      "crypto-node build scaffold (XCHAIN_NODE_CRYPTO_NODES_DIR) secrets the CLI writes are ignored by git ignores the generated crypto-node conf but not the template",
      "crypto-node build scaffold (XCHAIN_NODE_CRYPTO_NODES_DIR) secrets the CLI writes are ignored by git ignores the hub credential sidecar",
      "crypto-node build scaffold (XCHAIN_NODE_CRYPTO_NODES_DIR) secrets the CLI writes are ignored by git ignores the validator identity directory",
      "crypto-node build scaffold (XCHAIN_NODE_CRYPTO_NODES_DIR) stages a Dockerfile into a build dir that holds only the downloaded tarball",
      "crypto-node build scaffold (XCHAIN_NODE_CRYPTO_NODES_DIR) writes the credential-bearing conf 0600"
    ],
    "13499f2ff51c6f3c": [
      "installHubModule() stages the hub from the release manifest clears the target it published, on the failure path too",
      "installHubModule() stages the hub from the release manifest clones a named branch as a branch, without a release lookup",
      "installHubModule() stages the hub from the release manifest falls back to the branch the lookup names when no release exists",
      "installHubModule() stages the hub from the release manifest leaves an already-published target alone",
      "installHubModule() stages the hub from the release manifest pins the hub to the manifest of a named release, not to a hub tag of that name",
      "installHubModule() stages the hub from the release manifest resolves the latest release for a no-ref install, pinned, instead of cloning master"
    ],
    "14bd28e1e4b711a9": [
      "DiscoveryService.classifyContainer classifies shared modules (no coin/network)",
      "DiscoveryService.classifyContainer classifies the coin node module",
      "DiscoveryService.classifyContainer exempts the one-shot e2e-test runner from classification (never registered)",
      "DiscoveryService.classifyContainer falls back to the image name when the container was renamed externally",
      "DiscoveryService.classifyContainer keys on the container NAME even when the image tag was stolen (bare image ID)",
      "DiscoveryService.classifyContainer returns null for foreign containers and malformed identities",
      "DiscoveryService.classifyContainer tolerates docker API name variants (array form, leading slash)",
      "DiscoveryService.scanAndRegisterModules does NOT purge the live sync service row (shared, coin/network-independent)",
      "DiscoveryService.scanAndRegisterModules does NOT purge the registry row of a live, name-keyed container",
      "DiscoveryService.scanAndRegisterModules fail-closed registry refuses to scan against an unconfigured store instead of reporting a clean run",
      "DiscoveryService.scanAndRegisterModules fail-closed registry throws when a reconcile write fails",
      "DiscoveryService.scanAndRegisterModules fail-closed registry throws when the registry write fails rather than counting the row as added",
      "DiscoveryService.scanAndRegisterModules reconciles a stale container_id in place",
      "DiscoveryService.scanAndRegisterModules registers a live container whose image tag was stolen (the incident case)",
      "DiscoveryService.scanAndRegisterModules still purges genuinely orphaned rows"
    ],
    "152f5f77b38555af": [
      "utils/helpers assertSafeArchiveMemberNames() accepts an empty listing",
      "utils/helpers assertSafeArchiveMemberNames() accepts names merely containing dots (\"a..b\", \"..hidden\")",
      "utils/helpers assertSafeArchiveMemberNames() accepts relative member paths",
      "utils/helpers assertSafeArchiveMemberNames() throws on a bare \"..\" member",
      "utils/helpers assertSafeArchiveMemberNames() throws on a leading \"..\" segment",
      "utils/helpers assertSafeArchiveMemberNames() throws on an absolute member path",
      "utils/helpers assertSafeArchiveMemberNames() throws on an interior \"..\" segment",
      "utils/helpers decompressTarGz() lists members first, then runs tar -xvzf with the file path",
      "utils/helpers decompressTarGz() rejects a \"..\" member path without extracting",
      "utils/helpers decompressTarGz() rejects an absolute member path without extracting",
      "utils/helpers decompressTarGz() rejects with descriptive message on extraction error",
      "utils/helpers decompressTarGz() resolves true on success",
      "utils/helpers decompressTarGz() sets cwd to the parent directory of the file on extraction",
      "utils/helpers redactSecrets() accepts an Error and redacts secrets from its stack",
      "utils/helpers redactSecrets() masks IDENTIFIED BY '\u2026' literals",
      "utils/helpers redactSecrets() masks MYSQL_PWD / MYSQL_ROOT_PASSWORD env values",
      "utils/helpers redactSecrets() masks credentials embedded in a git/remote URL",
      "utils/helpers redactSecrets() masks the per-install service secrets in a failed docker-run argv",
      "utils/helpers redactSecrets() masks the value inside PASSWORD('\u2026')",
      "utils/helpers redactSecrets() returns clean strings unchanged and handles null",
      "utils/helpers sleep() resolves after the specified delay",
      "utils/helpers sleep() returns a promise that resolves",
      "utils/helpers stringToCoin() returns BITCOIN key for \"bitcoin\"",
      "utils/helpers stringToCoin() returns DOGECOIN key for \"dogecoin\"",
      "utils/helpers stringToCoin() returns LITECOIN key for \"litecoin\"",
      "utils/helpers stringToCoin() returns null for empty string",
      "utils/helpers stringToCoin() returns null for null",
      "utils/helpers stringToCoin() returns null for unknown coin",
      "utils/helpers stringToNetwork() handles single-segment string (no dash)",
      "utils/helpers stringToNetwork() parses \"bitcoin-mainnet\" correctly",
      "utils/helpers stringToNetwork() parses \"dogecoin-testnet\" correctly",
      "utils/helpers stringToNetwork() parses \"litecoin-regtest\" correctly",
      "utils/helpers stringToNetwork() returns null coin and network for unknown input",
      "utils/helpers stringToXChainService() returns XCHAIN_DECODER for \"xchain-decoder\"",
      "utils/helpers stringToXChainService() returns XCHAIN_E2E_TEST for \"xchain-e2e-test\"",
      "utils/helpers stringToXChainService() returns XCHAIN_ENCODER for \"xchain-encoder\"",
      "utils/helpers stringToXChainService() returns XCHAIN_INDEXER for \"xchain-indexer\"",
      "utils/helpers stringToXChainService() returns XCHAIN_REGTEST_MINER for \"xchain-regtest-miner\"",
      "utils/helpers stringToXChainService() returns XCHAIN_UTXO_TRACKER for \"xchain-utxo-tracker\"",
      "utils/helpers stringToXChainService() returns null for empty string",
      "utils/helpers stringToXChainService() returns null for unknown service"
    ],
    "1ea3450bb4fcc6a7": [
      "mutating command lock wait refuses a held lock immediately when the env var is unset (interactive default)",
      "mutating command lock wait takes the lock once the holder releases mid-wait",
      "mutating command lock wait waits for the holder when XCHAIN_NODE_MUTATING_LOCK_WAIT_MS is set"
    ],
    "2093a3bb7f34dc89": [
      "CLI `recreate` exit code exits NON-ZERO when every requested module was refused",
      "CLI `recreate` exit code exits NON-ZERO when recreate throws",
      "CLI `recreate` exit code exits ZERO when a container was actually recreated",
      "CLI `uninstall` exit code exits NON-ZERO when a module failed to uninstall",
      "CLI `uninstall` exit code exits ZERO when every module came down",
      "CLI `update` exit code exits NON-ZERO when the update changed nothing (silent no-op redeploy)",
      "CLI `update` exit code exits NON-ZERO when the update throws (skew-guard refusal)",
      "CLI `update` exit code exits ZERO when a module was actually updated",
      "CLI unhandled-rejection backstop keeps the stack when the reason is a real Error",
      "CLI unhandled-rejection backstop prints a string reason readably and exits non-zero",
      "CLI unhandled-rejection backstop registers exactly one handler"
    ],
    "2442df610ebb6911": [
      "coin-registry conformance (vendored copy) @regression byte-identity to canonical xchain-hub/src/coins BTC.js is byte-identical to the canonical xchain-hub copy",
      "coin-registry conformance (vendored copy) @regression byte-identity to canonical xchain-hub/src/coins DOGE.js is byte-identical to the canonical xchain-hub copy",
      "coin-registry conformance (vendored copy) @regression byte-identity to canonical xchain-hub/src/coins LTC.js is byte-identical to the canonical xchain-hub copy",
      "coin-registry conformance (vendored copy) @regression byte-identity to canonical xchain-hub/src/coins consensus_pin.js is byte-identical to the canonical xchain-hub copy",
      "coin-registry conformance (vendored copy) @regression byte-identity to canonical xchain-hub/src/coins index.js is byte-identical to the canonical xchain-hub copy",
      "coin-registry conformance (vendored copy) @regression pin == consensusHash over the vendored files BTC/regtest vendored pin matches the vendored consensusHash",
      "coin-registry conformance (vendored copy) @regression pin == consensusHash over the vendored files BTC/testnet vendored pin matches the vendored consensusHash",
      "coin-registry conformance (vendored copy) @regression pin == consensusHash over the vendored files DOGE/regtest vendored pin matches the vendored consensusHash",
      "coin-registry conformance (vendored copy) @regression pin == consensusHash over the vendored files DOGE/testnet vendored pin matches the vendored consensusHash",
      "coin-registry conformance (vendored copy) @regression pin == consensusHash over the vendored files LTC/regtest vendored pin matches the vendored consensusHash",
      "coin-registry conformance (vendored copy) @regression pin == consensusHash over the vendored files LTC/testnet vendored pin matches the vendored consensusHash",
      "coin-registry conformance (vendored copy) @regression pin == consensusHash over the vendored files verifyConsensusPin passes for every network on the vendored bundle"
    ],
    "26da132c6adf5f9c": [
      "GitHubDownloader _getHashForArch() returns arch-specific hash for object entry",
      "GitHubDownloader _getHashForArch() returns null for missing repo/version",
      "GitHubDownloader _getHashForArch() returns null for unknown arch in object entry",
      "GitHubDownloader _getHashForArch() returns string hash for legacy string entry (any arch)",
      "GitHubDownloader calculateDirectoryHash() computes SHA-256 hash from file contents",
      "GitHubDownloader constructor creates empty hashes file if it does not exist",
      "GitHubDownloader constructor loads hashes file on construction",
      "GitHubDownloader constructor throws on invalid hash format (non-64 char hex)",
      "GitHubDownloader downloadReleaseAsset() downloads gz asset using axios stream + pipeline, extracts with tar",
      "GitHubDownloader downloadReleaseAsset() extracts zip asset and calls unzip",
      "GitHubDownloader downloadReleaseAsset() handles single extracted directory by flattening contents",
      "GitHubDownloader downloadReleaseAsset() refuses to extract a gz asset with an unsafe member path",
      "GitHubDownloader downloadReleaseAsset() refuses to extract a zip asset with an unsafe member path",
      "GitHubDownloader downloadReleaseAsset() throws when no matching linux asset found for host arch",
      "GitHubDownloader downloadReleaseAsset() throws when tar fails with non-zero exit code",
      "GitHubDownloader downloadReleaseAsset() throws when unzip fails with non-zero exit code",
      "GitHubDownloader downloadReleaseAsset() warns on unrecognized file extension (no extraction)",
      "GitHubDownloader downloadReleaseAsset(): verifyHash=true calls verifyRepositoryHash when verifyHash=true",
      "GitHubDownloader downloadRepoVersion() cleans up the staging directory when download fails and leaves the previous tree alone",
      "GitHubDownloader downloadRepoVersion() extracts into a staging directory and swaps it over the previous version tree",
      "GitHubDownloader downloadRepoVersion() throws when hash not found for repo/version",
      "GitHubDownloader downloadRepoVersion() writes version file on successful download",
      "GitHubDownloader getAllFiles() returns empty array on error",
      "GitHubDownloader getAllFiles() returns single file in array when path is a file",
      "GitHubDownloader getAllFiles(): directory traversal returns all files recursively from a directory",
      "GitHubDownloader getLatestCompatibleVersion() returns the latest release regardless of hash when verifyHash=false",
      "GitHubDownloader getLatestCompatibleVersion() returns the latest release with a matching hash when verifyHash=true",
      "GitHubDownloader getLatestCompatibleVersion() throws when no version has a matching hash",
      "GitHubDownloader getReleaseByTag() calls correct GitHub API URL with tag",
      "GitHubDownloader getReleaseByTag(): error throws with descriptive message on error",
      "GitHubDownloader getReleases() a plain 403 (not rate-limited) keeps the generic API error",
      "GitHubDownloader getReleases() calls GitHub API with correct URL",
      "GitHubDownloader getReleases() returns release data on success",
      "GitHubDownloader getReleases() sends Authorization: Bearer when GITHUB_TOKEN is set",
      "GitHubDownloader getReleases() sends no Authorization header without a token",
      "GitHubDownloader getReleases() surfaces a rate-limit-specific error on 403 with remaining=0",
      "GitHubDownloader getReleases() throws with descriptive message on 404",
      "GitHubDownloader getReleases(): generic error throws with generic error message on non-404 errors",
      "GitHubDownloader hasHash() returns false for unknown repo",
      "GitHubDownloader hasHash() returns false when hash does not exist",
      "GitHubDownloader hasHash() returns true when hash exists for repo/version",
      "GitHubDownloader hasHash(): arch-specific returns true for object entry with matching arch",
      "GitHubDownloader hasHash(): arch-specific returns true for object entry with no arch requirement when keys exist",
      "GitHubDownloader loadHashesFile(): invalid hash in object entry throws when arch hash is invalid in object entry",
      "GitHubDownloader loadHashesFile(): invalid hash in object entry throws when top-level repo entry is not an object",
      "GitHubDownloader loadHashesFile(): invalid hash in object entry throws when version entry is neither string nor object",
      "GitHubDownloader verifyFileHash() computes the SHA-256 of the file bytes",
      "GitHubDownloader verifyFileHash() fails closed when no hash is registered for the version/arch",
      "GitHubDownloader verifyFileHash() passes when the tarball hash matches the registered per-arch hash",
      "GitHubDownloader verifyFileHash() throws when the tarball hash does not match",
      "GitHubDownloader verifyRepositoryHash() does not throw when hash matches",
      "GitHubDownloader verifyRepositoryHash() passes explicit arch parameter to _getHashForArch",
      "GitHubDownloader verifyRepositoryHash() throws when hash does not match",
      "GitHubDownloader verifyRepositoryHash() throws when no hash registered for the repo/version/arch"
    ],
    "2ac8a00f5806cacb": [
      "ValidatorService ROLLCALL status reporting getActiveSignerFile() falls back to the generated signer.js once init has written one",
      "ValidatorService ROLLCALL status reporting getActiveSignerFile() is null when no signer is configured at all",
      "ValidatorService ROLLCALL status reporting getActiveSignerFile() prefers an operator-supplied signer directory",
      "ValidatorService ROLLCALL status reporting getRollcallStatus() a streak of one reads as a warning shot, not an eviction",
      "ValidatorService ROLLCALL status reporting getRollcallStatus() a streak of two with evicted:1 on the head row reads as evicted",
      "ValidatorService ROLLCALL status reporting getRollcallStatus() degrades the DOGE runway to unavailable rather than reporting 0 roll calls",
      "ValidatorService ROLLCALL status reporting getRollcallStatus() degrades to unavailable, never a reassuring zero, when the absence read throws",
      "ValidatorService ROLLCALL status reporting getRollcallStatus() degrades to unavailable, not \"no absences\", when the indexer predates this read",
      "ValidatorService ROLLCALL status reporting getRollcallStatus() healthy: a DOGE runway, no absences, and a publish-capable signer",
      "ValidatorService ROLLCALL status reporting getRollcallStatus() reports a signer with no broadcast export, naming the file, independent of wallet state",
      "ValidatorService ROLLCALL status reporting rollcallAbsenceStreak() counts a single absence as a streak of one",
      "ValidatorService ROLLCALL status reporting rollcallAbsenceStreak() counts two absences one epoch apart as a streak of two",
      "ValidatorService ROLLCALL status reporting rollcallAbsenceStreak() only counts the run starting at the head of the array, never a streak buried deeper in the history",
      "ValidatorService ROLLCALL status reporting rollcallAbsenceStreak() returns 0 for an empty list (healthy: no absence on record, not \"unknown\")",
      "ValidatorService ROLLCALL status reporting rollcallAbsenceStreak() stops the streak at a rolled (present) epoch between two absences, even though only absences are in the array",
      "ValidatorService ROLLCALL status reporting rollcallEpochBlocks() is 30 on regtest and 1008 on testnet, mainnet, and anything unrecognized",
      "ValidatorService ROLLCALL status reporting signerModuleExportsBroadcast() is not fooled by a TODO comment INSIDE the exports block that mentions broadcast(",
      "ValidatorService ROLLCALL status reporting signerModuleExportsBroadcast() is not fooled by a contract comment BEFORE module.exports that only mentions broadcast",
      "ValidatorService ROLLCALL status reporting signerModuleExportsBroadcast() recognizes the CLI-generated template shape (object-literal broadcast beside walletSign)",
      "ValidatorService ROLLCALL status reporting signerModuleExportsBroadcast() recognizes the separate exports.broadcast = assignment form",
      "ValidatorService ROLLCALL status reporting signerModuleExportsBroadcast() returns false for a hand-built module that only signs",
      "ValidatorService ROLLCALL status reporting signerModuleExportsBroadcast() returns null when there is no signer file to check",
      "ValidatorService VALIDATOR_DIR is configDir/validator",
      "ValidatorService capability-config layout migration MOVES rather than copies, so only one config can ever be edited",
      "ValidatorService capability-config layout migration does not migrate the empty DIRECTORY docker leaves at a missing mount source",
      "ValidatorService capability-config layout migration is a no-op once migrated",
      "ValidatorService capability-config layout migration moves a pre-hub-caps capabilities.json into its own directory",
      "ValidatorService capability-drift probe alerts when a host that resolved standalone is still in a live set",
      "ValidatorService capability-drift probe alerts when a resolved validator is in NO capability set",
      "ValidatorService capability-drift probe an indexer error body reads as unknown rather than an empty set",
      "ValidatorService capability-drift probe degrades to unknown on an unknown network rather than guessing a coin",
      "ValidatorService capability-drift probe degrades to unknown when the indexer predates the capability-set read",
      "ValidatorService capability-drift probe degrades to unknown when the tip read fails, instead of probing a made-up block",
      "ValidatorService capability-drift probe does not alert on a standalone host whose key is in no set: that is a real config oracle",
      "ValidatorService capability-drift probe matches the answered pubkey case-insensitively",
      "ValidatorService capability-drift probe names each capability the indexer answers absent while this host serves it",
      "ValidatorService capability-drift probe never queries a capability the operator opted out of in capabilities.json",
      "ValidatorService capability-drift probe one capability erroring does not hide the answer for the others",
      "ValidatorService capability-drift probe renders the resolved side, the answered side and the alert in one report",
      "ValidatorService capability-drift probe reports no drift when every resolved capability answers IN the set",
      "ValidatorService capability-drift probe says there is nothing to compare, and asks no indexer, when the host has no identity",
      "ValidatorService capability-drift probe settles membership at the block it was handed, when one is given",
      "ValidatorService capability-drift probe still compares a DISABLED validator, whose key can be staked and in a set",
      "ValidatorService capability-drift probe treats a TRUNCATED set as unknown, never as \"not in the set\"",
      "ValidatorService coin wallets --force-wallets replaces them",
      "ValidatorService coin wallets --network names the federation and picks the matching port",
      "ValidatorService coin wallets --no-wallets skips wallet generation entirely",
      "ValidatorService coin wallets --oracle-epoch-start still overrides the federation default",
      "ValidatorService coin wallets both federation default epochs are in the past, and differ",
      "ValidatorService coin wallets creates that mountpoint at mount time too, for a config written before it existed",
      "ValidatorService coin wallets creates the node_modules mountpoint the SDK mount lands on",
      "ValidatorService coin wallets defaults ORACLE_EPOCH_START to the mainnet federation value",
      "ValidatorService coin wallets defaults ORACLE_EPOCH_START to the testnet federation value",
      "ValidatorService coin wallets does not claim a signer mount when the signer files are absent",
      "ValidatorService coin wallets fills oracle_publish in the fresh capabilities file from the DOGE wallet",
      "ValidatorService coin wallets generates a testnet stake wallet and a testnet DOGE wallet for port 10002",
      "ValidatorService coin wallets hub env wiring a validator without wallets gets no DOGE wiring (pre-wallets install)",
      "ValidatorService coin wallets hub env wiring an operator-supplied signer directory takes precedence over the generated one",
      "ValidatorService coin wallets hub env wiring hands the hub the DOGE publisher address, pubkey, encoder and signer module, never the WIF",
      "ValidatorService coin wallets hub env wiring host env wins over the recorded values",
      "ValidatorService coin wallets imports operator-supplied WIFs from the environment and derives their addresses",
      "ValidatorService coin wallets keeps existing wallets across --force (a funded address must not be abandoned silently)",
      "ValidatorService coin wallets leaves ORACLE_EPOCH_START null when the network is unknown",
      "ValidatorService coin wallets points the signer at the DOGE wallet and the public testnet encoder",
      "ValidatorService coin wallets publicWalletInfo never carries a WIF",
      "ValidatorService coin wallets refuses to import a key when the network is unknown",
      "ValidatorService coin wallets rejects a WIF from the wrong network",
      "ValidatorService coin wallets rejects an unknown --network",
      "ValidatorService coin wallets skips wallets on a non-standard port and says so, without failing init",
      "ValidatorService coin wallets the emitted signer marks post-funding failures does not tag a successful two-phase publish",
      "ValidatorService coin wallets the emitted signer marks post-funding failures is valid JavaScript once the template literal is expanded",
      "ValidatorService coin wallets the emitted signer marks post-funding failures leaves a pre-funding failure untagged, so the round stays retryable",
      "ValidatorService coin wallets the emitted signer marks post-funding failures tags a definitive phase-2 rejection with fundsCommitted and the phase-1 txid",
      "ValidatorService coin wallets writes wallets.env and the signer .env with mode 0600, and the signer module beside it",
      "ValidatorService container mount points keeps the hub-side file path unchanged, so the hub needs no change",
      "ValidatorService container mount points mounts the container DIRECTORY that path sits in",
      "ValidatorService getCapabilityConfigHostPath() returns CAPS_FILE path when initialized and caps file exists",
      "ValidatorService getCapabilityConfigHostPath() returns null when initialized but caps file missing",
      "ValidatorService getCapabilityConfigHostPath() returns null when not initialized",
      "ValidatorService getCapabilityConfigMountDir() REFUSES to hand the hub a directory that also holds the signing key",
      "ValidatorService getCapabilityConfigMountDir() refuses any other stray file in the mounted directory",
      "ValidatorService getCapabilityConfigMountDir() returns null when this node is not a validator",
      "ValidatorService getCapabilityConfigMountDir() returns the DIRECTORY holding the capability config, not the file",
      "ValidatorService getValidatorEnv() omits ORACLE_EPOCH_START when null",
      "ValidatorService getValidatorEnv() returns empty object when not initialized",
      "ValidatorService getValidatorEnv() returns env vars with SIGNING_PRIVKEY_HEX when initialized",
      "ValidatorService getValidatorSettings() returns null when not initialized",
      "ValidatorService getValidatorSettings() returns null when readFileSync throws",
      "ValidatorService getValidatorSettings() returns null when settings JSON is invalid",
      "ValidatorService getValidatorSettings() returns null when settings.enabled is false",
      "ValidatorService getValidatorSettings() returns settings when initialized and enabled=true",
      "ValidatorService initValidator() creates the capability-config directory and writes the config inside it",
      "ValidatorService initValidator() creates validator dir if it does not exist",
      "ValidatorService initValidator() defaults P2P_VALIDATOR_ADDR to 0.0.0.0:<port> when p2pAddr not given",
      "ValidatorService initValidator() falls back to the mainnet federation epoch when none is supplied",
      "ValidatorService initValidator() fills only the publisher placeholders in an existing capabilities file",
      "ValidatorService initValidator() hub API key a re-run over an already-initialized node does NOT mint a key on a keyless host",
      "ValidatorService initValidator() hub API key a re-run over an already-initialized node does NOT mint under --force either",
      "ValidatorService initValidator() hub API key a re-run over an already-initialized node mints when --mint-hub-api-key asks it to",
      "ValidatorService initValidator() hub API key a re-run over an already-initialized node names the consequence instead of minting silently",
      "ValidatorService initValidator() hub API key a re-run over an already-initialized node still reports an existing key, without rotating it",
      "ValidatorService initValidator() hub API key ensures a hub API key exists as part of init",
      "ValidatorService initValidator() hub API key prints no key material, only the path",
      "ValidatorService initValidator() hub API key reports a pre-existing key as reused rather than claiming a fresh one",
      "ValidatorService initValidator() hub API key tells the operator WHERE the credential lives",
      "ValidatorService initValidator() leaves an existing, operator-tuned capabilities file alone when force is false",
      "ValidatorService initValidator() re-generates key with force=true even when already initialized",
      "ValidatorService initValidator() records the network on a validator.json that predates the field",
      "ValidatorService initValidator() repairs a pre-wallets validator on a re-run, without touching the signing key",
      "ValidatorService initValidator() returns existing settings and never rotates the signing key when already initialized",
      "ValidatorService initValidator() sets ORACLE_EPOCH_START from opts.oracleEpochStart",
      "ValidatorService initValidator() sets P2P_PORT from opts.p2pPort",
      "ValidatorService initValidator() sets P2P_VALIDATOR_ADDR from opts.p2pAddr",
      "ValidatorService initValidator() sets SEED_NODES from comma-separated opts.seedNodes",
      "ValidatorService initValidator() skips validator dir creation if it already exists",
      "ValidatorService initValidator() uses partial capabilities from opts.capabilities",
      "ValidatorService initValidator() writes key file with mode 0600",
      "ValidatorService initValidator() writes key, settings, and capabilities files on first run",
      "ValidatorService isInitialized() returns false when key file is missing",
      "ValidatorService isInitialized() returns false when neither file exists",
      "ValidatorService isInitialized() returns false when settings file is missing",
      "ValidatorService isInitialized() returns true when both settings and key files exist",
      "ValidatorService pubkeyFromSeedHex() different seeds \u2192 different pubkeys",
      "ValidatorService pubkeyFromSeedHex() is deterministic: same seed \u2192 same pubkey",
      "ValidatorService pubkeyFromSeedHex() returns a 64-char hex string",
      "ValidatorService validatorModeReport() calls a HALF-present state incomplete, never standalone, and names what is missing",
      "ValidatorService validatorModeReport() handles missing SEED_NODES field gracefully",
      "ValidatorService validatorModeReport() is incomplete with the key present and the settings gone, the other half of the pair",
      "ValidatorService validatorModeReport() joins SEED_NODES array into comma-separated string",
      "ValidatorService validatorModeReport() reports standalone, and still names the directory it checked",
      "ValidatorService validatorModeReport() reports validator mode and the directory it resolved from",
      "ValidatorService validatorModeReport() separates a DISABLED validator from one that was never configured"
    ],
    "2ae188b9157fab7b": [
      "crossChainNetworksFor applies to the indexer only: every other module gets no extra networks",
      "crossChainNetworksFor derives names through getDockerNetwork, so a NODE_PREFIX override follows automatically",
      "crossChainNetworksFor excludes the container's own coin network (docker run already provides it)",
      "crossChainNetworksFor is inert against a missing or empty installed map",
      "crossChainNetworksFor is inert without a coin/network context (shared or one-shot containers)",
      "crossChainNetworksFor joins an indexer to every installed sibling coin on the SAME network tier",
      "crossChainNetworksFor never crosses network tiers: a testnet sibling is invisible to a regtest indexer",
      "crossChainNetworksFor returns all same-tier siblings, sorted, when several are installed"
    ],
    "2be99405409c0a53": [
      "ConfigService checkIfCryptoNodeSourceExists() returns false when Dockerfile missing",
      "ConfigService checkIfCryptoNodeSourceExists() returns true when dir, Dockerfile, and src exist",
      "ConfigService checkIfModuleExists() returns false when Dockerfile is missing",
      "ConfigService checkIfModuleExists() returns false when package.json is missing",
      "ConfigService checkIfModuleExists() returns false when src directory is missing",
      "ConfigService checkIfModuleExists() returns true when dir, Dockerfile, src, and package.json all exist",
      "ConfigService ensureHubApiKey() adopts a key the operator wrote by hand rather than replacing it",
      "ConfigService ensureHubApiKey() creates the config directory when it does not exist yet",
      "ConfigService ensureHubApiKey() generates a key into the shared hub sidecar on a host that has none",
      "ConfigService ensureHubApiKey() locks the sidecar to 0600, so no other local user can read the credential",
      "ConfigService ensureHubApiKey() never returns the key itself, only where it lives",
      "ConfigService ensureHubApiKey() preserves the other credentials already in the sidecar",
      "ConfigService ensureHubApiKey() readHubApiKey() leaves a sidecar that holds other credentials byte-identical",
      "ConfigService ensureHubApiKey() readHubApiKey() never returns the key itself, only whether there is one",
      "ConfigService ensureHubApiKey() readHubApiKey() reports a present key without rotating it",
      "ConfigService ensureHubApiKey() readHubApiKey() reports absence and writes NOTHING on a keyless host",
      "ConfigService ensureHubApiKey() reuses an existing key and leaves the file byte-identical",
      "ConfigService filterCommandParameters() accepts the short shared-service names operators actually type",
      "ConfigService filterCommandParameters() adds explorer to servicesList[\"\"][\"\"] when modules is \"all\"",
      "ConfigService filterCommandParameters() expands \"all\" coins to bitcoin, dogecoin, litecoin",
      "ConfigService filterCommandParameters() expands \"all\" modules to full service list plus node",
      "ConfigService filterCommandParameters() expands \"all\" networks to mainnet, testnet, regtest",
      "ConfigService filterCommandParameters() filters regtest-only modules from mainnet",
      "ConfigService filterCommandParameters() filters regtest-only modules from testnet",
      "ConfigService filterCommandParameters() handles \"explorer\" module name",
      "ConfigService filterCommandParameters() handles \"node\" module name",
      "ConfigService filterCommandParameters() handles null coins (expands to all)",
      "ConfigService filterCommandParameters() handles null networks (expands to all)",
      "ConfigService filterCommandParameters() includes regtest-miner for regtest but excludes e2e-test from all",
      "ConfigService filterCommandParameters() lists the coin node before every service under \"all\", so install creates it first",
      "ConfigService filterCommandParameters() passes single module/coin/network through unchanged",
      "ConfigService filterCommandParameters() returns correct structure for all coins + all networks + all modules",
      "ConfigService getCryptoNodeDir() returns correct path for BITCOIN (enum key)",
      "ConfigService getCryptoNodeDir() returns correct path for bitcoin (string value)",
      "ConfigService getCryptoNodeDir() returns correct path for dogecoin",
      "ConfigService getCryptoNodeDir() returns correct path for litecoin",
      "ConfigService getDefaultConfig() regtest-only env passthrough to the indexer carries every regtest-only var onto a regtest indexer",
      "ConfigService getDefaultConfig() regtest-only env passthrough to the indexer carries none of them onto mainnet, so a host variable cannot reach a shared ledger",
      "ConfigService getDefaultConfig() regtest-only env passthrough to the indexer carries none of them onto testnet, so a host variable cannot reach a shared ledger",
      "ConfigService getDefaultConfig() with coin and network (coin-specific config) HUB_API_KEY from the shared hub sidecar deploys the hub KEYED off the sidecar instead of declaring it keyless",
      "ConfigService getDefaultConfig() with coin and network (coin-specific config) HUB_API_KEY from the shared hub sidecar gives the co-located indexer the same key the hub runs with",
      "ConfigService getDefaultConfig() with coin and network (coin-specific config) HUB_API_KEY from the shared hub sidecar leaves a node with no generated key on its prior keyless declaration",
      "ConfigService getDefaultConfig() with coin and network (coin-specific config) HUB_API_KEY from the shared hub sidecar yields to a host-env key (an operator override is never overwritten)",
      "ConfigService getDefaultConfig() with coin and network (coin-specific config) HUB_DB_PASS falls back to the shared static default when rotation cannot apply it",
      "ConfigService getDefaultConfig() with coin and network (coin-specific config) HUB_DB_PASS is generated, shared, and persisted when rotation can apply it",
      "ConfigService getDefaultConfig() with coin and network (coin-specific config) LevelDB tuning passthrough (LEVELDB_CACHE_BYTES / LEVELDB_WRITE_BUFFER_BYTES) does not leak the LevelDB passthrough onto encoder or decoder configs",
      "ConfigService getDefaultConfig() with coin and network (coin-specific config) LevelDB tuning passthrough (LEVELDB_CACHE_BYTES / LEVELDB_WRITE_BUFFER_BYTES) emits neither key when the host env carries no LevelDB passthrough values",
      "ConfigService getDefaultConfig() with coin and network (coin-specific config) LevelDB tuning passthrough (LEVELDB_CACHE_BYTES / LEVELDB_WRITE_BUFFER_BYTES) passes LEVELDB_CACHE_BYTES and LEVELDB_WRITE_BUFFER_BYTES through from the host env",
      "ConfigService getDefaultConfig() with coin and network (coin-specific config) NODE_URL is the coin-scoped node container name, distinct per coin",
      "ConfigService getDefaultConfig() with coin and network (coin-specific config) ROLLCALL rail passthrough NEVER arms a shared-ledger indexer, whatever the host env says",
      "ConfigService getDefaultConfig() with coin and network (coin-specific config) ROLLCALL rail passthrough NEVER arms the gates flag on a shared-ledger indexer, whatever the host env says",
      "ConfigService getDefaultConfig() with coin and network (coin-specific config) ROLLCALL rail passthrough arms the container hub from the same variable, so the venue arms as a unit",
      "ConfigService getDefaultConfig() with coin and network (coin-specific config) ROLLCALL rail passthrough arms the container hub gates flag from the same variable, so the venue arms as a unit",
      "ConfigService getDefaultConfig() with coin and network (coin-specific config) ROLLCALL rail passthrough arms the indexer gates flag on regtest when the host opts in",
      "ConfigService getDefaultConfig() with coin and network (coin-specific config) ROLLCALL rail passthrough arms the indexer on regtest when the host opts in",
      "ConfigService getDefaultConfig() with coin and network (coin-specific config) ROLLCALL rail passthrough carries a bare arming height through unaltered",
      "ConfigService getDefaultConfig() with coin and network (coin-specific config) ROLLCALL rail passthrough does NOT inject the rollcall vars into a non-indexer coin module (decoder)",
      "ConfigService getDefaultConfig() with coin and network (coin-specific config) ROLLCALL rail passthrough omits every rollcall var when unset, so a venue ships INERT",
      "ConfigService getDefaultConfig() with coin and network (coin-specific config) ROLLCALL rail passthrough passes the DOGE proof peer through on testnet as well",
      "ConfigService getDefaultConfig() with coin and network (coin-specific config) ROLLCALL rail passthrough passes the DOGE proof peer through to the indexer on regtest",
      "ConfigService getDefaultConfig() with coin and network (coin-specific config) an operator NODE_URL override in the config file still wins over the default",
      "ConfigService getDefaultConfig() with coin and network (coin-specific config) an operator override in the main config file wins and is not regenerated",
      "ConfigService getDefaultConfig() with coin and network (coin-specific config) config file values override defaults",
      "ConfigService getDefaultConfig() with coin and network (coin-specific config) default values used when config file is empty",
      "ConfigService getDefaultConfig() with coin and network (coin-specific config) defaults INDEXER_ALLOW_UNAUTHENTICATED=true on keyless regtest installs (gated methods usable)",
      "ConfigService getDefaultConfig() with coin and network (coin-specific config) does NOT auto-generate DB passwords where rotation cannot apply them (native/no-container -> static default)",
      "ConfigService getDefaultConfig() with coin and network (coin-specific config) does not include REGTEST_MINER_URL for mainnet",
      "ConfigService getDefaultConfig() with coin and network (coin-specific config) does not include REGTEST_MINER_URL for testnet",
      "ConfigService getDefaultConfig() with coin and network (coin-specific config) encoder passthrough (ENCODER_TRUST_PROXY / ENCODER_RATE_LIMIT_RPM) does not leak the encoder passthrough onto decoder or utxo-tracker configs",
      "ConfigService getDefaultConfig() with coin and network (coin-specific config) encoder passthrough (ENCODER_TRUST_PROXY / ENCODER_RATE_LIMIT_RPM) emits neither key when the host env carries no encoder passthrough values",
      "ConfigService getDefaultConfig() with coin and network (coin-specific config) encoder passthrough (ENCODER_TRUST_PROXY / ENCODER_RATE_LIMIT_RPM) keeps the regtest 99999 literal when the host env sets no override",
      "ConfigService getDefaultConfig() with coin and network (coin-specific config) encoder passthrough (ENCODER_TRUST_PROXY / ENCODER_RATE_LIMIT_RPM) lets a host ENCODER_RATE_LIMIT_RPM win over the regtest 99999 literal",
      "ConfigService getDefaultConfig() with coin and network (coin-specific config) encoder passthrough (ENCODER_TRUST_PROXY / ENCODER_RATE_LIMIT_RPM) passes ENCODER_TRUST_PROXY and ENCODER_RATE_LIMIT_RPM through from the host env",
      "ConfigService getDefaultConfig() with coin and network (coin-specific config) generates per-install DB passwords under EXTERNAL_DB (native-path rotation)",
      "ConfigService getDefaultConfig() with coin and network (coin-specific config) generates per-install DB passwords when a DB container exists (rotation can apply them) and persists them",
      "ConfigService getDefaultConfig() with coin and network (coin-specific config) generates random NODE_USER and NODE_PASSWORD when absent from config file",
      "ConfigService getDefaultConfig() with coin and network (coin-specific config) generates the MISSING RPC credential when only one of NODE_USER/NODE_PASSWORD is present (#2404: no both-or-nothing fallback to \"rpc\")",
      "ConfigService getDefaultConfig() with coin and network (coin-specific config) genesis-ledger bootstrap passthrough does NOT inject genesis vars into a non-indexer module (decoder)",
      "ConfigService getDefaultConfig() with coin and network (coin-specific config) genesis-ledger bootstrap passthrough injects the genesis env vars into the indexer config from the host env",
      "ConfigService getDefaultConfig() with coin and network (coin-specific config) genesis-ledger bootstrap passthrough omits genesis vars entirely when unset (genesis stays disabled)",
      "ConfigService getDefaultConfig() with coin and network (coin-specific config) genesis-ledger bootstrap passthrough passes the airdrop set through to the indexer (regtest dry-run seam)",
      "ConfigService getDefaultConfig() with coin and network (coin-specific config) includes REGTEST_MINER_URL for regtest network",
      "ConfigService getDefaultConfig() with coin and network (coin-specific config) indexer HUB_DB_PASS matches the generated per-install INDEXER_DB_PASS when rotation can apply",
      "ConfigService getDefaultConfig() with coin and network (coin-specific config) indexer HUB_DB_PASS matches the indexer account static default when rotation cannot apply (never undefined)",
      "ConfigService getDefaultConfig() with coin and network (coin-specific config) keeps regtest fail-closed when a host INDEXER_API_KEY is provided",
      "ConfigService getDefaultConfig() with coin and network (coin-specific config) native-coin fee destination injection defaults to the vendored coin-registry pin when no host env is set",
      "ConfigService getDefaultConfig() with coin and network (coin-specific config) native-coin fee destination injection falls back to a generic FEE_DESTINATION host env var",
      "ConfigService getDefaultConfig() with coin and network (coin-specific config) native-coin fee destination injection ignores host env overrides on mainnet and injects the registry pin",
      "ConfigService getDefaultConfig() with coin and network (coin-specific config) native-coin fee destination injection injects FEE_DESTINATION (decoder) + per-coin var (indexer) from the host env",
      "ConfigService getDefaultConfig() with coin and network (coin-specific config) never hands the e2e-test container the signing seed, only the public half",
      "ConfigService getDefaultConfig() with coin and network (coin-specific config) omits INDEXER_API_KEY when unset in host env (indexer stays fail-closed)",
      "ConfigService getDefaultConfig() with coin and network (coin-specific config) omits VALIDATOR_PUBKEY on a standalone node, so the onboarding suite skips rather than staking a key nothing runs as",
      "ConfigService getDefaultConfig() with coin and network (coin-specific config) passes INDEXER_API_KEY through from host env to the indexer config (federation auth)",
      "ConfigService getDefaultConfig() with coin and network (coin-specific config) passes the validator pubkey to the e2e-test container when one is configured",
      "ConfigService getDefaultConfig() with coin and network (coin-specific config) points HUB_API_URL at the same hub container HUB_API_HOST names",
      "ConfigService getDefaultConfig() with coin and network (coin-specific config) recovers RPC credentials glued onto a preceding setting with no separating newline (#2405)",
      "ConfigService getDefaultConfig() with coin and network (coin-specific config) regtest mirror arming arms the regtest indexer hub-mirror pointer at its own DB account",
      "ConfigService getDefaultConfig() with coin and network (coin-specific config) regtest mirror arming defaults all three watermark graces to 0 on regtest when the host sets none of them",
      "ConfigService getDefaultConfig() with coin and network (coin-specific config) regtest mirror arming leaves mainnet/testnet mirror arming exactly as before (same account, no grace defaults)",
      "ConfigService getDefaultConfig() with coin and network (coin-specific config) regtest mirror arming lets a host-set grace value win over the regtest default",
      "ConfigService getDefaultConfig() with coin and network (coin-specific config) returns DECODER_DB_HOST as mariadb",
      "ConfigService getDefaultConfig() with coin and network (coin-specific config) returns DECODER_DB_PORT as 3306",
      "ConfigService getDefaultConfig() with coin and network (coin-specific config) returns HUB_PORT as 10000",
      "ConfigService getDefaultConfig() with coin and network (coin-specific config) returns NETWORK matching the network arg",
      "ConfigService getDefaultConfig() with coin and network (coin-specific config) returns correct DECODER_DB_NAME",
      "ConfigService getDefaultConfig() with coin and network (coin-specific config) returns correct DECODER_DB_USER format",
      "ConfigService getDefaultConfig() with coin and network (coin-specific config) returns correct INDEXER_COIN for dogecoin",
      "ConfigService getDefaultConfig() with coin and network (coin-specific config) returns correct INDEXER_COIN ticker",
      "ConfigService getDefaultConfig() with coin and network (coin-specific config) returns correct INDEXER_DB_NAME",
      "ConfigService getDefaultConfig() with coin and network (coin-specific config) returns correct INDEXER_DB_USER format",
      "ConfigService getDefaultConfig() with coin and network (coin-specific config) returns correct NODE_PORT for mainnet (8332)",
      "ConfigService getDefaultConfig() with coin and network (coin-specific config) returns correct NODE_PORT for regtest (18444)",
      "ConfigService getDefaultConfig() with coin and network (coin-specific config) returns correct NODE_PORT for testnet (18332)",
      "ConfigService getDefaultConfig() with coin and network (coin-specific config) returns correct UTXO_TRACKER_URL as Docker image name",
      "ConfigService getDefaultConfig() with coin and network (coin-specific config) reuses the persisted DB password on subsequent calls (stable across installs/updates)",
      "ConfigService getDefaultConfig() with coin and network (coin-specific config) sets HUB_API_URL so the indexer hub client is enabled",
      "ConfigService getDefaultConfig() without coin/network (shared service config) ATTEST response mirror regtest-only override passthrough injects both ATTEST override knobs from host env into the hub config",
      "ConfigService getDefaultConfig() without coin/network (shared service config) ATTEST response mirror regtest-only override passthrough injects only the one override set, leaving the other unset",
      "ConfigService getDefaultConfig() without coin/network (shared service config) ATTEST response mirror regtest-only override passthrough leaves both ATTEST override knobs unset when host env is absent",
      "ConfigService getDefaultConfig() without coin/network (shared service config) HUB_NETWORK passthrough injects HUB_NETWORK from host env into the hub config",
      "ConfigService getDefaultConfig() without coin/network (shared service config) HUB_NETWORK passthrough leaves HUB_NETWORK unset when host env is absent (standalone unchanged)",
      "ConfigService getDefaultConfig() without coin/network (shared service config) HUB_NETWORK passthrough reads no validator identity off the host filesystem",
      "ConfigService getDefaultConfig() without coin/network (shared service config) ORACLE_BATCH_* passthrough injects all five ORACLE_BATCH_* knobs from host env into the hub config",
      "ConfigService getDefaultConfig() without coin/network (shared service config) ORACLE_BATCH_* passthrough leaves all five ORACLE_BATCH_* knobs unset when host env is absent (hub default unchanged)",
      "ConfigService getDefaultConfig() without coin/network (shared service config) does not include coin-specific keys like NODE_PORT",
      "ConfigService getDefaultConfig() without coin/network (shared service config) emits INDEXER_API_URL_<COIN>_<NETWORK> for every coin and network",
      "ConfigService getDefaultConfig() without coin/network (shared service config) emits no EXPLORER_BATCH_RATE_LIMIT_RPM when the host env carries none",
      "ConfigService getDefaultConfig() without coin/network (shared service config) emits no per-route explorer rate-limit keys when the host env carries none",
      "ConfigService getDefaultConfig() without coin/network (shared service config) emits no serving-limit keys when the host env carries none",
      "ConfigService getDefaultConfig() without coin/network (shared service config) honours a HUB_PORT host env override",
      "ConfigService getDefaultConfig() without coin/network (shared service config) hub keyless declaration declares keyless operation when no HUB_API_KEY is in the host env",
      "ConfigService getDefaultConfig() without coin/network (shared service config) hub keyless declaration does NOT declare keyless on a mainnet hub",
      "ConfigService getDefaultConfig() without coin/network (shared service config) hub keyless declaration does not declare keyless when a key is present (the hub is keyed)",
      "ConfigService getDefaultConfig() without coin/network (shared service config) hub keyless declaration still declares keyless on a regtest hub",
      "ConfigService getDefaultConfig() without coin/network (shared service config) hub keyless declaration yields to an explicit host-env refusal (HUB_ALLOW_UNAUTHENTICATED=false)",
      "ConfigService getDefaultConfig() without coin/network (shared service config) leaves HUB_PORT at the 10000 default when host env sets no override",
      "ConfigService getDefaultConfig() without coin/network (shared service config) omits HUB_API_KEY from shared-service configs when unset in host env",
      "ConfigService getDefaultConfig() without coin/network (shared service config) passes HUB_API_KEY through from host env to shared-service configs (keyed sensitive-read tier)",
      "ConfigService getDefaultConfig() without coin/network (shared service config) passes the batch explorer rate limit through from the host env",
      "ConfigService getDefaultConfig() without coin/network (shared service config) passes the explorer serving limits through from the host env",
      "ConfigService getDefaultConfig() without coin/network (shared service config) passes the five per-route explorer rate limits through from the host env",
      "ConfigService getDefaultConfig() without coin/network (shared service config) returns EXPLORER_API_PORT_HTTP as 8080",
      "ConfigService getDefaultConfig() without coin/network (shared service config) returns EXPLORER_PORT as 18080",
      "ConfigService getDefaultConfig() without coin/network (shared service config) returns EXPLORER_PORT_HTTPS as 18081",
      "ConfigService getDefaultConfig() without coin/network (shared service config) returns HUB_PORT as 10000",
      "ConfigService getDefaultConfig() without coin/network (shared service config) returns SYNC_API_PORT as 3006",
      "ConfigService getDefaultConfig() without coin/network (shared service config) returns SYNC_MODE as server",
      "ConfigService getDefaultConfig() without coin/network (shared service config) yields INDEXER_API_URL_<COIN>_<NETWORK> to the host env",
      "ConfigService getDockerContainerImageName() builds coin-specific image name: xchain-node-bitcoin-mainnet-xchain-encoder",
      "ConfigService getDockerContainerImageName() builds correct names for all coin/network combos",
      "ConfigService getDockerContainerImageName() builds database name: xchain-node-database",
      "ConfigService getDockerContainerImageName() builds explorer name: xchain-node-xchain-explorer",
      "ConfigService getDockerContainerImageName() builds shared module name without coin/network: xchain-node-xchain-hub",
      "ConfigService getDockerContainerImageName() builds sync name: xchain-node-xchain-sync",
      "ConfigService getDockerNetwork() handles only coin without network",
      "ConfigService getDockerNetwork() returns xchain-node for empty coin and network",
      "ConfigService getDockerNetwork() returns xchain-node-bitcoin-mainnet for bitcoin mainnet",
      "ConfigService getModuleDatabaseName() allows the shared-service empty-string network",
      "ConfigService getModuleDatabaseName() capitalizes module name correctly (utxo-tracker -> Utxo-tracker)",
      "ConfigService getModuleDatabaseName() returns XChain_BTC_Mainnet_Decoder for bitcoin mainnet decoder",
      "ConfigService getModuleDatabaseName() returns XChain_DOGE_Testnet_Indexer for dogecoin testnet indexer",
      "ConfigService getModuleDatabaseName() returns XChain_LTC_Regtest_Decoder for litecoin regtest decoder",
      "ConfigService getModuleDatabaseName() throws on an unknown network (e.g. a typo from a raw CLI arg)",
      "ConfigService getModuleDatabaseName() uses correct ticker for all coins",
      "ConfigService getModuleDir() returns moduleDir + / + module",
      "ConfigService getModuleTmpDir() returns tmpDir + / + module",
      "ConfigService getUtxoTrackerVolumeName() keeps the legacy unprefixed name under the default NODE_PREFIX",
      "ConfigService getUtxoTrackerVolumeName() prefixes the name under a non-default NODE_PREFIX",
      "ConfigService moduleDirExists() returns false for non-existent module",
      "ConfigService moduleDirExists() returns true when directory exists",
      "ConfigService persistSidecarCreds() does not add a blank line when the existing file already ends in a newline",
      "ConfigService persistSidecarCreds() does not prepend a newline when creating a fresh file",
      "ConfigService persistSidecarCreds() inserts a separating newline when appending to a file not ending in one (#2406)",
      "ConfigService resolveArgs() service resolution leaves the branch slot free to take an arbitrary name",
      "ConfigService resolveArgs() service resolution names the valid services in the refusal, so the operator can correct it",
      "ConfigService resolveArgs() service resolution refuses an unrecognized service name instead of expanding to every service",
      "ConfigService resolveArgs() service resolution resolves the canonical shared-service names",
      "ConfigService resolveArgs() service resolution resolves the short shared-service aliases to their canonical names",
      "ConfigService resolveArgs() service resolution still accepts every legitimate argument shape"
    ],
    "2c16774921b9b66c": [
      "HubConnector: a non-throwing failure still records why a 401 makes updateConfig return false AND records the status in lastFailures",
      "HubConnector: a non-throwing failure still records why a successful push clears the previous failures rather than leaving them stale",
      "HubConnector: a non-throwing failure still records why an unreachable endpoint records the connection code, not a bare null",
      "HubConnector: a non-throwing failure still records why names every endpoint it tried, so a multi-endpoint failure is diagnosable"
    ],
    "2e8ada203563571b": [
      "clone integrity (manifest-pinned installs) cloneGit() commit verification REFUSES a clone whose HEAD differs from the pinned commit",
      "clone integrity (manifest-pinned installs) cloneGit() commit verification accepts a clone whose HEAD matches the pinned commit",
      "clone integrity (manifest-pinned installs) cloneGit() commit verification cleans the tmp tree when a probe clone fails verification",
      "clone integrity (manifest-pinned installs) cloneGit() commit verification leaves an EXISTING checkout untouched when a rewrite fails verification",
      "clone integrity (manifest-pinned installs) cloneGit() commit verification removes the bad tree when a fresh clone fails verification",
      "clone integrity (manifest-pinned installs) cloneGit() commit verification skips verification entirely when no commit is pinned (branch install)",
      "clone integrity (manifest-pinned installs) resolveBundledLibRef() INHERITS the parent module ref when no release is active",
      "clone integrity (manifest-pinned installs) resolveBundledLibRef() NEVER returns a null ref, on any path",
      "clone integrity (manifest-pinned installs) resolveBundledLibRef() announces the default-branch fallback rather than taking it silently",
      "clone integrity (manifest-pinned installs) resolveBundledLibRef() falls back to the default branch only on a detached parent with no manifest",
      "clone integrity (manifest-pinned installs) resolveBundledLibRef() falls back when the parent is not checked out at all",
      "clone integrity (manifest-pinned installs) resolveBundledLibRef() inherits a feature branch too, not just the well-known ones",
      "clone integrity (manifest-pinned installs) resolveBundledLibRef() uses the manifest pin during a release install",
      "clone integrity (manifest-pinned installs) source-level guard stages no bundled library with a null ref"
    ],
    "2fa482502d5d1dbe": [
      "BootstrapArchiveMeta buildBootstrapMeta() carries the format, the combo and an integer height",
      "BootstrapArchiveMeta buildBootstrapMeta() stamps a creation time when none is given",
      "BootstrapArchiveMeta buildBootstrapMeta() turns a height it cannot vouch for into null rather than a number",
      "BootstrapArchiveMeta parseTarHeader() reads the name, the octal size and the type flag, stripping a ./ prefix",
      "BootstrapArchiveMeta parseTarHeader() returns null for the end-of-archive zero block and for a short block",
      "BootstrapArchiveMeta readLeadingArchiveMember() refuses a leading member larger than the metadata ceiling",
      "BootstrapArchiveMeta readLeadingArchiveMember() skips pax extension headers ahead of the member",
      "BootstrapArchiveMeta writeBootstrapMeta() + readBootstrapArchiveMeta() answers null for a leading member that is not format-1 metadata",
      "BootstrapArchiveMeta writeBootstrapMeta() + readBootstrapArchiveMeta() answers null for an archive published before the member existed",
      "BootstrapArchiveMeta writeBootstrapMeta() + readBootstrapArchiveMeta() answers null when the member is present but does not lead the archive",
      "BootstrapArchiveMeta writeBootstrapMeta() + readBootstrapArchiveMeta() answers null, never rejects, for a missing, truncated or non-gzip file",
      "BootstrapArchiveMeta writeBootstrapMeta() + readBootstrapArchiveMeta() reads a null height as null and ignores a non-integer height",
      "BootstrapArchiveMeta writeBootstrapMeta() + readBootstrapArchiveMeta() reads the height off a large archive without reading the whole archive",
      "BootstrapArchiveMeta writeBootstrapMeta() + readBootstrapArchiveMeta() round-trips the height through a real wrapper when the member leads it"
    ],
    "31b05a9177135645": [
      "TelemetryConnector an explicit url argument wins over the env override",
      "TelemetryConnector defaults to the central hub telemetry URL",
      "TelemetryConnector honors the XCHAIN_NODE_TELEMETRY_URL override",
      "TelemetryConnector report() resolves false (never throws) when the endpoint is unreachable"
    ],
    "33a5dd7bc655d47e": [
      "SelfUpdateService compareVersions() orders numeric triples and ignores a leading v",
      "SelfUpdateService describeCarrier() answers not-a-repo when git refuses",
      "SelfUpdateService describeCarrier() lists dirty tracked files and the current commit",
      "SelfUpdateService explicitUpdateArgs() always names every slot and the tag, whatever the operator typed",
      "SelfUpdateService noticeNewerRelease() caches the lookup for an hour, including a failed one",
      "SelfUpdateService noticeNewerRelease() gives up on a slow lookup rather than holding the command",
      "SelfUpdateService noticeNewerRelease() prints one line naming the newer release and the command to run",
      "SelfUpdateService noticeNewerRelease() stays silent inside an update and never asks the network there",
      "SelfUpdateService noticeNewerRelease() stays silent when current",
      "SelfUpdateService selfUpdateAndReexec() does nothing inside the re-executed child (loop guard)",
      "SelfUpdateService selfUpdateAndReexec() does nothing when the CLI already runs the target version",
      "SelfUpdateService selfUpdateAndReexec() does nothing when told not to, and says so",
      "SelfUpdateService selfUpdateAndReexec() exits with the child status",
      "SelfUpdateService selfUpdateAndReexec() fetches, verifies the tag, checks it out, installs, hands back the lock and re-executes the explicit command",
      "SelfUpdateService selfUpdateAndReexec() moves anyway, loudly, when signature checks are switched off",
      "SelfUpdateService selfUpdateAndReexec() puts the previous commit back when npm install fails",
      "SelfUpdateService selfUpdateAndReexec() refuses a dirty tracked tree with nothing changed",
      "SelfUpdateService selfUpdateAndReexec() refuses a tag the release key did not sign, before anything is checked out",
      "SelfUpdateService selfUpdateAndReexec() warns and continues when the CLI is not a git checkout"
    ],
    "37d41880660612cd": [
      "secret-env ConfigService accepts the renamed sidecar keys a credential in the MAIN config file is relocated to the sidecar under either name",
      "secret-env ConfigService accepts the renamed sidecar keys a half-finished rename in a sidecar fails the config load instead of picking one",
      "secret-env ConfigService accepts the renamed sidecar keys a sidecar written entirely under the new names produces a working config",
      "secret-env ConfigService accepts the renamed sidecar keys an install still on the legacy names keeps working",
      "secret-env ConfigService accepts the renamed sidecar keys does not regenerate a credential that arrived under the new name",
      "secret-env ConfigService accepts the renamed sidecar keys the new name in the sidecar beats the legacy name in the main config file",
      "secret-env deprecatedSecretEnvNames() reports a legacy name that has no redaction-safe twin",
      "secret-env deprecatedSecretEnvNames() stays quiet once the rename has landed",
      "secret-env foldSecretEnvAliases() folds a redaction-safe name onto the canonical name and drops the alias",
      "secret-env foldSecretEnvAliases() leaves keys it does not govern alone",
      "secret-env foldSecretEnvAliases() names no value in the conflict message",
      "secret-env foldSecretEnvAliases() never hands a credential to a container under two names at once",
      "secret-env foldSecretEnvAliases() refuses a half-finished rename rather than guessing which value is current",
      "secret-env foldSecretEnvAliases() treats an EMPTY alias as absent so it cannot blank out a real credential",
      "secret-env readSecretHostEnv() falls back to the legacy name so no existing .env breaks on upgrade",
      "secret-env readSecretHostEnv() is a plain env read for a name it does not govern",
      "secret-env readSecretHostEnv() prefers the redaction-safe name from the host env",
      "secret-env readSecretHostEnv() throws on a conflicting half-finished rename",
      "secret-env sidecar helpers a rotation on a sidecar still using legacy names does not force a rename",
      "secret-env sidecar helpers a rotation writes back under the name the sidecar already uses",
      "secret-env sidecar helpers readSidecarValue() finds a secret under its redaction-safe name",
      "secret-env sidecar helpers readSidecarValue() is unchanged for keys with no alias",
      "secret-env sidecar helpers readSidecarValue() prefers the renamed key when a stale legacy line lingers",
      "secret-env the alias table agrees with the rename the platform audit gate tells operators to make",
      "secret-env the alias table agrees with the xchain-hub table on every key both own",
      "secret-env the alias table covers the three xchain-node sidecar keys the audit sweep flagged",
      "secret-env the alias table every preferred name is one automatic redaction actually matches"
    ],
    "3916386139147a8b": [
      "BootstrapNodeTipGuard assessNodeTipForRestore() XCHAIN_NODE_SKIP_NODE_TIP_GUARD skips the comparison, loudly",
      "BootstrapNodeTipGuard assessNodeTipForRestore() falls back to the module version when the service cannot be probed",
      "BootstrapNodeTipGuard assessNodeTipForRestore() is ok, quietly, when the node is past the archive, without probing the service",
      "BootstrapNodeTipGuard assessNodeTipForRestore() is unknown when the node cannot be asked, carrying the reason, and never throws",
      "BootstrapNodeTipGuard assessNodeTipForRestore() is unknown without archive metadata and does not ask the node at all",
      "BootstrapNodeTipGuard assessNodeTipForRestore() never asks the service or the version for the indexer",
      "BootstrapNodeTipGuard assessNodeTipForRestore() refuses when the running service lacks the field",
      "BootstrapNodeTipGuard assessNodeTipForRestore() restores with a warning when the service publishes node_catching_up",
      "BootstrapNodeTipGuard assessNodeTipForRestore() survives a metadata reader that throws",
      "BootstrapNodeTipGuard evaluateNodeTipAgainstArchive() is behind-refuse when the service does not wait, and the detail names both recoveries",
      "BootstrapNodeTipGuard evaluateNodeTipAgainstArchive() is behind-wait when the node is below and the service waits, with the gap and the ps marker in the detail",
      "BootstrapNodeTipGuard evaluateNodeTipAgainstArchive() is ok when the node is at or past the archive",
      "BootstrapNodeTipGuard evaluateNodeTipAgainstArchive() is unknown without a node answer, naming the archive height",
      "BootstrapNodeTipGuard evaluateNodeTipAgainstArchive() is unknown without an archive height, and says why",
      "BootstrapNodeTipGuard probeServiceCapability() answers null when every attempt fails, when the port is unknown, or when the container is missing",
      "BootstrapNodeTipGuard probeServiceCapability() retries the status probe and returns the first payload",
      "BootstrapNodeTipGuard readCoinNodeChainInfo() reads a missing initialblockdownload as false and a missing headers as null",
      "BootstrapNodeTipGuard readCoinNodeChainInfo() runs the coin CLI inside the registered node container with the image's conf arguments",
      "BootstrapNodeTipGuard readCoinNodeChainInfo() throws with a reason for an unknown coin, a missing container, and a non-JSON answer",
      "BootstrapNodeTipGuard serviceWaitsOutCatchUp() a readable payload WITHOUT the field decides against, whatever the version says",
      "BootstrapNodeTipGuard serviceWaitsOutCatchUp() a status payload that carries node_catching_up (even null) is the capability",
      "BootstrapNodeTipGuard serviceWaitsOutCatchUp() falls back to the version when the payload is unreadable: 0.16.0 is the first that waits",
      "BootstrapNodeTipGuard serviceWaitsOutCatchUp() the indexer always passes: it follows the decoder, not the node",
      "BootstrapNodeTipGuard version parsing accepts a leading v and ignores a suffix"
    ],
    "3b04cc05990c4f9a": [
      "InstallTargetService inferInstallTarget() classifies a node with no checkouts as a release node",
      "InstallTargetService inferInstallTarget() classifies any module on a named branch as a branch node on that branch",
      "InstallTargetService inferInstallTarget() classifies detached checkouts as a release node",
      "InstallTargetService inferInstallTarget() skips a directory that is not a git checkout",
      "InstallTargetService recordInstallTarget() / readInstallTarget() answers null for an unreadable or malformed record rather than throwing",
      "InstallTargetService recordInstallTarget() / readInstallTarget() answers null when there is no record",
      "InstallTargetService recordInstallTarget() / readInstallTarget() does not throw when the data dir cannot be written",
      "InstallTargetService recordInstallTarget() / readInstallTarget() refuses to record a target it could not act on",
      "InstallTargetService recordInstallTarget() / readInstallTarget() round-trips a branch target with no tag",
      "InstallTargetService recordInstallTarget() / readInstallTarget() round-trips a release target and creates the data dir on the way",
      "InstallTargetService resolveUpdateTarget() falls back to the checkouts when there is no record",
      "InstallTargetService resolveUpdateTarget() prefers the record over the checkouts"
    ],
    "40db4f1a28173b1f": [
      "ExplorerService: installExplorerModule() already running returns true immediately when ping succeeds and force=false",
      "ExplorerService: installExplorerModule() already running returns true when status is cached and explorer is in lastStatus (force=false)",
      "ExplorerService: installExplorerModule() exhausts retries throws when explorer never responds after 10 tries",
      "ExplorerService: installExplorerModule() force=true kills and removes existing container when force=true and container exists",
      "ExplorerService: installExplorerModule() force=true skips kill/remove when no existing container in DB (force=true)",
      "ExplorerService: installExplorerModule() force=true swallows errors from killContainer/removeContainer during force rebuild",
      "ExplorerService: installExplorerModule() full happy path clones and builds when ping initially fails and status cache is cold",
      "ExplorerService: installExplorerModule() full happy path clones, builds, then returns true when first ping succeeds",
      "ExplorerService: installExplorerModule() honours the install ref clones a release branch verbatim, so a frozen-ref e2e tests the frozen tree",
      "ExplorerService: installExplorerModule() honours the install ref clones at the branch the caller named",
      "ExplorerService: installExplorerModule() honours the install ref passes null when no ref was named, keeping the default-branch behaviour",
      "ExplorerService: installExplorerModule() honours the install ref passes the manifest pin (ref AND commit) when a release install is active",
      "ExplorerService: installExplorerModule() on a stack with no coins yet a healthy explorer is accepted whether or not a coin is installed",
      "ExplorerService: installExplorerModule() on a stack with no coins yet accepts an answering-but-degraded explorer when no coin is installed",
      "ExplorerService: installExplorerModule() on a stack with no coins yet refuses a container that never answers at all, coins or not",
      "ExplorerService: installExplorerModule() on a stack with no coins yet still REFUSES an answering-but-degraded explorer once a coin is installed",
      "ExplorerService: installExplorerModule() persistent attach failure exhausts the ping retries and throws instead of returning success",
      "ExplorerService: installExplorerModule() updateExplorer error in loop retries on updateExplorer error and returns true when subsequent ping+updateExplorer succeed",
      "ExplorerService: updateExplorer() connects explorer container to all installed coin networks",
      "ExplorerService: updateExplorer() rejects naming the unreachable networks when the attach keeps failing",
      "ExplorerService: updateExplorer() retries a failed attach once and returns true when the retry succeeds",
      "ExplorerService: updateExplorer() returns true immediately when explorer container is exited",
      "ExplorerService: updateExplorer() returns true immediately when explorer entry is absent from lastStatus",
      "ExplorerService: updateExplorer() skips network connection when no explorerContainerId found in DB",
      "ExplorerService: waitForExplorerReady() gives up early, reporting no problem, when nothing is listening at all",
      "ExplorerService: waitForExplorerReady() keeps waiting through degraded replies, then succeeds when it converges",
      "ExplorerService: waitForExplorerReady() reports false when a talking explorer never converges inside the budget",
      "ExplorerService: waitForExplorerReady() returns true as soon as the explorer reports healthy"
    ],
    "40fdfec30fa58f39": [
      "NodeService: buildCryptoNode() XCHAIN_NODE_BLOCKS_DIR (blocks + txindex split) bitcoin testnet: relocates txindex under testnet4 (bitcoin-testnet.conf sets testnet4=1)",
      "NodeService: buildCryptoNode() XCHAIN_NODE_BLOCKS_DIR (blocks + txindex split) bitcoin: -blocksdir against /blocks + relocates txindex",
      "NodeService: buildCryptoNode() XCHAIN_NODE_BLOCKS_DIR (blocks + txindex split) dogecoin mainnet: nests blocks onto the in-datadir path, never -blocksdir",
      "NodeService: buildCryptoNode() XCHAIN_NODE_BLOCKS_DIR (blocks + txindex split) dogecoin regtest: nests under the regtest subdir",
      "NodeService: buildCryptoNode() XCHAIN_NODE_BLOCKS_DIR (blocks + txindex split) dogecoin testnet: nests under the testnet3 subdir",
      "NodeService: buildCryptoNode() XCHAIN_NODE_BLOCKS_DIR (blocks + txindex split) litecoin testnet: relocates txindex under testnet4",
      "NodeService: buildCryptoNode() XCHAIN_NODE_BLOCKS_DIR (blocks + txindex split) litecoin: honors -blocksdir (Bitcoin 0.21 base)",
      "NodeService: buildCryptoNode() XCHAIN_NODE_BLOCKS_DIR (blocks + txindex split) no blocks/txindex mounts when the env var is unset",
      "NodeService: buildCryptoNode() aborts before the docker build when a host-port conflict is detected",
      "NodeService: buildCryptoNode() bakes no CRYPTO_NODE_VERSION env into the coin-node container",
      "NodeService: buildCryptoNode() blocks-dir persistence + mount-drift guard does not rewrite the sidecar when it already holds the same value",
      "NodeService: buildCryptoNode() blocks-dir persistence + mount-drift guard falls back to the persisted sidecar value when the env var is absent",
      "NodeService: buildCryptoNode() blocks-dir persistence + mount-drift guard persists the env value to the config/node.local sidecar",
      "NodeService: buildCryptoNode() blocks-dir persistence + mount-drift guard refuses to replace a container whose bind mounts the new spec would drop",
      "NodeService: buildCryptoNode() blocks-dir persistence + mount-drift guard replaces normally when the new spec keeps every existing bind mount",
      "NodeService: buildCryptoNode() blocks-dir persistence + mount-drift guard stop budget and its outcome falls back to the default on a value that is not a whole number of seconds, and says so",
      "NodeService: buildCryptoNode() blocks-dir persistence + mount-drift guard stop budget and its outcome reports a clean stop with the time it took and the budget",
      "NodeService: buildCryptoNode() blocks-dir persistence + mount-drift guard stop budget and its outcome says nothing about the stop when there was no previous daemon",
      "NodeService: buildCryptoNode() blocks-dir persistence + mount-drift guard stop budget and its outcome takes the budget from XCHAIN_NODE_STOP_TIMEOUT_SECONDS and stamps it on the new container",
      "NodeService: buildCryptoNode() blocks-dir persistence + mount-drift guard stop budget and its outcome warns when the daemon ran out of budget and was killed, naming the override",
      "NodeService: buildCryptoNode() blocks-dir persistence + mount-drift guard stops the previous daemon gracefully, with a flush budget, before force-removing it",
      "NodeService: buildCryptoNode() blocks-dir persistence + mount-drift guard treats an existing symlink at the blocks host path as provisioned (no mkdir)",
      "NodeService: buildCryptoNode() includes --restart unless-stopped in docker run args",
      "NodeService: buildCryptoNode() includes --ulimit nofile flag in docker run args",
      "NodeService: buildCryptoNode() includes port mapping when NODE_EXPOSED_PORT and NODE_PORT are configured",
      "NodeService: buildCryptoNode() reads and rewrites conf file when it exists",
      "NodeService: buildCryptoNode() rejects (does not hang) when docker run output is not a container id",
      "NodeService: buildCryptoNode() rejects (does not hang) when the blocksDir mkdir fails",
      "NodeService: buildCryptoNode() rejects (does not hang) when the docker build fails",
      "NodeService: buildCryptoNode() rejects when db.insertModuleContainer returns false",
      "NodeService: buildCryptoNode() rejects when docker run fails",
      "NodeService: buildCryptoNode() runs docker build with correct image name and cwd",
      "NodeService: buildCryptoNode() stores container ID and fires statusChanged on success",
      "NodeService: getCryptoNode() a broken mirror is not the whole install answers the all:true form Node actually calls it with",
      "NodeService: getCryptoNode() a broken mirror is not the whole install does not try other mirrors for an HTTP status, where every mirror agrees",
      "NodeService: getCryptoNode() a broken mirror is not the whole install names the URL, the cause and a way forward when every mirror fails",
      "NodeService: getCryptoNode() a broken mirror is not the whole install retries on another mirror address after a TLS failure and succeeds",
      "NodeService: getCryptoNode() a broken mirror is not the whole install still makes one attempt when the name cannot be resolved at all",
      "NodeService: getCryptoNode() a tarball the operator placed by hand is used, not destroyed discards a file that fails the pinned hash and downloads instead",
      "NodeService: getCryptoNode() a tarball the operator placed by hand is used, not destroyed never decompresses bytes that fail the pinned hash",
      "NodeService: getCryptoNode() a tarball the operator placed by hand is used, not destroyed skips the download entirely when the tarball is already at the path",
      "NodeService: getCryptoNode() a tarball the operator placed by hand is used, not destroyed still verifies the placed tarball against the pinned hash before using it",
      "NodeService: getCryptoNode() downloads bitcoin node for x64 arch, decompresses and renames",
      "NodeService: getCryptoNode() downloads dogecoin node via gitHubDownloader",
      "NodeService: getCryptoNode() downloads litecoin node via gitHubDownloader",
      "NodeService: getCryptoNode() rejects (and never decompresses) when the bitcoin tarball hash mismatches",
      "NodeService: getCryptoNode() rejects on a non-200 download response",
      "NodeService: getCryptoNode() rejects when decompressTarGz fails",
      "NodeService: getCryptoNode() strips leading \"v\" from bitcoin version string",
      "NodeService: getCryptoNode() throws for an unsupported coin",
      "NodeService: getCryptoNode() throws on unsupported architecture",
      "NodeService: getCryptoNode() uses rmSync when node version >= 14.14.0 and old bitcoin dir exists",
      "NodeService: getCryptoNode() verifies the downloaded bitcoin tarball before decompressing",
      "NodeService: installNode() bootstraps utxo tracker when volume is fresh",
      "NodeService: installNode() does NOT install regtest miner when network is mainnet",
      "NodeService: installNode() fetches remote version and calls getCryptoNode when not installed (dogecoin)",
      "NodeService: installNode() installs regtest miner when network is regtest",
      "NodeService: installNode() succeeds when local node version already installed (skips getCryptoNode)",
      "NodeService: installNode() throws when no valid remote version is available",
      "NodeService: node version pin (XCHAIN_NODE_NODE_VERSION_<COIN>) assertNodeVersionPin throws on a mismatched local daemon",
      "NodeService: node version pin (XCHAIN_NODE_NODE_VERSION_<COIN>) installNode downloads the pinned version, bypassing remote-latest lookup",
      "NodeService: node version pin (XCHAIN_NODE_NODE_VERSION_<COIN>) installNode fails loudly when a cached local daemon mismatches the pin",
      "NodeService: node version pin (XCHAIN_NODE_NODE_VERSION_<COIN>) resolveNodeVersionPin returns null when env unset or blank",
      "NodeService: node version pin (XCHAIN_NODE_NODE_VERSION_<COIN>) resolveNodeVersionPin returns the trimmed pin for the coin"
    ],
    "4405034a199528ec": [
      "coverage ratchet floors enforces every declared floor, at the declared value",
      "coverage ratchet floors fails the job on a shortfall rather than only reporting it",
      "coverage ratchet floors ships the coverage:check script the CI coverage job invokes"
    ],
    "482a3259eebf8fa8": [
      "DockerService addContainerToNetwork() connects container when not already in network",
      "DockerService addContainerToNetwork() rejects when network connect fails",
      "DockerService addContainerToNetwork() skips connect when container already in network",
      "DockerService checkContainerdDataRootRelocation() honors XCHAIN_NODE_CONTAINERD_ROOT override",
      "DockerService checkContainerdDataRootRelocation() returns null (best-effort) when docker info fails",
      "DockerService checkContainerdDataRootRelocation() returns null when containerd lives on a third off-root disk",
      "DockerService checkContainerdDataRootRelocation() returns null when containerd was also relocated off / (same disk as data-root)",
      "DockerService checkContainerdDataRootRelocation() returns null when data-root is still on the root filesystem",
      "DockerService checkContainerdDataRootRelocation() returns null when docker info returns an empty data-root",
      "DockerService checkContainerdDataRootRelocation() returns null when the containerd path does not exist",
      "DockerService checkContainerdDataRootRelocation() returns null when the data-root path is unreadable",
      "DockerService checkContainerdDataRootRelocation() warns when data-root moved off / but containerd is still on /",
      "DockerService checkDockerInstalledAndReachable() checkBuildKitAvailable rejects with the install hint when buildx is missing",
      "DockerService checkDockerInstalledAndReachable() checkBuildKitAvailable resolves when docker buildx version succeeds",
      "DockerService checkDockerInstalledAndReachable() rejects when docker --version fails",
      "DockerService checkDockerInstalledAndReachable() rejects when docker --version returns unexpected format",
      "DockerService checkDockerInstalledAndReachable() rejects when docker ps fails (user not in docker group)",
      "DockerService checkDockerInstalledAndReachable() resolves true when docker --version and docker ps succeed",
      "DockerService createDockerNetwork() creates network when inspect fails (network does not exist)",
      "DockerService createDockerNetwork() resolves true when network already exists",
      "DockerService createDockerNetwork(): network create failure rejects false when network create fails",
      "DockerService execContainer() runs docker exec -i <containerId> <commandArgs>",
      "DockerService execContainer(): error branch rejects when docker exec fails",
      "DockerService getDockerContainerFileCat() runs docker exec cat <path>",
      "DockerService getDockerContainerFileCat(): error branch rejects when docker exec cat fails",
      "DockerService getDockerContainerFileData() runs docker cp and reads the copied file",
      "DockerService getDockerContainerFileData(): error branch rejects when docker cp fails",
      "DockerService getDockerNetworkInspect() runs docker network inspect and parses JSON",
      "DockerService getDockerNetworkInspect(): error branch rejects when docker network inspect fails",
      "DockerService getPublishedHostPorts() ignores exposed-but-unpublished ports (no \"->\")",
      "DockerService getPublishedHostPorts() maps host ports to the containers publishing them (0.0.0.0 + :::)",
      "DockerService getPublishedHostPorts() parses host-IP-scoped bindings (127.0.0.1:13306->3306)",
      "DockerService getPublishedHostPorts() records multiple containers contending for the same host port",
      "DockerService getPublishedHostPorts() returns an empty map when docker ps fails (best-effort)",
      "DockerService getPublishedHostPorts() returns an empty map when there are no running containers",
      "DockerService getStatusFromContainer() rejects on exec error",
      "DockerService getStatusFromContainer() runs docker inspect and returns parsed JSON",
      "DockerService killContainer() runs docker kill <containerId>",
      "DockerService killContainer(): error branches rejects on exec error",
      "DockerService killContainer(): error branches rejects when stdout does not match container ID",
      "DockerService logContainer() calls spawn with --tail and --follow for follow=true",
      "DockerService logContainer() calls spawn without --follow for follow=false",
      "DockerService logContainer(): TTY keypress branches Ctrl-C keypress kills child process",
      "DockerService logContainer(): TTY keypress branches ESC keypress kills child process",
      "DockerService logContainer(): TTY keypress branches sets up stdin raw mode and keypress handler when follow=true and isTTY=true",
      "DockerService probeContainerPresenceByName() reports 'exists' on a clean 64-hex id",
      "DockerService probeContainerPresenceByName() reports 'gone' only when docker says no such container",
      "DockerService probeContainerPresenceByName() reports 'unknown' on a clean exit that yielded no id",
      "DockerService probeContainerPresenceByName() reports 'unknown' on a timeout, which carries no absence evidence",
      "DockerService probeContainerPresenceByName() reports 'unknown' when the daemon is unreachable",
      "DockerService removeContainer() rejects on other errors",
      "DockerService removeContainer() rejects when stdout does not match container ID and no stderr error",
      "DockerService removeContainer() runs docker rm <containerId>",
      "DockerService removeContainer() treats \"No such container\" as success (idempotent)",
      "DockerService restartContainer() runs docker restart <containerId>",
      "DockerService restartContainer(): error branches rejects when execFile errors",
      "DockerService restartContainer(): error branches rejects when stdout does not match container ID",
      "DockerService saveContainerLogs() rejects when spawn emits error",
      "DockerService saveContainerLogs() spawns docker logs and pipes to file, resolves when output finishes",
      "DockerService shellContainer() calls spawnSync with docker exec -it <containerId> bash",
      "DockerService startContainer() rejects on exec error",
      "DockerService startContainer() rejects when stdout does not match container ID",
      "DockerService startContainer() runs docker start <containerId>",
      "DockerService startDockerMonitor() rejects when containerIds is empty",
      "DockerService startDockerMonitor() rejects when containerIds is null",
      "DockerService startDockerMonitor(): with containers does not warn and keeps the plain banner when at most MAX_CONTAINERS are requested",
      "DockerService startDockerMonitor(): with containers sets up blessed UI, spawns docker logs for each container, resolves on q key",
      "DockerService startDockerMonitor(): with containers spawns docker logs without -f flag when follow=false",
      "DockerService startDockerMonitor(): with containers warns and names omitted containers plus labels banner \"n of N\" when more than MAX_CONTAINERS are requested",
      "DockerService stopContainer() rejects when stdout does not match",
      "DockerService stopContainer() runs docker stop <containerId>",
      "DockerService stopContainer(): error branches rejects on exec error",
      "DockerService stopContainerByName() reports a kill when the container exited 137",
      "DockerService stopContainerByName() reports not stopped, and does not inspect, when docker did not confirm the stop",
      "DockerService stopContainerByName() runs docker stop -t <budget> <name> and reads the exit code after it",
      "DockerService stopContainerByName() treats an unreadable exit code as clean unless the stop consumed the whole budget",
      "DockerService stringToDockerContainerFile() rejects on spawn error",
      "DockerService stringToDockerContainerFile() rejects when tee exits with non-zero code",
      "DockerService stringToDockerContainerFile() resolves true when tee exits with code 0",
      "DockerService waitContainer() rejects on error",
      "DockerService waitContainer() returns non-zero exit code",
      "DockerService waitContainer() runs docker wait and returns parsed exit code"
    ],
    "4e86adb287c39e1d": [
      "CLI `rollback` incident-path behaviour does NOT run the Docker/MariaDB precheck for a command that provisions nothing",
      "CLI `rollback` incident-path behaviour does NOT take the mutating command lock, so it cannot queue behind a deploy",
      "CLI `rollback` incident-path behaviour echoes the operator's own arguments back, so the guidance is copy-pasteable",
      "CLI `rollback` incident-path behaviour exits NON-ZERO instead of leaving the process alive on open handles",
      "CLI `rollback` incident-path behaviour leaves the precheck and the lock in place for a command that does provision",
      "CLI `rollback` incident-path behaviour names the reset-and-restore recovery path as runnable commands",
      "CLI `rollback` incident-path behaviour says it did nothing, so no operator reads the refusal as a completed rollback"
    ],
    "4fed77ea7d38ca97": [
      "ps: NODE UNREACHABLE surface reads null, an absent field, an older image and a malformed object as reachable",
      "ps: NODE UNREACHABLE surface reduces an unreachable payload to since, the last answer and the duration",
      "ps: NODE UNREACHABLE surface renders durations the way an operator scans them",
      "ps: NODE UNREACHABLE surface the note names the last answer when there was one",
      "ps: NODE UNREACHABLE surface the note says never answered, for how long, that it is not the IBD wait, and where to look",
      "ps: NODE UNREACHABLE surface tolerates a missing or negative duration",
      "ps: WAITING FOR NODE surface reads null, an absent field, an older image and a malformed object as not waiting",
      "ps: WAITING FOR NODE surface reduces a waiting payload to the two heights and the start of the wait",
      "ps: WAITING FOR NODE surface the note names the service, both heights, the remaining gap and that it resolves itself",
      "ps: WAITING FOR NODE surface tolerates a missing stored height",
      "ps: decoder REORG_HALT surface reads an older image with no reorg_halted field as not halted, and a cleared halt as not halted",
      "ps: decoder REORG_HALT surface reduces a halted health payload",
      "ps: decoder REORG_HALT surface returns null for anything that is not a payload",
      "ps: decoder REORG_HALT surface the note names the coin, the meaning, the halt reason and both recoveries"
    ],
    "5125fdb96876c007": [
      "EncoderMaintenanceWindow clearEncoderMaintenance() is a no-op with no encoder on this host",
      "EncoderMaintenanceWindow clearEncoderMaintenance() never throws when the removal fails",
      "EncoderMaintenanceWindow clearEncoderMaintenance() removes the sentinel from the encoder container",
      "EncoderMaintenanceWindow declareEncoderMaintenance() falls back to a generic reason rather than writing an empty one",
      "EncoderMaintenanceWindow declareEncoderMaintenance() honours an explicit window length",
      "EncoderMaintenanceWindow declareEncoderMaintenance() never throws when the container lookup fails",
      "EncoderMaintenanceWindow declareEncoderMaintenance() never throws when the write fails",
      "EncoderMaintenanceWindow declareEncoderMaintenance() reports false and writes nothing when there is no encoder here",
      "EncoderMaintenanceWindow declareEncoderMaintenance() writes a bounded, currently-open sentinel into the encoder container",
      "EncoderMaintenanceWindow sentinel path defaults to the path the encoder reads"
    ],
    "54cd8fb7321c2356": [
      "MariaDbStore assertReady() passes once the pool is open",
      "MariaDbStore assertReady() throws on an unconfigured store, naming the operation",
      "MariaDbStore countModules() returns 0 when no rows",
      "MariaDbStore countModules() returns 0 when pool is not ready",
      "MariaDbStore countModules() returns the number of registered modules",
      "MariaDbStore createDatabase() / isReady() / close() is idempotent: second createDatabase returns the existing pool",
      "MariaDbStore createDatabase() / isReady() / close() opens the pool and reports ready",
      "MariaDbStore createDatabase() / isReady() / close() reports not ready after close",
      "MariaDbStore createDatabase() / isReady() / close() reports not ready before createDatabase",
      "MariaDbStore createDatabase() / isReady() / close() throws when createDatabase is called without config",
      "MariaDbStore createDatabase() connection retry retries and succeeds after transient connection failures",
      "MariaDbStore createDatabase() connection retry throws after 6 failed connection attempts",
      "MariaDbStore getAllModuleContainers() always includes shared modules in filtered results",
      "MariaDbStore getAllModuleContainers() filters by coin and network and includes shared modules",
      "MariaDbStore getAllModuleContainers() returns all entries when no filters",
      "MariaDbStore getAllModuleContainers() returns empty array when no rows",
      "MariaDbStore getAllModuleContainers() returns empty array when pool is not ready",
      "MariaDbStore getAllModuleContainers() returns rows shaped as { module, coin, network, container_id }",
      "MariaDbStore insertModuleContainer() + getModuleContainer() coerces null coin/network to empty string for keying",
      "MariaDbStore insertModuleContainer() + getModuleContainer() get returns null when pool is not ready",
      "MariaDbStore insertModuleContainer() + getModuleContainer() insert is a no-op (returns false) when pool is not ready",
      "MariaDbStore insertModuleContainer() + getModuleContainer() keeps separate entries for different coin/network combos",
      "MariaDbStore insertModuleContainer() + getModuleContainer() overwrites existing entry on re-insert",
      "MariaDbStore insertModuleContainer() + getModuleContainer() returns null for non-existent key",
      "MariaDbStore insertModuleContainer() + getModuleContainer() returns true on successful insert",
      "MariaDbStore insertModuleContainer() + getModuleContainer() stores and retrieves a container ID",
      "MariaDbStore insertModuleContainer() + getModuleContainer() stores shared modules with empty coin/network",
      "MariaDbStore registry scoping by NODE_PREFIX gives a second stack its own table, so neither upsert nor purge can reach the first",
      "MariaDbStore registry scoping by NODE_PREFIX keeps the bare `modules` table on the default prefix, so an existing install migrates nothing",
      "MariaDbStore registry scoping by NODE_PREFIX never gives two distinct prefixes the same table, separator or length",
      "MariaDbStore registry scoping by NODE_PREFIX sanitizes a prefix that is legal for docker but not for a MariaDB identifier",
      "MariaDbStore removeModuleContainer() does not affect other entries",
      "MariaDbStore removeModuleContainer() entry is no longer retrievable after removal",
      "MariaDbStore removeModuleContainer() removes an existing entry and returns the container ID",
      "MariaDbStore removeModuleContainer() returns false when pool is not ready",
      "MariaDbStore removeModuleContainer() returns true for non-existent key (idempotent delete)"
    ],
    "5cc2b8143bdd5808": [
      "Security: remediated dependency advisories @regression @tier4 ADV-10: the installed mariadb reports a patched runtime version",
      "Security: remediated dependency advisories @regression @tier4 ADV-1: package.json pins axios at or above 1.18.0",
      "Security: remediated dependency advisories @regression @tier4 ADV-1: package.json pins brace-expansion at or above 5.0.9",
      "Security: remediated dependency advisories @regression @tier4 ADV-1: package.json pins fast-uri at or above 3.1.5",
      "Security: remediated dependency advisories @regression @tier4 ADV-1: package.json pins form-data at or above 4.0.6",
      "Security: remediated dependency advisories @regression @tier4 ADV-1: package.json pins ip-address at or above 10.3.1",
      "Security: remediated dependency advisories @regression @tier4 ADV-1: package.json pins js-yaml at or above 4.3.1",
      "Security: remediated dependency advisories @regression @tier4 ADV-1: package.json pins mariadb at or above 3.5.3",
      "Security: remediated dependency advisories @regression @tier4 ADV-1: package.json pins minimatch at or above 10.2.5",
      "Security: remediated dependency advisories @regression @tier4 ADV-1: package.json pins serialize-javascript at or above 7.0.5",
      "Security: remediated dependency advisories @regression @tier4 ADV-1: package.json pins shell-quote at or above 1.9.0",
      "Security: remediated dependency advisories @regression @tier4 ADV-1: package.json pins tmp at or above 0.2.6",
      "Security: remediated dependency advisories @regression @tier4 ADV-2: every axios entry in package-lock.json is at or above 1.18.0",
      "Security: remediated dependency advisories @regression @tier4 ADV-2: every brace-expansion entry in package-lock.json is at or above 5.0.9",
      "Security: remediated dependency advisories @regression @tier4 ADV-2: every fast-uri entry in package-lock.json is at or above 3.1.5",
      "Security: remediated dependency advisories @regression @tier4 ADV-2: every form-data entry in package-lock.json is at or above 4.0.6",
      "Security: remediated dependency advisories @regression @tier4 ADV-2: every ip-address entry in package-lock.json is at or above 10.3.1",
      "Security: remediated dependency advisories @regression @tier4 ADV-2: every js-yaml entry in package-lock.json is at or above 4.3.1",
      "Security: remediated dependency advisories @regression @tier4 ADV-2: every mariadb entry in package-lock.json is at or above 3.5.3",
      "Security: remediated dependency advisories @regression @tier4 ADV-2: every minimatch entry in package-lock.json is at or above 10.2.5",
      "Security: remediated dependency advisories @regression @tier4 ADV-2: every serialize-javascript entry in package-lock.json is at or above 7.0.5",
      "Security: remediated dependency advisories @regression @tier4 ADV-2: every shell-quote entry in package-lock.json is at or above 1.9.0",
      "Security: remediated dependency advisories @regression @tier4 ADV-2: every tmp entry in package-lock.json is at or above 0.2.6",
      "Security: remediated dependency advisories @regression @tier4 ADV-3: minimatch can still brace-expand through the overridden brace-expansion",
      "Security: remediated dependency advisories @regression @tier4 ADV-4: brace-expansion survives the CVE-2026-14257 unbounded-length input",
      "Security: remediated dependency advisories @regression @tier4 ADV-5: the installed axios reports a patched runtime version"
    ],
    "5cd927f17b924ef8": [
      "AutohealService --dry-run reports candidates without restarting or arming the cooldown",
      "AutohealService SERVICE_HEALTHCHECK opt-in flags xchain-indexer is opted in",
      "AutohealService SERVICE_HEALTHCHECK opt-in flags xchain-utxo-tracker is never opted in (deliberate stable-halt design)",
      "AutohealService caps the doubled cooldown at the ceiling instead of growing without bound",
      "AutohealService clears the persisted onset when the container recovers, so the next episode restarts the clock",
      "AutohealService does NOT restart a container an operator stopped, whose health is frozen at unhealthy",
      "AutohealService does NOT restart a healthy container",
      "AutohealService does NOT restart an unhealthy container whose service is not opted in (utxo-tracker)",
      "AutohealService does NOT restart the same container twice within the cooldown window",
      "AutohealService does NOT restart while still inside the grace window",
      "AutohealService doubles the cooldown for each restart that does not clear the wedge",
      "AutohealService drops the earned backoff when retained probes show a recovery past the restart probation",
      "AutohealService drops the episode onset while a container is stopped, so a restarted one gets a fresh grace window",
      "AutohealService getLastHealthyProbeMs returns null on an empty or missing log",
      "AutohealService getLastHealthyProbeMs returns null rather than throwing on an unparseable timestamp",
      "AutohealService getLastHealthyProbeMs returns null when every retained probe failed",
      "AutohealService getLastHealthyProbeMs returns the newest passing probe",
      "AutohealService getUnhealthySinceMs returns null on an empty log",
      "AutohealService getUnhealthySinceMs returns null when the newest probe passed",
      "AutohealService getUnhealthySinceMs returns the start of the trailing failing run",
      "AutohealService honors XCHAIN_NODE_AUTOHEAL_GRACE_MS override",
      "AutohealService keeps the earned backoff when a pass catches the container not running",
      "AutohealService keeps the earned backoff when a restarted container is still in Docker starting probation",
      "AutohealService keeps the earned backoff when the only retained pass falls inside restart probation",
      "AutohealService prunes persisted onsets for containers that left the registry",
      "AutohealService rates a pass as recovery only when no restart can claim it or it outlasted probation",
      "AutohealService reads a legacy state file that predates the unhealthySince map",
      "AutohealService records a failed restart without aborting the pass and reports it",
      "AutohealService refuses to run against an unconfigured module registry",
      "AutohealService resets the backoff when the container recovers, so the next episode starts at the base cooldown",
      "AutohealService restarts a container wedged past the grace window even though Health.Log only spans ~60s",
      "AutohealService restarts again once the cooldown window has elapsed",
      "AutohealService restarts an unhealthy container whose service opted in (autoheal: true)",
      "AutohealService restarts the grace clock when retained probes show a recovery after the persisted onset",
      "AutohealService skips (does not restart) when the health log gives no timing evidence",
      "AutohealService skips a container that cannot be inspected instead of failing the pass"
    ],
    "66a744498ed159be": [
      "SkewGuardService assertHubNotBehind REFUSES (fail-closed) when the hub is installed but its version is unreadable",
      "SkewGuardService assertHubNotBehind REFUSES when the installed hub is behind the required version",
      "SkewGuardService assertHubNotBehind honors the skip env var, loudly",
      "SkewGuardService assertHubNotBehind lists indexer, explorer and sync as hub-dependent",
      "SkewGuardService assertHubNotBehind passes when the installed hub meets or exceeds the required version",
      "SkewGuardService assertHubNotBehind proceeds when the module declares no constraint",
      "SkewGuardService assertHubNotBehind proceeds when the module source cannot be cloned (update will fail there anyway)",
      "SkewGuardService assertHubNotBehind proceeds with a warning on a hubless stack",
      "SkewGuardService assertHubNotBehind skips non-hub-dependent modules without cloning",
      "SkewGuardService assertHubNotBehind threads the requested branch into the tmp clone",
      "SkewGuardService compareVersions orders numerically, not lexically",
      "SkewGuardService compareVersions pads missing segments with zero",
      "SkewGuardService compareVersions treats equal versions as equal, with or without v prefix",
      "SkewGuardService getRequiredHubVersion returns null when the field is absent",
      "SkewGuardService getRequiredHubVersion returns the trimmed version when declared",
      "SkewGuardService getRequiredHubVersion throws on a non-version value (range syntax not supported)"
    ],
    "6e3e97a6bec46f56": [
      "ConfigService hub network and BTC indexer composition BTC_INDEXER_API_URL composes the co-located BTC indexer for the hub network",
      "ConfigService hub network and BTC indexer composition BTC_INDEXER_API_URL composes the indexer the running install is about to create",
      "ConfigService hub network and BTC indexer composition BTC_INDEXER_API_URL ignores a BTC indexer installed on a different network",
      "ConfigService hub network and BTC indexer composition BTC_INDEXER_API_URL leaves it empty when this deployment runs no BTC indexer",
      "ConfigService hub network and BTC indexer composition BTC_INDEXER_API_URL never overrides the operator host env",
      "ConfigService hub network and BTC indexer composition BTC_INDEXER_API_URL says it once however often the config is composed",
      "ConfigService hub network and BTC indexer composition BTC_INDEXER_API_URL warns, naming the consequence, when nothing can be composed",
      "ConfigService hub network and BTC indexer composition HUB_NETWORK does not change the keyless declaration a mainnet install already got",
      "ConfigService hub network and BTC indexer composition HUB_NETWORK leaves HUB_NETWORK unset when the deployment spans two networks",
      "ConfigService hub network and BTC indexer composition HUB_NETWORK never overrides the operator host env",
      "ConfigService hub network and BTC indexer composition HUB_NETWORK prefers the registered stacks over the command tokens",
      "ConfigService hub network and BTC indexer composition HUB_NETWORK takes the network the running install names when no stack is registered yet"
    ],
    "774bd66498878fa9": [
      "ReleaseManifestService getComponentPin() THROWS when a pin has a malformed commit",
      "ReleaseManifestService getComponentPin() THROWS when a pin has no commit",
      "ReleaseManifestService getComponentPin() returns null for a component the manifest does not carry",
      "ReleaseManifestService getComponentPin() returns null for an empty manifest rather than inventing a pin",
      "ReleaseManifestService getComponentPin() returns the tag and commit for a pinned component",
      "ReleaseManifestService isReleaseRef() classifies '' as NOT a release",
      "ReleaseManifestService isReleaseRef() classifies 'develop' as NOT a release",
      "ReleaseManifestService isReleaseRef() classifies 'feature/v2-rewrite' as NOT a release",
      "ReleaseManifestService isReleaseRef() classifies 'hotfix/v0.9.1' as NOT a release",
      "ReleaseManifestService isReleaseRef() classifies 'master' as NOT a release",
      "ReleaseManifestService isReleaseRef() classifies 'release/v0.9.0' as NOT a release",
      "ReleaseManifestService isReleaseRef() classifies 'v0.9' as NOT a release",
      "ReleaseManifestService isReleaseRef() classifies 'v0.9.0.1' as NOT a release",
      "ReleaseManifestService isReleaseRef() classifies a legacy bare component version as a branch, not a release",
      "ReleaseManifestService isReleaseRef() classifies v0.9.0 as a release",
      "ReleaseManifestService isReleaseRef() classifies v0.9.0+build.5 as a release",
      "ReleaseManifestService isReleaseRef() classifies v1.0.0 as a release",
      "ReleaseManifestService isReleaseRef() classifies v1.0.0-rc.1 as a release",
      "ReleaseManifestService isReleaseRef() classifies v10.20.30 as a release",
      "ReleaseManifestService isReleaseRef() rejects non-strings",
      "ReleaseManifestService manifestHasPins() is false for null and for a manifest with no components key",
      "ReleaseManifestService manifestHasPins() is false for the shipped pre-first-train manifest",
      "ReleaseManifestService manifestHasPins() is true once a component is pinned",
      "ReleaseManifestService resolveComponentRef() OVERRIDES a caller-supplied fallback during a release install",
      "ReleaseManifestService resolveComponentRef() clearActiveTarget() ends pinned mode",
      "ReleaseManifestService resolveComponentRef() falls back for a component the release does not pin",
      "ReleaseManifestService resolveComponentRef() returns the fallback ref when the active install is a branch install",
      "ReleaseManifestService resolveComponentRef() returns the fallback ref, unpinned, when no install is active",
      "ReleaseManifestService resolveComponentRef() returns the manifest pin during a release install",
      "ReleaseManifestService resolveInstallTarget() classifies a branch ref without touching the network",
      "ReleaseManifestService resolveInstallTarget() falls back to the default branch when NO release exists (pre-first-train)",
      "ReleaseManifestService resolveInstallTarget() falls back to the default branch when the lookup fails outright",
      "ReleaseManifestService resolveInstallTarget() loads the manifest for an explicit release ref",
      "ReleaseManifestService resolveInstallTarget() prefers the LOCAL manifest when the running checkout is that release",
      "ReleaseManifestService resolveInstallTarget() refuses to fall back to a branch when told not to and no release exists",
      "ReleaseManifestService resolveInstallTarget() refuses to fall back to a branch when told not to and the lookup fails",
      "ReleaseManifestService resolveInstallTarget() rejects a manifest that is not valid JSON",
      "ReleaseManifestService resolveInstallTarget() reports a release ref that has no manifest, instead of installing tips",
      "ReleaseManifestService resolveInstallTarget() resolves the latest release when no ref is given",
      "ReleaseManifestService resolveLatestReleaseTag() propagates a non-404 failure rather than reporting \"no release\"",
      "ReleaseManifestService resolveLatestReleaseTag() returns null (not an error) when no release has been published yet",
      "ReleaseManifestService resolveLatestReleaseTag() returns the tag from releases/latest",
      "ReleaseManifestService the provenance gate a refusal from the gate aborts the install",
      "ReleaseManifestService the provenance gate fetchReleaseAsset downloads the asset bytes",
      "ReleaseManifestService the provenance gate fetchReleaseAsset follows the storage redirect WITHOUT the credentials",
      "ReleaseManifestService the provenance gate fetchReleaseAsset returns null when the release carries no such asset",
      "ReleaseManifestService the provenance gate fetchReleaseAsset returns null when the release itself is absent",
      "ReleaseManifestService the provenance gate verifies the fetched manifest BEFORE parsing it",
      "ReleaseManifestService the shipped manifest declares a platform version and a release date",
      "ReleaseManifestService the shipped manifest does not pin xchain-node itself",
      "ReleaseManifestService the shipped manifest pins every component to a tag and a full 40-hex commit",
      "ReleaseManifestService the shipped manifest pins no component above the platform version it ships in"
    ],
    "79cc27613f3e7980": [
      "cli maybeSelfUpdateBeforeUpdate() does not move the CLI for a branch update",
      "cli maybeSelfUpdateBeforeUpdate() does not move the CLI on a branch node",
      "cli maybeSelfUpdateBeforeUpdate() does nothing inside the re-executed child",
      "cli maybeSelfUpdateBeforeUpdate() does nothing when self-update is switched off",
      "cli maybeSelfUpdateBeforeUpdate() hands the resolved tag on when the CLI is already there, so the action does not look it up again",
      "cli maybeSelfUpdateBeforeUpdate() leaves an unparseable invocation for the action to report",
      "cli maybeSelfUpdateBeforeUpdate() lets a lookup failure propagate as the update failure",
      "cli maybeSelfUpdateBeforeUpdate() serializes on the command lock and hands it to the move as the pre-spawn release",
      "cli maybeSelfUpdateBeforeUpdate() targets the latest release for a no-ref update on a release node",
      "cli maybeSelfUpdateBeforeUpdate() targets the named release and keeps the operator's scope in the child command"
    ],
    "82171a751987af6e": [
      "nine-field FILE scan (pre-window gate) BATCH unwrapping a clean BATCH is clean",
      "nine-field FILE scan (pre-window gate) BATCH unwrapping finds a nine-field FILE inside a BATCH",
      "nine-field FILE scan (pre-window gate) BATCH unwrapping splits every command, not just the first",
      "nine-field FILE scan (pre-window gate) BATCH unwrapping tolerates a trailing separator without inventing an empty command",
      "nine-field FILE scan (pre-window gate) scanning rows counts what it scanned and reports every hit with its location",
      "nine-field FILE scan (pre-window gate) scanning rows handles no rows at all",
      "nine-field FILE scan (pre-window gate) scanning rows survives null and empty payloads",
      "nine-field FILE scan (pre-window gate) the gate report counts only payload-bearing rows as the corpus",
      "nine-field FILE scan (pre-window gate) the gate report distinguishes clean-by-emptiness from a scan that examined rows",
      "nine-field FILE scan (pre-window gate) the gate report names every hit and refuses to let it pass as a footnote",
      "nine-field FILE scan (pre-window gate) the gate report says CLEAN in a way that states what was established",
      "nine-field FILE scan (pre-window gate) the gate report says so when the corpus was not measured, rather than implying a full scan",
      "nine-field FILE scan (pre-window gate) the query reads the RAW payload, which is the whole point filters on the raw payload prefix and pages by keyset",
      "nine-field FILE scan (pre-window gate) the query reads the RAW payload, which is the whole point selects the decoder payload column and never a parsed params table",
      "nine-field FILE scan (pre-window gate) what is a hit does NOT flag a non-gated FILE with trailing fields stripped",
      "nine-field FILE scan (pre-window gate) what is a hit does NOT flag a trailing EMPTY ninth field",
      "nine-field FILE scan (pre-window gate) what is a hit does NOT flag the ordinary eight-field form",
      "nine-field FILE scan (pre-window gate) what is a hit flags a FILE carrying a non-empty ninth field",
      "nine-field FILE scan (pre-window gate) what is a hit flags a TENTH field even when the ninth is empty, because the wire is malformed",
      "nine-field FILE scan (pre-window gate) what is a hit ignores actions that are not FILE",
      "nine-field FILE scan (pre-window gate) what is a hit is case- and whitespace-tolerant on the action name"
    ],
    "842ea67df5e152c0": [
      "TelemetryService.gatherModules() (via gatherPayload) adds a null-safe `health` field and leaves `running` a pure liveness signal",
      "TelemetryService.gatherModules() (via gatherPayload) never throws a TypeError when status shape is sparse"
    ],
    "88431f058ac97635": [
      "MigrationPreconditionService assertRequiredMigrationsApplied checks the database belonging to the module, coin and network being updated",
      "MigrationPreconditionService assertRequiredMigrationsApplied covers the indexer and the decoder, the two migration-bearing modules",
      "MigrationPreconditionService assertRequiredMigrationsApplied does NOT print a scoped command the running build would ignore",
      "MigrationPreconditionService assertRequiredMigrationsApplied does not touch the database when the target source declares no preconditions",
      "MigrationPreconditionService assertRequiredMigrationsApplied is inert for a module that ships no migrations",
      "MigrationPreconditionService assertRequiredMigrationsApplied passes when every declared precondition is in the ledger",
      "MigrationPreconditionService assertRequiredMigrationsApplied proceeds on a genuinely empty database (a fresh install cannot be behind)",
      "MigrationPreconditionService assertRequiredMigrationsApplied proceeds with a warning when the source itself cannot be cloned",
      "MigrationPreconditionService assertRequiredMigrationsApplied proceeds, loudly, when the skip env is set",
      "MigrationPreconditionService assertRequiredMigrationsApplied reads the source tree about to be deployed, at the pinned ref",
      "MigrationPreconditionService assertRequiredMigrationsApplied refuses when the migration state cannot be read, and says so is not the same as missing",
      "MigrationPreconditionService assertRequiredMigrationsApplied refuses when the target DB has not applied a declared precondition",
      "MigrationPreconditionService assertRequiredMigrationsApplied still refuses when the capability probe itself throws",
      "MigrationPreconditionService assertRequiredMigrationsApplied treats an unreadable container as lacking the capability",
      "MigrationPreconditionService listDeployPreconditionMigrations reads the REAL indexer tree and finds the bridge-tables migration",
      "MigrationPreconditionService listDeployPreconditionMigrations reads the REAL indexer tree and finds the migration behind the 2026-08-09 halt",
      "MigrationPreconditionService listDeployPreconditionMigrations returns [] for a missing directory (a ref with no migrations declares nothing)",
      "MigrationPreconditionService listDeployPreconditionMigrations returns only the tagged .sql files, sorted",
      "MigrationPreconditionService migrationDeclaresDeployPrecondition ignores the token on a comment line that is not the directive",
      "MigrationPreconditionService migrationDeclaresDeployPrecondition ignores the token once the SQL body has started",
      "MigrationPreconditionService migrationDeclaresDeployPrecondition is false for an ordinary migration and for an empty file",
      "MigrationPreconditionService migrationDeclaresDeployPrecondition reads the tag off the xchain:migration directive line",
      "MigrationPreconditionService migrationDeclaresDeployPrecondition sees the tag through a long license banner",
      "MigrationPreconditionService migrationDeclaresDeployPrecondition tolerates spacing around the token",
      "MigrationPreconditionService migrationMode ignores a mode token that appears after the prologue",
      "MigrationPreconditionService migrationMode reads the mode a header declares",
      "MigrationPreconditionService migrationMode returns null when no mode is declared",
      "MigrationPreconditionService pendingManualMigrations excludes what the ledger already carries",
      "MigrationPreconditionService pendingManualMigrations lists only manual migrations the ledger has not recorded",
      "MigrationPreconditionService pendingManualMigrations yields nothing for a missing directory rather than throwing",
      "MigrationPreconditionService readAppliedMigrations calls a database with no tables empty, not unreadable",
      "MigrationPreconditionService readAppliedMigrations calls a populated database with no ledger table UNREADABLE, never empty",
      "MigrationPreconditionService readAppliedMigrations reads the ledger into a set of applied names",
      "MigrationPreconditionService readAppliedMigrations refuses a database name that is not a plain identifier, before any query runs",
      "MigrationPreconditionService readAppliedMigrations refuses an empty-string table count instead of reading it as zero",
      "MigrationPreconditionService readAppliedMigrations refuses an unreadable ledger-presence count instead of reading it as \"no ledger\"",
      "MigrationPreconditionService readAppliedMigrations refuses an unreadable table count instead of collapsing it into empty-database",
      "MigrationPreconditionService readAppliedMigrations tolerates a ledger read that comes back empty",
      "MigrationPreconditionService readAppliedMigrations turns a driver failure into unreadable instead of throwing past the guard"
    ],
    "8b127ec2a1140cac": [
      "BootstrapHealthGate XCHAIN_NODE_BOOTSTRAP_SKIP_HEALTH_GATE=1 bypasses the gate (loudly)",
      "BootstrapHealthGate container state REFUSES a container that restarted moments ago",
      "BootstrapHealthGate container state REFUSES a restarting (crash-looping) container",
      "BootstrapHealthGate container state REFUSES a stopped container",
      "BootstrapHealthGate container state REFUSES when docker inspect cannot be run",
      "BootstrapHealthGate container state REFUSES when docker reports the healthcheck unhealthy",
      "BootstrapHealthGate container state REFUSES when no container is registered",
      "BootstrapHealthGate container state accepts a container that restarted long ago and has been stable since",
      "BootstrapHealthGate container state reads RestartCount from the top level, not from .State (docker rejects the latter)",
      "BootstrapHealthGate durable halt markers REFUSES a MariaDB source whose schema reports no events table",
      "BootstrapHealthGate durable halt markers REFUSES a REORG_HALT count that is a partial number",
      "BootstrapHealthGate durable halt markers REFUSES a database carrying an uncleared xchain-sync divergence halt",
      "BootstrapHealthGate durable halt markers REFUSES a decoder whose database carries a REORG_HALT row",
      "BootstrapHealthGate durable halt markers REFUSES a marker-table count outside 0..1",
      "BootstrapHealthGate durable halt markers REFUSES a negative REORG_HALT count",
      "BootstrapHealthGate durable halt markers REFUSES a sync_halt count that is a partial number",
      "BootstrapHealthGate durable halt markers REFUSES an indexer when the paired decoder database is absent",
      "BootstrapHealthGate durable halt markers REFUSES an indexer when the paired decoder probe throws",
      "BootstrapHealthGate durable halt markers REFUSES an indexer whose PAIRED DECODER database carries a REORG_HALT row",
      "BootstrapHealthGate durable halt markers REFUSES an indexer whose paired decoder database carries an uncleared sync halt",
      "BootstrapHealthGate durable halt markers REFUSES in external-DB mode when the native probe answers with nothing",
      "BootstrapHealthGate durable halt markers REFUSES when a marker-table token is a partial number, and does not skip sync_halt",
      "BootstrapHealthGate durable halt markers REFUSES when the REORG_HALT count itself is unreadable",
      "BootstrapHealthGate durable halt markers REFUSES when the marker query itself fails (fail closed, never assume clean)",
      "BootstrapHealthGate durable halt markers REFUSES when the marker-table probe returns non-numeric output",
      "BootstrapHealthGate durable halt markers REFUSES when the marker-table probe returns nothing (unreadable, not clean)",
      "BootstrapHealthGate durable halt markers REFUSES when the marker-table probe returns the wrong number of tokens",
      "BootstrapHealthGate durable halt markers REFUSES when the sync_halt count itself is unreadable",
      "BootstrapHealthGate durable halt markers does not run marker queries for the utxo-tracker (LevelDB, no such table)",
      "BootstrapHealthGate durable halt markers dump-window watermark REFUSES a halt raised and cleared while the dump was streaming",
      "BootstrapHealthGate durable halt markers dump-window watermark REFUSES a halt raised inside the window in the paired decoder database",
      "BootstrapHealthGate durable halt markers dump-window watermark REFUSES a sync_halt raised inside the window even though it is cleared now",
      "BootstrapHealthGate durable halt markers dump-window watermark REFUSES an unusable watermark rather than trusting it",
      "BootstrapHealthGate durable halt markers dump-window watermark REFUSES when the id sequence went backwards",
      "BootstrapHealthGate durable halt markers dump-window watermark hands the caller a watermark for both marker tables",
      "BootstrapHealthGate durable halt markers dump-window watermark passes when nothing was raised inside the window",
      "BootstrapHealthGate durable halt markers dump-window watermark reports a null sync_halt watermark when the table does not exist",
      "BootstrapHealthGate durable halt markers gating a decoder queries no second database",
      "BootstrapHealthGate durable halt markers passes a decoder with no marker rows",
      "BootstrapHealthGate durable halt markers passes an indexer when BOTH its own and the decoder database are clean",
      "BootstrapHealthGate durable halt markers skips the sync_halt query on a schema that has no sync_halt table",
      "BootstrapHealthGate evaluateContainerState() accepts a stable running container with no healthcheck",
      "BootstrapHealthGate evaluateContainerState() treats unreadable inspect output as a refusal",
      "BootstrapHealthGate evaluateStatusPayload() REFUSES a decoder that reports not-halted having never completed a probe",
      "BootstrapHealthGate evaluateStatusPayload() REFUSES a lag that is not a number, rather than coercing it to zero",
      "BootstrapHealthGate evaluateStatusPayload() REFUSES a negative lag: the committed tip sits above the node tip (orphaned view)",
      "BootstrapHealthGate evaluateStatusPayload() REFUSES a payload with no lag field at all: position unverifiable is not caught-up",
      "BootstrapHealthGate evaluateStatusPayload() REFUSES an indexer reporting its block counter wedged",
      "BootstrapHealthGate evaluateStatusPayload() does not demand a probe timestamp from an indexer payload",
      "BootstrapHealthGate evaluateStatusPayload() formats a block-fetch desync object instead of printing [object Object]",
      "BootstrapHealthGate evaluateStatusPayload() passes a healthy payload whose lag is zero",
      "BootstrapHealthGate evaluateStatusPayload() passes the healthy future-block wait and an in-grace barrier defer",
      "BootstrapHealthGate evaluateStatusPayload() passes the same decoder once a probe has actually completed",
      "BootstrapHealthGate evaluateStatusPayload() refuses an indexer frozen behind a halted decoder",
      "BootstrapHealthGate evaluateStatusPayload() refuses when the node tip is stale, since the lag is then unknowable",
      "BootstrapHealthGate evaluateStatusPayload() still accepts the two shapes a producer legitimately emits",
      "BootstrapHealthGate evaluateStatusPayload() treats an empty payload as a refusal, not a pass",
      "BootstrapHealthGate service health probe REFUSES a decoder reporting its own latent REORG_HALT marker",
      "BootstrapHealthGate service health probe REFUSES a halted utxo-tracker",
      "BootstrapHealthGate service health probe REFUSES a service materially behind its tip",
      "BootstrapHealthGate service health probe REFUSES a service reporting unhealthy",
      "BootstrapHealthGate service health probe REFUSES when the health probe cannot be run at all",
      "BootstrapHealthGate service health probe REFUSES when the service cannot say how far behind it is",
      "BootstrapHealthGate service health probe falls back to GET /status when the JSON-RPC route is unavailable",
      "BootstrapHealthGate service health probe honours XCHAIN_NODE_BOOTSTRAP_MAX_LAG_BLOCKS",
      "BootstrapHealthGate service health probe reports EVERY reason, not just the first",
      "makeBootstrap() consults the source health gate aborts the create when the gate refuses",
      "makeBootstrap() consults the source health gate gates the utxo-tracker BEFORE stopping its container, so a refusal costs no downtime",
      "makeBootstrap() consults the source health gate still rejects an unsupported module before consulting the gate",
      "parseCountTokens() accepts whole nonnegative integers",
      "parseCountTokens() quotes the offending output in the refusal so the operator can see it",
      "parseCountTokens() refuses a value above max when one is given, and ignores max when it is not",
      "parseCountTokens() refuses empty and whitespace-only output",
      "parseCountTokens() refuses partial tokens, signs, decimals and exponents",
      "parseCountTokens() refuses the wrong token count in either direction"
    ],
    "8d3d114fcc0b254f": [
      "ModuleService assertNoHostPortConflicts() does NOT flag the container being re-created (selfName excluded)",
      "ModuleService assertNoHostPortConflicts() parses IP-scoped publish specs (IP:HOST:CONTAINER)",
      "ModuleService assertNoHostPortConflicts() reports every conflicting port, not just the first",
      "ModuleService assertNoHostPortConflicts() resolves when no host ports are requested",
      "ModuleService assertNoHostPortConflicts() resolves when requested ports are free on the host",
      "ModuleService assertNoHostPortConflicts() throws naming the conflicting container when a host port is taken",
      "ModuleService buildAndUp() calls statusChanged after storing container ID",
      "ModuleService buildAndUp() healthcheck args dependency-derived healthcheck start periods grants the encoder a window at least as long as the utxo-tracker it probes",
      "ModuleService buildAndUp() healthcheck args dependency-derived healthcheck start periods grants the hub and the explorer at least the DB start period their probes SELECT 1 against",
      "ModuleService buildAndUp() healthcheck args does not warn and returns health args when the portKey is present",
      "ModuleService buildAndUp() healthcheck args emits a docker-parsable duration for every start-period it returns",
      "ModuleService buildAndUp() healthcheck args grants sync a start period covering the whole MAX_HUB_WAIT_MS hub wait",
      "ModuleService buildAndUp() healthcheck args honors XCHAIN_NODE_HEALTH_START_PERIOD_<SERVICE> override",
      "ModuleService buildAndUp() healthcheck args ignores a malformed start-period override and keeps the descriptor default",
      "ModuleService buildAndUp() healthcheck args includes --health-cmd for decoder (http_get probe on /live, not /status)",
      "ModuleService buildAndUp() healthcheck args includes --health-cmd for encoder (http_get probe)",
      "ModuleService buildAndUp() healthcheck args includes --health-cmd for explorer (jsonrpc_ping probe)",
      "ModuleService buildAndUp() healthcheck args includes --health-cmd for indexer (http_get probe)",
      "ModuleService buildAndUp() healthcheck args includes --health-interval, --health-timeout, --health-retries, --health-start-period for encoder",
      "ModuleService buildAndUp() healthcheck args normalizes a bare-seconds start-period override to a docker duration",
      "ModuleService buildAndUp() healthcheck args omits healthcheck args for onlyExecution containers",
      "ModuleService buildAndUp() healthcheck args probes /health (not /status) for sync (http_get probe)",
      "ModuleService buildAndUp() healthcheck args probes health (not ping) for hub (jsonrpc_health probe)",
      "ModuleService buildAndUp() healthcheck args probes health (not ping) for regtest-miner (jsonrpc_health probe)",
      "ModuleService buildAndUp() healthcheck args returns [] with no warning for a module that has no healthcheck descriptor",
      "ModuleService buildAndUp() healthcheck args warns and returns [] when the descriptor portKey is missing from the env",
      "ModuleService buildAndUp() hub credential ordering does not touch the hub account for a one-shot execution container",
      "ModuleService buildAndUp() hub credential ordering does not touch the hub account for any other module",
      "ModuleService buildAndUp() hub credential ordering rotates the grant even when the config push then fails",
      "ModuleService buildAndUp() hub credential ordering rotates the hub grant BEFORE the config push that needs the hub to answer",
      "ModuleService buildAndUp() includes --network flag in docker run",
      "ModuleService buildAndUp() includes port mapping for encoder",
      "ModuleService buildAndUp() includes ulimit for utxo-tracker",
      "ModuleService buildAndUp() includes volume mount for decoder",
      "ModuleService buildAndUp() keeps the legacy tracker volume name under the default NODE_PREFIX",
      "ModuleService buildAndUp() kills and removes old container when overwriteContainerId is provided",
      "ModuleService buildAndUp() passes environment variables via the child env (bare --env NAME), not as argv values",
      "ModuleService buildAndUp() prefixes the tracker volume name under a non-default NODE_PREFIX (F11)",
      "ModuleService buildAndUp() reuseImage creates the container without running docker build",
      "ModuleService buildAndUp() reuseImage does not re-clone the bundled libraries",
      "ModuleService buildAndUp() reuseImage leaves the default path building the image",
      "ModuleService buildAndUp() reuseImage still tears the old container down and registers the new id",
      "ModuleService buildAndUp() reuseImage verifies the tag exists first, and says so when it does not",
      "ModuleService buildAndUp() runs docker build with correct image name and cwd",
      "ModuleService buildAndUp() stores container ID in LevelDB on success",
      "ModuleService buildAndUp() throws when module does not exist",
      "ModuleService buildAndUp(): hub consensus-env drift guard wiring calls assertNoHubConsensusEnvDrift with the generated hub env",
      "ModuleService buildAndUp(): hub consensus-env drift guard wiring does not call the hub guard for an unrelated module",
      "ModuleService buildAndUp(): hub consensus-env drift guard wiring does not reach the real guard from a default harness load (venue independence)",
      "ModuleService buildAndUp(): hub consensus-env drift guard wiring propagates a refusal from the guard instead of building the hub",
      "ModuleService buildAndUp(): log retention and observability env adds no observability name when the deploy host sets none",
      "ModuleService buildAndUp(): log retention and observability env caps json-file logs at 50m x 4 so 48 h of history survives",
      "ModuleService buildAndUp(): log retention and observability env forwards a host-set LOG_LEVEL as a bare --env name, value out of argv",
      "ModuleService buildAndUp(): module-specific port/volume branches includes --restart unless-stopped for non-execution containers",
      "ModuleService buildAndUp(): module-specific port/volume branches includes port mapping for xchain-hub",
      "ModuleService buildAndUp(): module-specific port/volume branches includes port mapping for xchain-indexer and invokes cpSync filter for bundled libs",
      "ModuleService buildAndUp(): module-specific port/volume branches includes port mapping for xchain-regtest-miner",
      "ModuleService buildAndUp(): module-specific port/volume branches includes port mapping for xchain-sync",
      "ModuleService buildAndUp(): module-specific port/volume branches includes two port mappings for xchain-explorer",
      "ModuleService buildAndUp(): module-specific port/volume branches mounts the capability config DIRECTORY when HUB_CAPABILITY_CONFIG is set",
      "ModuleService buildAndUp(): module-specific port/volume branches omits --restart for onlyExecution containers",
      "ModuleService buildAndUp(): module-specific port/volume branches refuses to build on a host without buildx and never invokes docker build",
      "ModuleService buildAndUp(): module-specific port/volume branches rejects when container ID returned is not a valid 64-char hex",
      "ModuleService buildAndUp(): module-specific port/volume branches rejects when docker build fails",
      "ModuleService buildAndUp(): module-specific port/volume branches rejects when docker run fails",
      "ModuleService buildAndUp(): module-specific port/volume branches rejects when insertModuleContainer returns false",
      "ModuleService buildAndUp(): module-specific port/volume branches rejects when port value is invalid (non-numeric)",
      "ModuleService buildAndUp(): module-specific port/volume branches runs docker build with DOCKER_BUILDKIT=1 even when the host exports DOCKER_BUILDKIT=0",
      "ModuleService buildAndUp(): module-specific port/volume branches runs the host-port preflight BEFORE docker build so a conflict fails fast",
      "ModuleService buildAndUp(): module-specific port/volume branches skips db insert for onlyExecution=true and resolves containerId directly",
      "ModuleService cloneGit() branch handling clones with -b flag when a valid branch is specified",
      "ModuleService cloneGit() branch handling rejects (no silent fallback) when the requested branch is not found",
      "ModuleService cloneGit() branch handling rejects on invalid branch name",
      "ModuleService cloneGit() clones to module directory with correct git URL",
      "ModuleService cloneGit() rejects on git clone error",
      "ModuleService cloneGit() rejects when directory exists and rewrite=false",
      "ModuleService cloneGit() rejects when module has no URL mapping",
      "ModuleService cloneGit() removes existing directory when rewrite=true",
      "ModuleService cloneGit() rewrite over an existing checkout clones into a staging dir, never over the live checkout",
      "ModuleService cloneGit() rewrite over an existing checkout leaves the checkout in place when the branch is missing from the remote",
      "ModuleService cloneGit() rewrite over an existing checkout leaves the checkout in place when the clone fails for any other reason",
      "ModuleService cloneGit() rewrite over an existing checkout points a missing branch at the real mechanism (remote clone / local-path override)",
      "ModuleService cloneGit() rewrite over an existing checkout rejects an invalid branch name before touching the filesystem",
      "ModuleService cloneGit() rewrite over an existing checkout rejects an unknown module before touching the filesystem",
      "ModuleService cloneGit() rewrite over an existing checkout restores the original checkout when the swap-in rename fails",
      "ModuleService cloneGit() uses tmp directory when useTmp=true",
      "ModuleService containerExistsByName() via installModule rebuilds singleton when remoteUpdate=true even if container exists",
      "ModuleService containerExistsByName(): via singleton installModule with inspect rejection treats docker inspect rejection as container-not-present (proceeds with install)",
      "ModuleService getModuleBranch() returns trimmed branch name from git rev-parse",
      "ModuleService installModule() singleton guard skips re-creating a singleton module whose container already exists",
      "ModuleService installModule(): DB module calls buildDatabaseModule and statusChanged for DB module",
      "ModuleService installModule(): DB module throws when buildDatabaseModule fails",
      "ModuleService installModule(): bootstrap paths via @global proxyquire calls ensureBootstrapMariaDb when decoder DB was fresh",
      "ModuleService installModule(): bootstrap paths via @global proxyquire calls ensureBootstrapUtxoTracker when utxo-tracker volume was fresh",
      "ModuleService installModule(): bootstrap paths via @global proxyquire does NOT call ensureBootstrapMariaDb when the decoder DB freshness is unknown",
      "ModuleService installModule(): bootstrap paths via @global proxyquire refuses a drifting decoder update before tearing the container down",
      "ModuleService installModule(): branch switch path reclones when existing branch differs from requested branch",
      "ModuleService installModule(): explorer error path throws when installExplorerModule fails",
      "ModuleService installModule(): explorer module calls installExplorerModule and statusChanged",
      "ModuleService installModule(): generic module full build clones, builds, and returns containerId for a fresh generic module",
      "ModuleService installModule(): generic module full build throws when cloneGit fails during installModule",
      "ModuleService installModule(): generic module, container version already known returns false when container version is already set and remoteUpdate=false",
      "ModuleService installModule(): node module builds crypto node when no container version and localNodeVersion is null",
      "ModuleService installModule(): node module returns false when node container_version already set and remoteUpdate=false",
      "ModuleService installModule(): node module skips getCryptoNode when localNodeVersion already set and remoteUpdate=false",
      "ModuleService installModule(): node: checkRemoteNodeVersion called when coin not in remoteVersions calls checkRemoteNodeVersion when coin not in remote versions map",
      "ModuleService installModule(): node: checkRemoteNodeVersion called when coin not in remoteVersions throws when getCryptoNode fails",
      "ModuleService resolveObservabilityEnv() carries a deploy-host value into the container env",
      "ModuleService resolveObservabilityEnv() fabricates nothing when neither source sets a name",
      "ModuleService resolveObservabilityEnv() leaves a per-install config-store value alone",
      "ModuleService resolveObservabilityEnv() names exactly the four shim controls",
      "ModuleService resolveObservabilityEnv() never carries a name that is not one of the four",
      "ModuleService resolveObservabilityEnv() treats an empty host value as unset",
      "ModuleService uninstallModule() removes stale tracking row when no container in status but row exists in DB",
      "ModuleService uninstallModule() returns true when module is not found (already uninstalled)",
      "ModuleService uninstallModule() skips kill for exited containers",
      "ModuleService uninstallModule() stops a running container with its budget, never docker kill, before removing",
      "ModuleService uninstallModule() throws when container kill/remove fails (catch rethrows)",
      "ModuleService uninstallModule() throws when trying to uninstall database",
      "ModuleService uninstallModule(): removeModuleContainer returns false throws when removeModuleContainer returns false after successful container removal",
      "ModuleService unit-suite venue independence leaves proxyquire in noCallThru mode after a call-through load",
      "ModuleService unit-suite venue independence trips the guard instead of probing the host when a call-through load forgets the stub"
    ],
    "9778dd0f1c360aef": [
      "VersionService checkAllRemoteVersions() is a function that returns a promise",
      "VersionService checkRemoteNodeVersion() calls gitHubDownloader for bitcoin with correct owner/repo",
      "VersionService checkRemoteNodeVersion() calls gitHubDownloader for dogecoin with correct owner/repo",
      "VersionService checkRemoteNodeVersion() calls gitHubDownloader for litecoin with correct owner/repo",
      "VersionService checkRemoteNodeVersion() stores result in remoteModuleVersions with node-<coin> key",
      "VersionService getContainerModuleVersion() prefers the exec read over the host copy",
      "VersionService getContainerModuleVersion() reads package.json from container and returns version",
      "VersionService getContainerModuleVersion() reads the version with docker exec even when docker cp is permanently broken",
      "VersionService getContainerModuleVersion() throws on docker cp error",
      "VersionService getContainerNodeVersion() reads version file from container via docker cp",
      "VersionService getContainerNodeVersion() throws on docker cp error",
      "VersionService getGithubProjectVersion() calls GitHub API with correct URL",
      "VersionService getGithubProjectVersion() preserves tag_name without v prefix",
      "VersionService getGithubProjectVersion() returns the release id",
      "VersionService getGithubProjectVersion() strips leading v from tag_name",
      "VersionService getLocalModuleVersion() reads version from module package.json",
      "VersionService getLocalModuleVersion() rejects when package.json does not exist",
      "VersionService getLocalModuleVersion() rejects when package.json has no version field",
      "VersionService getLocalModuleVersion() rejects when package.json is malformed",
      "VersionService getLocalNodeVersion() reads version from correct file path",
      "VersionService getLocalNodeVersion() rejects on read error",
      "VersionService getLocalNodeVersion() rejects when version file does not exist",
      "VersionService readContainerFile() falls back to docker cp when the container cannot be exec'd",
      "VersionService readContainerFile() names BOTH failures when neither read works"
    ],
    "99bd35e26fa0632d": [
      "MemoryLimitService countInstalledTrackers() counts one when the registry cannot be read or the handle has no registry",
      "MemoryLimitService countInstalledTrackers() counts the registered trackers plus the one being created",
      "MemoryLimitService countInstalledTrackers() does not double-count a tracker that is already registered (recreate)",
      "MemoryLimitService memoryArgsFor() 0 disables the derived tracker cap, and says so",
      "MemoryLimitService memoryArgsFor() an explicit XCHAIN_NODE_MODULE_MEMORY_MB_<SERVICE> wins for any module",
      "MemoryLimitService memoryArgsFor() caps the tracker with --memory and an equal --memory-swap",
      "MemoryLimitService memoryArgsFor() derives the env key from the service name",
      "MemoryLimitService memoryArgsFor() ignores an unparseable value rather than guessing, and names it",
      "MemoryLimitService memoryArgsFor() leaves every other module uncapped by default",
      "MemoryLimitService memoryArgsFor() warns in the note when the share is under the floor",
      "MemoryLimitService trackerMemoryLimitMb() ceilings a huge host",
      "MemoryLimitService trackerMemoryLimitMb() divides the half among the trackers on the host (the 16 GB three-chain case)",
      "MemoryLimitService trackerMemoryLimitMb() floors, and says so, when the host cannot afford its trackers",
      "MemoryLimitService trackerMemoryLimitMb() gives one tracker half the host",
      "MemoryLimitService trackerMemoryLimitMb() treats a missing or zero count as one"
    ],
    "9b210872d17a3ffa": [
      "identify-deployed-ref (deploy-informing script) argument parsing accepts several labelled hashes in one walk",
      "identify-deployed-ref (deploy-informing script) argument parsing honours --depth anywhere in the list and falls back on a junk value",
      "identify-deployed-ref (deploy-informing script) argument parsing keeps an explicit label and the default depth",
      "identify-deployed-ref (deploy-informing script) argument parsing labels a hash with its short form when no label is given",
      "identify-deployed-ref (deploy-informing script) argument parsing rejects a hash with uppercase or non-hex characters",
      "identify-deployed-ref (deploy-informing script) argument parsing rejects a truncated hash instead of turning it into a NO MATCH finding",
      "identify-deployed-ref (deploy-informing script) argument parsing requires a repo path and at least one hash",
      "identify-deployed-ref (deploy-informing script) blob identity reproduces git hash-object exactly, including empty and binary content",
      "identify-deployed-ref (deploy-informing script) blob identity uses the byte length, not the character length, in the blob header",
      "identify-deployed-ref (deploy-informing script) git ls-tree parsing keeps blobs, drops gitlinks, and sorts by path",
      "identify-deployed-ref (deploy-informing script) git ls-tree parsing returns an empty manifest for empty output rather than a line of noise",
      "identify-deployed-ref (deploy-informing script) matching a hash against the history walk leaves a hash unfound when no commit carries it",
      "identify-deployed-ref (deploy-informing script) matching a hash against the history walk reports the NEWEST commit carrying a shared tree",
      "identify-deployed-ref (deploy-informing script) matching a hash against the history walk stops walking once every wanted hash is placed",
      "identify-deployed-ref (deploy-informing script) probe and match agree on a real repository hashes only src/, so a change outside it does not rename the commit",
      "identify-deployed-ref (deploy-informing script) probe and match agree on a real repository reads the version as a hint and survives a missing or broken package.json",
      "identify-deployed-ref (deploy-informing script) probe and match agree on a real repository the file walk and the object store produce the identical manifest",
      "identify-deployed-ref (deploy-informing script) probe and match agree on a real repository yields an empty manifest when src/ is absent instead of throwing",
      "identify-deployed-ref (deploy-informing script) the entrypoint guard does not run the CLI when the module is required",
      "identify-deployed-ref (deploy-informing script) the entrypoint guard emits the bare manifest under --manifest, for diffing against ls-tree",
      "identify-deployed-ref (deploy-informing script) the entrypoint guard exits 2 on a bad hash rather than reporting a clean NO MATCH",
      "identify-deployed-ref (deploy-informing script) the entrypoint guard still probes when invoked directly as a file",
      "identify-deployed-ref (deploy-informing script) the entrypoint guard still probes when piped to `node -`, where require.main is undefined",
      "identify-deployed-ref (deploy-informing script) the report calls an unmatched artifact a FINDING and names the walk depth",
      "identify-deployed-ref (deploy-informing script) the report counts every unmatched hash when a run mixes both outcomes",
      "identify-deployed-ref (deploy-informing script) the report formats the probe report as the four keys a runbook greps for",
      "identify-deployed-ref (deploy-informing script) the report prints the commit, date and src file count for a match",
      "identify-deployed-ref (deploy-informing script) tree hash changes when any single blob changes",
      "identify-deployed-ref (deploy-informing script) tree hash depends on manifest order, so two trees of the same files differ",
      "identify-deployed-ref (deploy-informing script) tree hash is stable for the same manifest"
    ],
    "a04c8cc3113456d6": [
      "deploy source identity freshness of a named branch REFUSES the deploy when the ref keeps resolving to a stale commit",
      "deploy source identity freshness of a named branch accepts a clone that landed on the branch tip, with no second clone",
      "deploy source identity freshness of a named branch does not check a branch tip for a manifest-pinned install",
      "deploy source identity freshness of a named branch leaves the version-probe clone in tmp alone",
      "deploy source identity freshness of a named branch proceeds when the source cannot be queried, rather than failing the deploy",
      "deploy source identity freshness of a named branch re-clones ONCE when the clone landed behind the branch tip",
      "deploy source identity image stamping builds without labels rather than failing when the checkout is unreadable",
      "deploy source identity image stamping does not re-stamp an image it is reusing",
      "deploy source identity image stamping finds the commit when the branch ref is packed rather than loose",
      "deploy source identity image stamping labels a detached checkout with its commit and no ref",
      "deploy source identity image stamping labels the image with the commit it was built from",
      "deploy source identity local-path source behind its own upstream does not probe a remote URL source for a local upstream",
      "deploy source identity local-path source behind its own upstream says nothing when the local checkout is level with its upstream",
      "deploy source identity local-path source behind its own upstream warns, naming both commits and how far behind the checkout is",
      "deploy source identity reporting names the full commit, its date and its subject on every deploy clone",
      "deploy source identity reporting reports after the swap on a rewrite over an existing checkout"
    ],
    "a40634de4e012c02": [
      "state dbRootPassword can be reset to null",
      "state dbRootPassword defaults to null",
      "state dbRootPassword round-trips a password",
      "state installedModules defaults to empty object",
      "state installedModules resetInstalledModules clears to empty object",
      "state installedModules round-trips a nested object",
      "state lastPrintedStatus appendLastPrintedStatus concatenates",
      "state lastPrintedStatus defaults to empty string after reset",
      "state lastPrintedStatus round-trips a string",
      "state lastStatus defaults to null",
      "state lastStatus round-trips an object",
      "state remoteModuleVersions can set and retrieve individual versions",
      "state remoteModuleVersions preserves previous versions when adding new ones",
      "state statusUpdated can be set to true",
      "state statusUpdated defaults to false",
      "state verbose can be set to true",
      "state verbose defaults to false"
    ],
    "a5c2a73d73682ccc": [
      "DatabaseService addUserPasswordToDatabase() creates database and user via docker when db does not exist",
      "DatabaseService addUserPasswordToDatabase() grants DrillB parity permissions to a NON-mainnet indexer",
      "DatabaseService addUserPasswordToDatabase() grants MVH test permissions when module is xchain-hub",
      "DatabaseService addUserPasswordToDatabase() grants SLAVE MONITOR to indexer and decoder accounts, on MAINNET too",
      "DatabaseService addUserPasswordToDatabase() rotates the password via ALTER USER on the docker path when the user does not match",
      "DatabaseService addUserPasswordToDatabase() skips create when database already exists (dbCount != 0)",
      "DatabaseService addUserPasswordToDatabase() throws when docker exec fails",
      "DatabaseService addUserPasswordToDatabase() withholds SLAVE MONITOR from the hub account",
      "DatabaseService addUserPasswordToDatabase() withholds the DrillB grant from a MAINNET indexer",
      "DatabaseService addUserPasswordToDatabase(): EXTERNAL_DB path creates database and user via native mariadb when EXTERNAL_DB=true",
      "DatabaseService addUserPasswordToDatabase(): EXTERNAL_DB path grants MVH permissions for hub module via native mariadb (EXTERNAL_DB)",
      "DatabaseService addUserPasswordToDatabase(): EXTERNAL_DB path grants SLAVE MONITOR to an indexer account via native mariadb (EXTERNAL_DB)",
      "DatabaseService addUserPasswordToDatabase(): EXTERNAL_DB path skips creates when db and user already exist (EXTERNAL_DB)",
      "DatabaseService addUserPasswordToDatabase(): EXTERNAL_DB path throws when native mariadb command fails (EXTERNAL_DB)",
      "DatabaseService addUserPasswordToDatabase(): provisioning-SQL safety escapes a quote-containing password into a single well-formed string literal (docker path)",
      "DatabaseService addUserPasswordToDatabase(): provisioning-SQL safety leaves a plain password byte-for-byte unchanged (no behaviour drift for existing installs)",
      "DatabaseService addUserPasswordToDatabase(): provisioning-SQL safety rejects an injection-bearing database name before any SQL runs",
      "DatabaseService addUserPasswordToDatabase(): provisioning-SQL safety rejects an injection-bearing user name before any SQL runs",
      "DatabaseService askMariadbRootPassword() falls back to password prompt when no container found",
      "DatabaseService askMariadbRootPassword() falls back to the credentials-store copy when the container has no MYSQL_ROOT_PASSWORD env",
      "DatabaseService askMariadbRootPassword() falls through to the container-env read when XCHAIN_NODE_DB_ROOT_PASSWORD does not verify",
      "DatabaseService askMariadbRootPassword() ignores a stale credentials-store copy and reaches the prompt on a TTY",
      "DatabaseService askMariadbRootPassword() persists the container-env password to the credentials store on accept",
      "DatabaseService askMariadbRootPassword() reads from XCHAIN_NODE_DB_ROOT_PASSWORD env var when not cached and no container is running",
      "DatabaseService askMariadbRootPassword() reads from external DB config when EXTERNAL_DB=true and not cached",
      "DatabaseService askMariadbRootPassword() reads root password from running container printenv",
      "DatabaseService askMariadbRootPassword() returns cached password immediately",
      "DatabaseService askMariadbRootPassword() stays silent when the env override authenticates",
      "DatabaseService askMariadbRootPassword() throws an actionable error instead of prompting when stdin is not a TTY",
      "DatabaseService askMariadbRootPassword() verifies XCHAIN_NODE_DB_ROOT_PASSWORD against a running container before trusting it",
      "DatabaseService askMariadbRootPassword() warns, without printing a value, when the env override does not authenticate",
      "DatabaseService buildDatabaseModule() aborts the install (no docker run) when a host-port conflict is detected",
      "DatabaseService buildDatabaseModule() appends MariaDB tuning args to docker run when env vars are set",
      "DatabaseService buildDatabaseModule() defaults max-connections to 1000 when the env var is unset (multi-chain saturation guard)",
      "DatabaseService buildDatabaseModule() does not enroll the DB in any restart path",
      "DatabaseService buildDatabaseModule() fails fast with an actionable error when the existing DB container is not running",
      "DatabaseService buildDatabaseModule() force-removes only after docker positively reports the container gone",
      "DatabaseService buildDatabaseModule() gives the DB container a health probe so a stalled mariadbd is visible",
      "DatabaseService buildDatabaseModule() installs mariadb when no existing database found",
      "DatabaseService buildDatabaseModule() omits MariaDB tuning args when env vars are unset",
      "DatabaseService buildDatabaseModule() passes MYSQL_ROOT_PASSWORD via docker env, never in the argv",
      "DatabaseService buildDatabaseModule() refuses to force-remove the MariaDB container when the probe says 'exists'",
      "DatabaseService buildDatabaseModule() refuses to force-remove the MariaDB container when the probe says 'unknown'",
      "DatabaseService buildDatabaseModule() runs the multi-stack host-port pre-flight before docker run",
      "DatabaseService buildDatabaseModule() skips installation when database already exists and connects to network",
      "DatabaseService buildDatabaseModule() throws instead of returning undefined when docker run output is not a 64-hex id",
      "DatabaseService buildDatabaseModule(): EXTERNAL_DB path pings external MariaDB and returns true when reachable",
      "DatabaseService buildDatabaseModule(): EXTERNAL_DB path throws descriptive error when external MariaDB is unreachable",
      "DatabaseService buildDatabaseModule(): no coin/network appends DB data dir volume mount when XCHAIN_NODE_DB_DATA_DIR is set",
      "DatabaseService buildDatabaseModule(): no coin/network returns true without network join when coin+network are empty",
      "DatabaseService buildDatabaseModule(): no coin/network throws string error when addContainerToNetwork fails on existing container",
      "DatabaseService checkIfDatabaseIsReady() honours a caller-supplied retry budget",
      "DatabaseService checkIfDatabaseIsReady() passes -D database arg when database is specified",
      "DatabaseService checkIfDatabaseIsReady() passes the password via MYSQL_PWD env, never in argv",
      "DatabaseService checkIfDatabaseIsReady() retries up to 10 times then returns false",
      "DatabaseService checkIfDatabaseIsReady() returns true when database responds",
      "DatabaseService checkIfDatabaseIsReady() still spends the full readiness budget when the caller passes none",
      "DatabaseService checkIfDatabaseModuleExists() returns container ID when database container exists and is valid",
      "DatabaseService checkIfDatabaseModuleExists() returns null when container does not have State.Status",
      "DatabaseService checkIfDatabaseModuleExists() returns null when docker inspect fails",
      "DatabaseService checkIfDatabaseModuleExists() returns null when getStatusFromContainer throws",
      "DatabaseService clearHubPriceIngestWatermark() a hub database shared with another network clears the legacy unset bucket alongside the network row",
      "DatabaseService clearHubPriceIngestWatermark() a hub database shared with another network folds the reset network case- and whitespace-insensitively, matching the hub",
      "DatabaseService clearHubPriceIngestWatermark() a hub database shared with another network issues a different WHERE clause per network for the same chain",
      "DatabaseService clearHubPriceIngestWatermark() a hub database shared with another network no longer refuses on a hub configured for another network",
      "DatabaseService clearHubPriceIngestWatermark() a hub database shared with another network scopes the delete to the network being reset, leaving every other network's fence",
      "DatabaseService clearHubPriceIngestWatermark() deletes only the reset chain's fence row from the hub DB",
      "DatabaseService clearHubPriceIngestWatermark() goes through the native driver in EXTERNAL_DB mode",
      "DatabaseService clearHubPriceIngestWatermark() prints the manual statement and returns false when this MariaDB has no hub table",
      "DatabaseService clearHubPriceIngestWatermark() refuses a hub DB name that is not a safe identifier",
      "DatabaseService clearHubPriceIngestWatermark() throws on an unknown coin rather than deleting nothing and reporting success",
      "DatabaseService clearHubPriceIngestWatermark() warns instead of throwing when the hub config carries no HUB_DB_NAME",
      "DatabaseService ensureDatabasePool() creates database pool when not ready (docker path)",
      "DatabaseService ensureDatabasePool() is a no-op when db is already ready",
      "DatabaseService ensureXchainNodeAccess() reprovisions when stored credentials no longer work",
      "DatabaseService ensureXchainNodeAccess() returns existing working credentials without reprovisioning",
      "DatabaseService ensureXchainNodeAccess() throws \"MariaDB is not responding\" when root checkIfDatabaseIsReady returns false",
      "DatabaseService ensureXchainNodeAccess() throws when container not found and no credentials",
      "DatabaseService ensureXchainNodeAccess(): EXTERNAL_DB path provisions fresh creds when no existing credentials stored",
      "DatabaseService ensureXchainNodeAccess(): EXTERNAL_DB path reprovisions via native commands when existing creds fail on external MariaDB",
      "DatabaseService ensureXchainNodeAccess(): EXTERNAL_DB path returns existing creds when they work against external MariaDB",
      "DatabaseService executeDockerMariaDbCommand() appends commandOptions when provided",
      "DatabaseService executeDockerMariaDbCommand() pipes SQL via stdin (never argv) with the password in env, not argv",
      "DatabaseService executeDockerMariaDbCommand() rejects on non-zero exit",
      "DatabaseService executeDockerMariaDbCommand() scrubs the SQL from a failed command (avoids leaking an embedded password)",
      "DatabaseService executeNativeMariaDbCommand() asks the driver for array rows when a clustered flag grants batch mode",
      "DatabaseService executeNativeMariaDbCommand() closes connection even when query throws",
      "DatabaseService executeNativeMariaDbCommand() recognizes --batch flag",
      "DatabaseService executeNativeMariaDbCommand() recognizes the clustered short flag -BN as batch mode",
      "DatabaseService executeNativeMariaDbCommand() recognizes the clustered short flag in either order (-NB)",
      "DatabaseService executeNativeMariaDbCommand() returns empty string for DDL (non-array result)",
      "DatabaseService executeNativeMariaDbCommand() returns empty string for SELECT without batch mode",
      "DatabaseService executeNativeMariaDbCommand() returns empty string for empty batch result",
      "DatabaseService executeNativeMariaDbCommand() returns tab-delimited rows in batch mode (-B -N)",
      "DatabaseService executeNativeMariaDbCommand() returns tab-joined columns in batch mode for multi-column rows",
      "DatabaseService executeNativeMariaDbCommand() scrubs the SQL from a driver query error (avoids leaking an embedded password)",
      "DatabaseService executeNativeMariaDbCommand() still returns empty for an option string carrying no batch flag",
      "DatabaseService getDatabaseContainerId() returns container ID when inspect returns valid 64-char hex",
      "DatabaseService getDatabaseContainerId() returns null when inspect command throws",
      "DatabaseService getDatabaseContainerId() returns null when inspect returns non-hex output",
      "DatabaseService getDatabaseHostPort() parses port from docker port output",
      "DatabaseService getDatabaseHostPort() returns default port when docker port fails",
      "DatabaseService getDatabaseHostPort() returns default port when output has no port match",
      "DatabaseService getExternalDbConfig() fails fast instead of prompting when stdin is not a TTY",
      "DatabaseService getExternalDbConfig() fails fast on a partial env set rather than prompting headlessly",
      "DatabaseService getExternalDbConfig() re-prompts interactively when saved config ping fails",
      "DatabaseService getExternalDbConfig() returns config from env vars when all four are set",
      "DatabaseService getExternalDbConfig() returns saved config from credentials when ping succeeds",
      "DatabaseService getExternalDbConfig() throws on a malformed external-DB port from env instead of returning NaN",
      "DatabaseService getExternalDbConfig() throws on an out-of-range external-DB port from env",
      "DatabaseService purgeHubCrossChainRows() goes through the native driver in EXTERNAL_DB mode",
      "DatabaseService purgeHubCrossChainRows() keeps the snapshots when the co-located hub is configured for another network",
      "DatabaseService purgeHubCrossChainRows() keeps the snapshots when the hub database holds another network's rows",
      "DatabaseService purgeHubCrossChainRows() prints the manual statements and returns false when this MariaDB has no hub tables",
      "DatabaseService purgeHubCrossChainRows() purges all three tables for a Bitcoin regtest re-genesis",
      "DatabaseService purgeHubCrossChainRows() purges only the leg-scoped rows for a non-Bitcoin regtest re-genesis",
      "DatabaseService purgeHubCrossChainRows() refuses a hub DB name that is not a safe identifier",
      "DatabaseService purgeHubCrossChainRows() throws on an unknown coin rather than purging nothing and reporting success",
      "DatabaseService purgeHubCrossChainRows() touches nothing at all off regtest",
      "DatabaseService resetDatabases() drops and recreates each database module",
      "DatabaseService resetDatabases() drops the CONFIGURED database names, not the derived defaults",
      "DatabaseService resetDatabases() falls back to the derived name when config carries no database name",
      "DatabaseService resetDatabases() refuses the docker-mode reset when a derived database name is not a safe identifier",
      "DatabaseService resetDatabases() refuses the external-DB reset on the second module before the first is dropped",
      "DatabaseService resetDatabases() refuses the reset when a CONFIGURED database name is not a safe identifier",
      "DatabaseService resetDatabases() refuses to drop anything when no MariaDB container exists",
      "DatabaseService setDatabaseParameters() checks for credential drift with the config values, before any account write",
      "DatabaseService setDatabaseParameters() does not blame the docker network for a drift refusal",
      "DatabaseService setDatabaseParameters() iterates installed coins+networks and adds DB users",
      "DatabaseService setDatabaseParameters() refuses to run against an unconfigured module registry",
      "DatabaseService setDatabaseParameters() throws and logs when coin network processing fails",
      "DatabaseService setDatabaseParameters() throws instead of reporting success when no coin/network is installed",
      "DatabaseService setDatabaseParameters() throws when the registry lists a coin but holds no decoder/indexer container",
      "DatabaseService setDatabaseParameters() writes no account when the drift guard refuses",
      "DatabaseService: clearHubPriceIngestWatermark() against a pre-network-column hub does not fall back for an unknown column that is not `network`",
      "DatabaseService: clearHubPriceIngestWatermark() against a pre-network-column hub does not fall back on an ordinary SQL failure",
      "DatabaseService: clearHubPriceIngestWatermark() against a pre-network-column hub falls back to the chain-only delete and reports success",
      "DatabaseService: clearHubPriceIngestWatermark() against a pre-network-column hub recognises the driver-shaped ER_BAD_FIELD_ERROR in EXTERNAL_DB mode",
      "DatabaseService: clearHubPriceIngestWatermark() against a pre-network-column hub warns once, naming the column and the hub version that carries it"
    ],
    "a67181a3796fad54": [
      "StopBudgetService moduleStopTimeoutSeconds() answers the coin node from its own resolver so `stop node` and `stop all` share one path",
      "StopBudgetService moduleStopTimeoutSeconds() gives the decoder and tracker more than docker's ten seconds, and the rest a default above it",
      "StopBudgetService moduleStopTimeoutSeconds() ignores a value that is not a whole number of seconds, and says so",
      "StopBudgetService moduleStopTimeoutSeconds() reads the per-service override by the derived variable name",
      "StopBudgetService stopModuleContainer() says nothing when there was nothing to stop",
      "StopBudgetService stopModuleContainer() stops with the budget and reports a clean stop with the time it took",
      "StopBudgetService stopModuleContainer() warns when the service ran out of budget and was killed, naming the override",
      "StopBudgetService stopTimeoutArgs() stamps the same budget on the container so a plain docker stop honours it"
    ],
    "a67cabacd0112216": [
      "GoLiveGate armed (XCHAIN_NODE_GO_LIVE=1): refuses honors the explicit skip escape hatch, loudly",
      "GoLiveGate armed (XCHAIN_NODE_GO_LIVE=1): refuses passes a fully armed deploy",
      "GoLiveGate armed (XCHAIN_NODE_GO_LIVE=1): refuses refuses a mainnet indexer with missing keys, itemizing violations",
      "GoLiveGate armed (XCHAIN_NODE_GO_LIVE=1): refuses refuses an open mainnet encoder (no API_KEY)",
      "GoLiveGate armed (XCHAIN_NODE_GO_LIVE=1): refuses refuses the shared hub (no coin/network) with no HUB_API_KEY",
      "GoLiveGate armed (XCHAIN_NODE_GO_LIVE=1): refuses refuses when flag-day placeholder timestamps remain in src",
      "GoLiveGate armed (XCHAIN_NODE_GO_LIVE=1): refuses refuses when signed-bootstrap enforcement is opted out",
      "GoLiveGate collectViolations key resolution accepts keys from the generated container env over host env",
      "GoLiveGate collectViolations key resolution falls back to host env for passthrough-only keys",
      "GoLiveGate findFlagDayPlaceholders finds an un-armed mainnet activation anywhere under src/ but skips node_modules",
      "GoLiveGate findFlagDayPlaceholders hasUnarmedMainnetActivation flags: MAINNET constant",
      "GoLiveGate findFlagDayPlaceholders hasUnarmedMainnetActivation flags: addChange identifier resolved",
      "GoLiveGate findFlagDayPlaceholders hasUnarmedMainnetActivation flags: addChange literal mainnet block",
      "GoLiveGate findFlagDayPlaceholders hasUnarmedMainnetActivation flags: addChange literal mainnet time",
      "GoLiveGate findFlagDayPlaceholders hasUnarmedMainnetActivation flags: map coin-scoped mainnet key",
      "GoLiveGate findFlagDayPlaceholders hasUnarmedMainnetActivation flags: map mainnet key",
      "GoLiveGate findFlagDayPlaceholders hasUnarmedMainnetActivation passes: 1798761600 is an ARMED instant",
      "GoLiveGate findFlagDayPlaceholders hasUnarmedMainnetActivation passes: an armed real instant",
      "GoLiveGate findFlagDayPlaceholders hasUnarmedMainnetActivation passes: an inert null",
      "GoLiveGate findFlagDayPlaceholders hasUnarmedMainnetActivation passes: sentinel as a query upper bound",
      "GoLiveGate findFlagDayPlaceholders hasUnarmedMainnetActivation passes: sentinel in a block comment",
      "GoLiveGate findFlagDayPlaceholders hasUnarmedMainnetActivation passes: sentinel in a line comment",
      "GoLiveGate findFlagDayPlaceholders hasUnarmedMainnetActivation passes: sentinel in a string",
      "GoLiveGate findFlagDayPlaceholders hasUnarmedMainnetActivation passes: sentinel in a testnet map entry",
      "GoLiveGate findFlagDayPlaceholders hasUnarmedMainnetActivation passes: sentinel in the testnet position",
      "GoLiveGate findFlagDayPlaceholders returns empty for a module with no src dir",
      "GoLiveGate pre-launch (XCHAIN_NODE_GO_LIVE unset): warn-only is silent when everything is armed",
      "GoLiveGate pre-launch (XCHAIN_NODE_GO_LIVE unset): warn-only warns but does not throw on a mainnet indexer with missing keys",
      "GoLiveGate scope ignores read surfaces (decoder) on mainnet even when armed",
      "GoLiveGate scope ignores write surfaces on regtest even when armed"
    ],
    "a88dc187e71ed005": [
      "constants: the e2e image stages every sibling its suites require bundles sync, so the consensus hash drift-lock can run instead of skipping",
      "constants: the e2e image stages every sibling its suites require keeps the siblings the other suites resolve directly",
      "moduleOperations clearDecoderReorgHalt() ignores non-decoder modules in the list",
      "moduleOperations clearDecoderReorgHalt() passes --force and --dry-run through",
      "moduleOperations clearDecoderReorgHalt() refuses a trivial reason without touching any container",
      "moduleOperations clearDecoderReorgHalt() reports false when no decoder container is installed for the target",
      "moduleOperations clearDecoderReorgHalt() reports false when the script refuses (non-zero exit) and prints its text",
      "moduleOperations clearDecoderReorgHalt() runs the decoder's own clear script inside the decoder container with the reason",
      "moduleOperations execModules() passes command to execContainer",
      "moduleOperations execModules(): error path continues after execContainer error",
      "moduleOperations execModules(): error path skips when container ID is null",
      "moduleOperations installModules() builds database before installing modules",
      "moduleOperations installModules() calls installModule for each module in the list",
      "moduleOperations installModules() creates Docker network for each coin/network pair",
      "moduleOperations installModules() handles multiple coins and networks",
      "moduleOperations installModules() reports a module installModule declined as skipped, not installed",
      "moduleOperations installModules() reports the bootstrap outcomes even when the install throws",
      "moduleOperations installModules() reports what it installed on success",
      "moduleOperations installModules() skips network creation and database for shared services (empty coin/network)",
      "moduleOperations installModules() still reports the bootstrap outcomes on a clean install",
      "moduleOperations logModules() calls logContainer with follow=true by default",
      "moduleOperations logModules() follow: only attaches to the first service (single-TTY limit)",
      "moduleOperations logModules() non-follow: dumps every selected service, not just the first",
      "moduleOperations logModules() passes follow=false when specified",
      "moduleOperations logModules(): no containers prints \"No service was selected\" when no containers found",
      "moduleOperations monitorModules() collects container IDs and passes to startDockerMonitor",
      "moduleOperations recreateModules() does not probe docker when the registry has a container id",
      "moduleOperations recreateModules() does not rotate the hub account when the hub was not recreated",
      "moduleOperations recreateModules() finishes the sweep past a failing venue and names every failure",
      "moduleOperations recreateModules() never re-clones or rebuilds through installModule",
      "moduleOperations recreateModules() propagates a failure instead of reporting success",
      "moduleOperations recreateModules() provisions the DB accounts only after every container is back on config values",
      "moduleOperations recreateModules() provisions the venues that came back before failing the run",
      "moduleOperations recreateModules() recreates a container the registry has lost rather than skipping it",
      "moduleOperations recreateModules() recreates from the current config while reusing the existing image",
      "moduleOperations recreateModules() refuses the modules whose containers are not built from the config map",
      "moduleOperations recreateModules() refuses to recreate a module docker positively reports gone",
      "moduleOperations recreateModules() rotates the shared hub DB account after recreating the hub",
      "moduleOperations recreateModules() skips DB provisioning for a service that owns no DB account",
      "moduleOperations recreateModules() still recreates the supported modules when the request also names an unsupported one",
      "moduleOperations recreateModules() still recreates when the presence probe is inconclusive",
      "moduleOperations resetModules() EXTERNAL_DB pre-wipe reachability guard aborts before anything is stopped or wiped when the external DB is unreachable",
      "moduleOperations resetModules() EXTERNAL_DB pre-wipe reachability guard aborts the same way when the external config cannot be resolved",
      "moduleOperations resetModules() EXTERNAL_DB pre-wipe reachability guard does not probe the external DB when no database is being reset",
      "moduleOperations resetModules() EXTERNAL_DB pre-wipe reachability guard proceeds to the reset when the external DB answers",
      "moduleOperations resetModules() aborts a missing-MariaDB reset without stopping anything",
      "moduleOperations resetModules() aborts before any wipe when a target fails to stop, and restarts what it stopped",
      "moduleOperations resetModules() aborts when stopContainer rejects a non-Error value",
      "moduleOperations resetModules() clears the hub price ingest fence when the indexer DB is reset",
      "moduleOperations resetModules() does not continue past a failed stopContainer",
      "moduleOperations resetModules() leaves the fence alone on a decoder-only reset (that chain keeps pushing prices)",
      "moduleOperations resetModules() marks the reindexed combos for a forced bootstrap republish does not abort the reset when the ledger cannot be written",
      "moduleOperations resetModules() marks the reindexed combos for a forced bootstrap republish marks all three on a reset all",
      "moduleOperations resetModules() marks the reindexed combos for a forced bootstrap republish marks nothing for a node-only reset",
      "moduleOperations resetModules() marks the reindexed combos for a forced bootstrap republish marks nothing when the reset aborts before any wipe",
      "moduleOperations resetModules() marks the reindexed combos for a forced bootstrap republish marks the tracker combo when the tracker volume is wiped",
      "moduleOperations resetModules() node datadir resolution falls back to the configured datadir when the container reports no mount",
      "moduleOperations resetModules() node datadir resolution names the container, the configured path and the env var in the refusal",
      "moduleOperations resetModules() node datadir resolution refuses the whole reset when the datadir resolves to nothing",
      "moduleOperations resetModules() node datadir resolution skips the node wipe out loud, and completes, when no node is installed",
      "moduleOperations resetModules() node datadir resolution wipes the path the node container reports, not the env-derived one",
      "moduleOperations resetModules() only resets node data when service=node",
      "moduleOperations resetModules() reports a fence-clear failure without aborting the restart pass",
      "moduleOperations resetModules() resets all services when service=all",
      "moduleOperations resetModules() resets decoder: stops, resets DB, and bounces",
      "moduleOperations resetModules() resets node data when nodeDataPath exists",
      "moduleOperations resetModules() scopes the hand-run fence statement to the reset network, plus the legacy bucket",
      "moduleOperations resetModules() still treats a \"no such container\" stop as a skip and completes the reset",
      "moduleOperations resetModules() stops and resets utxo-tracker when service=xchain-utxo-tracker",
      "moduleOperations resetModules() the coupled decoder/indexer pair aborts the decoder-only reset when the indexer registry row cannot be read",
      "moduleOperations resetModules() the coupled decoder/indexer pair allows a decoder-only reset when no indexer is installed to strand",
      "moduleOperations resetModules() the coupled decoder/indexer pair leaves `reset all` alone, which already moves both",
      "moduleOperations resetModules() the coupled decoder/indexer pair names the joint form in the refusal, so the remedy is runnable",
      "moduleOperations resetModules() the coupled decoder/indexer pair refuses a decoder-only reset while an indexer is installed",
      "moduleOperations resetModules() the coupled decoder/indexer pair resets both halves together when the joint form is used",
      "moduleOperations resetModules() the coupled decoder/indexer pair still allows an indexer-only reset",
      "moduleOperations resetModules() the registry read that decides what to stop aborts before any wipe when a target row cannot be read",
      "moduleOperations resetModules() the registry read that decides what to stop reports a module the rollback cannot resolve as still down",
      "moduleOperations resetModules() the registry read that decides what to stop still skips a module that is genuinely absent from the registry",
      "moduleOperations resetModules() the regtest re-genesis hub purge does not abort the restart pass when the purge throws",
      "moduleOperations resetModules() the regtest re-genesis hub purge leaves the hub rows alone when the chain itself is not reset",
      "moduleOperations resetModules() the regtest re-genesis hub purge names the hub rows in the confirmation for a regtest node reset",
      "moduleOperations resetModules() the regtest re-genesis hub purge never purges off regtest, where these rows are live federation history",
      "moduleOperations resetModules() the regtest re-genesis hub purge purges after the price fence on reset all",
      "moduleOperations resetModules() the regtest re-genesis hub purge purges the hub cross-chain rows on a regtest node reset",
      "moduleOperations resetModules() the utxo-tracker volume wipe aborts and restores the stack when the wipe fails with nothing else touched",
      "moduleOperations resetModules() the utxo-tracker volume wipe refuses the reset when the volume presence cannot be determined",
      "moduleOperations resetModules() the utxo-tracker volume wipe refuses to drop the databases after a failed wipe on reset all",
      "moduleOperations resetModules() the utxo-tracker volume wipe treats docker's own no-such-volume as absence and completes",
      "moduleOperations resetModules() throws on an unknown coin",
      "moduleOperations resetModules() throws on an unknown network",
      "moduleOperations resetModules() throws on an unknown service instead of a silent no-op success",
      "moduleOperations resetModules(): confirmation guard force=true skips the confirmation prompt entirely",
      "moduleOperations resetModules(): confirmation guard refuses to reset on a non-interactive terminal without --yes/force",
      "moduleOperations restartModules() calls restartContainer and statusChanged",
      "moduleOperations restartModules(): error path continues after restartContainer error",
      "moduleOperations runE2ETest() builds mocha docker args when testName is provided",
      "moduleOperations runE2ETest() clones the suite at the ref it was given",
      "moduleOperations runE2ETest() includes --grep when grep is provided with testName",
      "moduleOperations runE2ETest() installs e2e module, waits, saves logs, removes container",
      "moduleOperations runE2ETest() passes null dockerCmdArgs when no testName or script",
      "moduleOperations runE2ETest() passes null when no ref was given, keeping the default-branch behaviour",
      "moduleOperations runE2ETest() uses npm run script when script is provided",
      "moduleOperations shellModule() calls shellContainer for the first module only",
      "moduleOperations shellModule(): error path continues after shellContainer error and returns true",
      "moduleOperations shellModule(): error path returns true when no container found",
      "moduleOperations startModules() looks up container ID and calls startContainer",
      "moduleOperations startModules() skips module when container ID is not found",
      "moduleOperations startModules(): error path continues after startContainer error",
      "moduleOperations stopModules() looks up the container id and stops it with the service budget, not a bare docker stop",
      "moduleOperations stopModules(): skip when no container continues after stopContainer error",
      "moduleOperations stopModules(): skip when no container skips when container ID is not found",
      "moduleOperations uninstallModules() calls uninstallModule for each module",
      "moduleOperations uninstallModules() continues on error for individual modules, then fails the batch",
      "moduleOperations uninstallModules() rejects when all modules fail, naming every one of them",
      "moduleOperations uninstallModules() reports what it removed when every module succeeds",
      "moduleOperations uninstallModules(): includeShared includes shared modules when includeShared=true",
      "moduleOperations uninstallModules(): includeShared includes xchain-sync when includeShared=true",
      "moduleOperations uninstallModules(): includeShared keeps a shared module when another coin/network is still installed",
      "moduleOperations uninstallModules(): includeShared orders the shared pass LAST, so a full teardown still reaches it",
      "moduleOperations uninstallModules(): includeShared refuses the shared removal rather than guessing when the registry is unreadable",
      "moduleOperations uninstallModules(): includeShared skips module when container ID is null",
      "moduleOperations uninstallModules(): includeShared skips shared modules by default",
      "moduleOperations uninstallModules(): includeShared skips xchain-sync by default (shared singleton, same guard as database/hub/explorer)",
      "moduleOperations uninstallModules(): includeShared still removes shared modules once the last coin/network is gone",
      "moduleOperations updateModules() aborts the update, leaving the running container untouched, when the guard refuses",
      "moduleOperations updateModules() checks the migration precondition BEFORE the container is rebuilt",
      "moduleOperations updateModules() counts a rebuilt node as updated",
      "moduleOperations updateModules() fetches existing container ID before updating",
      "moduleOperations updateModules() handles the node module via installModule (built from releases, not git-cloned)",
      "moduleOperations updateModules() leaves the running node container to buildCryptoNode instead of force-removing it up front",
      "moduleOperations updateModules() passes container ID to installModule for replacement",
      "moduleOperations updateModules() rebuilds non-node modules via installModule (which re-clones internally)",
      "moduleOperations updateModules() rebuilds the image, unlike recreate",
      "moduleOperations updateModules() records a declined NODE rebuild as a no-op too",
      "moduleOperations updateModules() records a module installModule declined as a no-op, not as updated",
      "moduleOperations updateModules() recreates the node even when its container is missing (no silent no-op)",
      "moduleOperations updateModules() refuses the database instead of reporting an untouched container as updated",
      "moduleOperations updateModules() reports an uninstalled module as SKIPPED (empty updated list), and warns about it",
      "moduleOperations updateModules() reports what it updated so a no-op run is not indistinguishable from success",
      "moduleOperations updateModules() still skips a NON-node module whose container is missing",
      "moduleOperations updateModules() still updates the other requested modules when the request also names the database",
      "moduleOperations updateModules(): branch handling falls back to getModuleBranch (current branch) when no branch specified",
      "moduleOperations updateModules(): branch handling proceeds with null branch if getModuleBranch throws",
      "moduleOperations updateModules(): branch handling skips module when container ID is not found",
      "moduleOperations updateModules(): branch handling threads the provided branch through to installModule",
      "moduleOperations updateModules(): no-ref target resolution clones the default branch, never \"HEAD\", for a detached module the manifest does not carry",
      "moduleOperations updateModules(): no-ref target resolution keeps a BRANCH node on its branch and takes newer commits",
      "moduleOperations updateModules(): no-ref target resolution moves a RELEASE node to the latest release, pinned, refusing any branch fallback",
      "moduleOperations updateModules(): no-ref target resolution records what the node is on, so the next no-ref update converges on it",
      "moduleOperations updateModules(): no-ref target resolution stops with nothing changed when the latest release cannot be resolved",
      "moduleOperations updateModules(): no-ref target resolution treats an explicitly named branch as a decision and records it",
      "moduleOperations updateModules(): no-ref target resolution uses the tag a self-update already resolved instead of looking it up again",
      "moduleOperations updateModules(): the `all` expansion collapses the absent services into one line under `all`, and keeps the per-service warning for a targeted update",
      "moduleOperations updateModules(): the `all` expansion continues the hub update when the validator repair fails, and says so",
      "moduleOperations updateModules(): the `all` expansion does not install a coin node that is absent under `all`, but still recreates one on a targeted update",
      "moduleOperations updateModules(): the `all` expansion does not touch validator config on a node that is not a validator, or when the hub is out of scope",
      "moduleOperations updateModules(): the `all` expansion leaves a coin node running when it already carries the pinned daemon version",
      "moduleOperations updateModules(): the `all` expansion leaves the hub and sync out of a targeted update, as before",
      "moduleOperations updateModules(): the `all` expansion re-runs the validator repair before rebuilding the hub on an initialized validator",
      "moduleOperations updateModules(): the `all` expansion rebuilds a coin node on a TARGETED update whatever version it runs",
      "moduleOperations updateModules(): the `all` expansion rebuilds a coin node whose pinned version it cannot determine",
      "moduleOperations updateModules(): the `all` expansion reports a shared service that is not installed as skipped, not as a failure",
      "moduleOperations updateModules(): the `all` expansion still rebuilds a coin node that is behind the pinned daemon version",
      "moduleOperations updateModules(): the `all` expansion updates the hub, then sync, then the explorer, before any coin stack",
      "moduleOperations: syncSharedServicesAfterInstall() does nothing when the run installed no coin stack",
      "moduleOperations: syncSharedServicesAfterInstall() honours XCHAIN_NODE_ALLOW_DEGRADED_EXPLORER for install-then-fix flows",
      "moduleOperations: syncSharedServicesAfterInstall() pushes hub config, attaches the explorer, then waits for it to serve",
      "moduleOperations: syncSharedServicesAfterInstall() reports the stack UNUSABLE when the explorer never converges",
      "moduleOperations: syncSharedServicesAfterInstall() reports the stack usable when the explorer converges",
      "moduleOperations: syncSharedServicesAfterInstall() reports usable when the run installed no coin stack",
      "moduleOperations: syncSharedServicesAfterInstall() still waits when the hub push fails, and never fails the command",
      "moduleOperations: syncSharedServicesAfterInstall() tolerates a missing outcome rather than throwing at the end of an install",
      "moduleOperations: syncSharedServicesAfterInstall() warns, without throwing, when the explorer never converges"
    ],
    "b9b75c9f2ae1b837": [
      "ExplorerConnector constructor builds the URL as http://<url>:<port>",
      "ExplorerConnector constructor stores the port on the instance",
      "ExplorerConnector ping() returns false on network error",
      "ExplorerConnector ping() returns false when response has no result",
      "ExplorerConnector ping() returns true when response contains a result",
      "ExplorerConnector ping() sends correct JSON-RPC payload",
      "ExplorerConnector.probe() a 200 carrying no result is answering but not healthy",
      "ExplorerConnector.probe() a 503 is ANSWERING but not healthy: the server replied, it just holds no pool",
      "ExplorerConnector.probe() a refused connection is neither: nothing answered",
      "ExplorerConnector.probe() a success result is answering AND healthy",
      "ExplorerConnector.probe() a timeout is neither, so a hung explorer cannot pass as installed",
      "ExplorerConnector.probe() ping() keeps its old contract, reporting probe()s health half only"
    ],
    "ba304d2c23bb7a06": [
      "BootstrapService bootstrap archive cleanup after a restore attempt keeps the archive and reports its size and path when the restore itself fails",
      "BootstrapService bootstrap archive cleanup after a restore attempt removes latest.tgz and its .sig after a successful decoder (MariaDB) restore",
      "BootstrapService bootstrap archive cleanup after a restore attempt removes latest.tgz and its .sig after a successful utxo-tracker restore, and prints the GiB released",
      "BootstrapService bootstrap archive cleanup after a restore attempt removes the archive on a node-behind refusal too, since a re-run downloads a fresh copy anyway",
      "BootstrapService bootstrap archive cleanup after a restore attempt says in the install summary whether the archive was removed or kept",
      "BootstrapService bootstrap archive cleanup after a restore attempt warns but does not fail the run when removing the archive itself errors",
      "BootstrapService bootstrap listing and staging safety also refuses when only the OUTPUT filesystem is too small",
      "BootstrapService bootstrap listing and staging safety excludes the .sig and .sha256 sidecars from the restorable list",
      "BootstrapService bootstrap listing and staging safety lists archives NEWEST first, so \"the latest\" is the head of the list",
      "BootstrapService bootstrap listing and staging safety orders deterministically when mtimes tie or are unavailable",
      "BootstrapService bootstrap listing and staging safety proceeds when the filesystem has room for the data plus the reserve",
      "BootstrapService bootstrap listing and staging safety refuses BEFORE stopping the container, so a capacity failure costs no downtime",
      "BootstrapService bootstrap listing and staging safety refuses to stage a bootstrap the work-dir filesystem cannot hold",
      "BootstrapService bootstrap restore is reported, not just logged distinguishes \"none published\" from a failure",
      "BootstrapService bootstrap restore is reported, not just logged names the service and the reason when a restore fails",
      "BootstrapService bootstrap restore is reported, not just logged reports a disabled run as disabled rather than failed",
      "BootstrapService bootstrap restore is reported, not just logged reports each service separately when several fail in one run",
      "BootstrapService bootstrap restore is reported, not just logged says nothing at all when no bootstrap was attempted",
      "BootstrapService bootstrap restore is reported, not just logged starts a fresh report per run instead of replaying the last one",
      "BootstrapService downloadBootstrap() creates destDir when it does not exist",
      "BootstrapService downloadBootstrap() falls back to latest.tgz.sig when the archive response carries no redirect URL",
      "BootstrapService downloadBootstrap() pins the .sig fetch to the archive redirect target, not latest.tgz.sig",
      "BootstrapService downloadBootstrap() returns \"latest.tgz\" on success and writes to destPath",
      "BootstrapService downloadBootstrap() returns null on 404",
      "BootstrapService downloadBootstrap() the age of the archive an operator is about to take does not report a negative age for a clock-skewed future stamp",
      "BootstrapService downloadBootstrap() the age of the archive an operator is about to take names the halt for the tracker and only the slower resync for the rest",
      "BootstrapService downloadBootstrap() the age of the archive an operator is about to take reads a same-day archive as zero days old, not stale",
      "BootstrapService downloadBootstrap() the age of the archive an operator is about to take reads the publisher timestamp out of the archive name",
      "BootstrapService downloadBootstrap() the age of the archive an operator is about to take says nothing about a hand-placed name that carries no timestamp",
      "BootstrapService downloadBootstrap() the age of the archive an operator is about to take stays quiet about an archive published today",
      "BootstrapService downloadBootstrap() the age of the archive an operator is about to take stays quiet through a normal weekly publish cadence",
      "BootstrapService downloadBootstrap() the age of the archive an operator is about to take warns during the download when the resolved archive has aged out",
      "BootstrapService downloadBootstrap() throws when axios rejects",
      "BootstrapService downloadBootstrap() throws when data stream emits error",
      "BootstrapService downloadBootstrap() throws when writeStream emits error",
      "BootstrapService downloadBootstrap() uses the BOOTSTRAP_BASE_URL to build the request URL",
      "BootstrapService ensureBootstrapMariaDb() returns false (best-effort) when download throws",
      "BootstrapService ensureBootstrapMariaDb() returns false when XCHAIN_NODE_NO_BOOTSTRAP is set",
      "BootstrapService ensureBootstrapMariaDb() returns false when no bootstrap available for decoder",
      "BootstrapService ensureBootstrapMariaDb() returns false when no bootstrap available for indexer",
      "BootstrapService ensureBootstrapMariaDb() returns true when download succeeds and restoreBootstrap resolves for decoder",
      "BootstrapService ensureBootstrapUtxoTracker() returns false (best-effort) when downloadBootstrap throws",
      "BootstrapService ensureBootstrapUtxoTracker() returns false when XCHAIN_NODE_NO_BOOTSTRAP is set",
      "BootstrapService ensureBootstrapUtxoTracker() returns false when getDefaultConfig throws",
      "BootstrapService ensureBootstrapUtxoTracker() returns false when no bootstrap is available (downloadBootstrap returns null)",
      "BootstrapService ensureBootstrapUtxoTracker() returns true when download succeeds and restoreBootstrap resolves (happy path via restoreBootstrap stub)",
      "BootstrapService ensureDirWritable(): Docker fallback (dir exists, not writable) invokes docker mkdir/chown/chmod when outputDir exists but accessSync throws",
      "BootstrapService forceBootstrapRequested() is off for an explicit 0 or empty value",
      "BootstrapService forceBootstrapRequested() is off when the variable is unset, so a healthy service is never wiped",
      "BootstrapService forceBootstrapRequested() is on for a set value, which is what re-offers a spent restore",
      "BootstrapService getBootstrapFilesList() filters out non-file entries",
      "BootstrapService getBootstrapFilesList() propagates readdir error",
      "BootstrapService getBootstrapFilesList() returns file list for XCHAIN_DECODER",
      "BootstrapService getBootstrapFilesList() returns file list for XCHAIN_INDEXER",
      "BootstrapService getBootstrapFilesList() returns file list for XCHAIN_UTXO_TRACKER",
      "BootstrapService getBootstrapFilesList() throws for unsupported module",
      "BootstrapService listServedBootstrapCombos() de-duplicates and sorts",
      "BootstrapService listServedBootstrapCombos() filters regtest and non-bootstrappable modules",
      "BootstrapService listServedBootstrapCombos() lists a combo whose container is stopped",
      "BootstrapService listServedBootstrapCombos() returns nothing for an empty or unconfigured store",
      "BootstrapService makeBootstrap(): dispatch throws for unsupported module",
      "BootstrapService makeBootstrap(): the reindex republish marker clears the marker after a create that produced an archive",
      "BootstrapService makeBootstrap(): the reindex republish marker leaves the marker standing when the create itself fails",
      "BootstrapService makeBootstrap(): the reindex republish marker leaves the marker standing when the source-health gate refuses",
      "BootstrapService makeBootstrapMariaDb(): happy path discards a finished dump when the source stops being healthy during it",
      "BootstrapService makeBootstrapMariaDb(): happy path dumps decoder DB: stops, streams dump, checksums, wraps, returns true",
      "BootstrapService makeBootstrapMariaDb(): happy path proceeds when mariadb size estimate fails (catch branch, progress shows ?%)",
      "BootstrapService makeBootstrapMariaDb(): happy path throws when getDatabaseContainerId returns null",
      "BootstrapService makeBootstrapMariaDb(): happy path throws when mariadb-dump exits non-zero",
      "BootstrapService makeBootstrapMariaDb(): happy path works for XCHAIN_INDEXER (picks INDEXER_BOOTSTRAP_VOLUME)",
      "BootstrapService makeBootstrapUtxoTracker(): happy path aborts before compressing when a failed snapshot cannot be cleaned up",
      "BootstrapService makeBootstrapUtxoTracker(): happy path checksums the inner archive inline instead of re-reading it",
      "BootstrapService makeBootstrapUtxoTracker(): happy path creates a bootstrap for utxo-tracker: stops container, tars, checksums, wraps, restarts",
      "BootstrapService makeBootstrapUtxoTracker(): happy path encoder maintenance window clears the window as soon as the tracker is back, not when the compress ends",
      "BootstrapService makeBootstrapUtxoTracker(): happy path encoder maintenance window clears the window even when the publish fails mid-compress",
      "BootstrapService makeBootstrapUtxoTracker(): happy path encoder maintenance window declares the window BEFORE the tracker stops",
      "BootstrapService makeBootstrapUtxoTracker(): happy path encoder maintenance window holds the window for the whole compress on the stopped-tracker fallback",
      "BootstrapService makeBootstrapUtxoTracker(): happy path encoder maintenance window publishes normally when the encoder cannot be told",
      "BootstrapService makeBootstrapUtxoTracker(): happy path falls back to compressing with the tracker stopped when the snapshot fails",
      "BootstrapService makeBootstrapUtxoTracker(): happy path proceeds when du estimate fails (catch branch, progress shows ?%)",
      "BootstrapService makeBootstrapUtxoTracker(): happy path refuses to publish a truncated outer archive when the wrap tar dies",
      "BootstrapService makeBootstrapUtxoTracker(): happy path restarts container even when tar spawn fails",
      "BootstrapService makeBootstrapUtxoTracker(): happy path restarts container when docker tar exits non-zero",
      "BootstrapService makeBootstrapUtxoTracker(): happy path restarts the tracker before the compress, off a hardlink snapshot",
      "BootstrapService makeBootstrapUtxoTracker(): happy path throws when container not found",
      "BootstrapService makeBootstrapUtxoTracker(): happy path volume headroom warning stays quiet when the volume has room",
      "BootstrapService makeBootstrapUtxoTracker(): happy path volume headroom warning warns when free space is under the churn headroom",
      "BootstrapService makeBootstrapUtxoTracker(): happy path wraps the outer archive with a store-only gzip, not a second deflate",
      "BootstrapService mariaDbModuleFreshness() reports empty when docker says the database container is gone",
      "BootstrapService mariaDbModuleFreshness() reports empty when the blocks table does not exist (tblOut = 0)",
      "BootstrapService mariaDbModuleFreshness() reports empty when the blocks table exists but has 0 rows",
      "BootstrapService mariaDbModuleFreshness() reports populated for XCHAIN_INDEXER module",
      "BootstrapService mariaDbModuleFreshness() reports populated when the blocks table has data",
      "BootstrapService mariaDbModuleFreshness() reports unknown when askMariadbRootPassword throws",
      "BootstrapService mariaDbModuleFreshness() reports unknown when exec throws on the table check",
      "BootstrapService mariaDbModuleFreshness() reports unknown when getDatabaseContainerId throws",
      "BootstrapService mariaDbModuleFreshness() reports unknown when the container exists but its id will not resolve",
      "BootstrapService mariaDbModuleFreshness() reports unknown when the container presence probe cannot answer",
      "BootstrapService mariaDbModuleFreshness() reports unknown when the container presence probe throws",
      "BootstrapService mariaDbModuleFreshness() reports unknown when the row count does not parse",
      "BootstrapService restoreBootstrap(): dispatch throws for unsupported module",
      "BootstrapService restoreBootstrapMariaDb(): file not found throws when archive file does not exist",
      "BootstrapService restoreBootstrapMariaDb(): file not found throws when archive file does not exist for indexer",
      "BootstrapService restoreBootstrapMariaDb(): happy path drops/recreates DB, restores dump, restarts service container",
      "BootstrapService restoreBootstrapMariaDb(): happy path extracts + verifies the mariadb dump against the declared checksum on a fresh run",
      "BootstrapService restoreBootstrapMariaDb(): happy path throws on malformed mariadb archive (missing dump files)",
      "BootstrapService restoreBootstrapMariaDb(): happy path throws when getDatabaseContainerId returns null",
      "BootstrapService restoreBootstrapMariaDb(): happy path throws when mariadb restore proc exits non-zero",
      "BootstrapService restoreBootstrapMariaDb(): happy path throws when the mariadb dump fails the declared checksum",
      "BootstrapService restoreBootstrapMariaDb(): happy path works for XCHAIN_INDEXER (picks INDEXER_BOOTSTRAP_VOLUME)",
      "BootstrapService restoreBootstrapMariaDb(): integrity refusal is fail-closed and classified classifies a malformed archive (missing inner members) the same way",
      "BootstrapService restoreBootstrapMariaDb(): integrity refusal is fail-closed and classified does not stop the running service for a restore it is going to refuse",
      "BootstrapService restoreBootstrapMariaDb(): integrity refusal is fail-closed and classified leaves an operational failure unclassified so it is not mistaken for tampering",
      "BootstrapService restoreBootstrapMariaDb(): integrity refusal is fail-closed and classified never issues DROP DATABASE, and never starts the import, when the archive fails integrity",
      "BootstrapService restoreBootstrapMariaDb(): integrity refusal is fail-closed and classified raises BootstrapIntegrityError so the CLI can report it as a refusal, not a crash",
      "BootstrapService restoreBootstrapUtxoTracker(): file not found throws when archive file does not exist",
      "BootstrapService restoreBootstrapUtxoTracker(): fresh extract extracts outer archive, verifies inner against the declared checksum, restores volume",
      "BootstrapService restoreBootstrapUtxoTracker(): fresh extract refuses an outer archive with an unsafe member path (never extracts)",
      "BootstrapService restoreBootstrapUtxoTracker(): fresh extract throws when container ID is null (pool not ready)",
      "BootstrapService restoreBootstrapUtxoTracker(): fresh extract throws when container ID is null but DB pool is ready (no matching row)",
      "BootstrapService restoreBootstrapUtxoTracker(): fresh extract throws when malformed archive (inner archive missing after extract)",
      "BootstrapService restoreBootstrapUtxoTracker(): fresh extract throws when tar restore proc exits non-zero",
      "BootstrapService restoreBootstrapUtxoTracker(): fresh extract throws when the inner archive fails the declared checksum (before container stop, no finally restart)",
      "BootstrapService restoreBootstrapUtxoTracker(): resumable reuse (inner archive already matches verified checksum) discards a pre-planted work-dir inner archive that does NOT match the verified checksum, and re-extracts",
      "BootstrapService restoreBootstrapUtxoTracker(): resumable reuse (inner archive already matches verified checksum) reuses a work-dir inner archive whose bytes match the verified checksum (skips outer extract)",
      "BootstrapService the coin node tip guard between download and restore a behind-wait verdict still restores and the summary marks it WAITING FOR NODE",
      "BootstrapService the coin node tip guard between download and restore a refusal records node-behind, restores nothing, and the summary says how to take it later",
      "BootstrapService the coin node tip guard between download and restore the tracker path refuses the same way, before its volume is touched",
      "BootstrapService utxoTrackerVolumeFreshness() reports empty when docker itself says there is no such volume",
      "BootstrapService utxoTrackerVolumeFreshness() reports empty when volume ls output is empty",
      "BootstrapService utxoTrackerVolumeFreshness() reports populated when volume ls shows a non-empty entry",
      "BootstrapService utxoTrackerVolumeFreshness() reports unknown when ls exec fails",
      "BootstrapService utxoTrackerVolumeFreshness() reports unknown when the inspect fails for any other reason",
      "BootstrapService utxoTrackerVolumeFreshness() uses a listing command that exits non-zero when the listing fails"
    ],
    "bb7e5453878cf8dd": [
      "preCheck(): a hub that is not answering does not wedge its own repair @regression a NON-repairing command still aborts against a hub that is not answering",
      "preCheck(): a hub that is not answering does not wedge its own repair @regression a repairing command against a HEALTHY hub still pushes and still fails on a real error",
      "preCheck(): a hub that is not answering does not wedge its own repair @regression hydrates the sidecar key before the liveness probe on the unhealthy path",
      "preCheck(): a hub that is not answering does not wedge its own repair @regression is not consulted at all for a read-only command that skips the push",
      "preCheck(): a hub that is not answering does not wedge its own repair @regression lets a repairing command through on a warning, skipping the push",
      "preCheck(): a hub that is not answering does not wedge its own repair @regression still continues when the skipped push leaves updateHub rejecting anyway",
      "preCheck(): a hub that is not answering does not wedge its own repair @regression treats a liveness probe that throws as \"not answering\"",
      "preCheck(): containerd data-root relocation warning @regression does not block the command when the containerd probe throws",
      "preCheck(): containerd data-root relocation warning @regression prints a warning when Docker data-root moved off / but containerd is still on /",
      "preCheck(): containerd data-root relocation warning @regression prints no containerd warning when the probe reports no relocation hazard",
      "preCheck(): hub/explorer config push @regression pushes both and returns true when neither rejects",
      "preCheck(): hub/explorer config push @regression still pushes explorer config when updateHub rejects, and fails the command",
      "preCheck(): remote version check degrades gracefully @regression a GitHub 403 rate-limit rejection does not abort preCheck",
      "preCheck(): remote version check degrades gracefully @regression a successful version check keeps checkVersions=true for getStatus",
      "preCheck(): remote version check degrades gracefully @regression checkVersions=false skips the remote version fetch entirely",
      "preCheck(): xchain_node pool host/port resolution @regression EXTERNAL_DB \u2192 never falls back to the 127.0.0.1 load-time default",
      "preCheck(): xchain_node pool host/port resolution @regression EXTERNAL_DB \u2192 opens the pool against the getExternalDbConfig() host/port",
      "preCheck(): xchain_node pool host/port resolution @regression a failing external-DB resolution surfaces as the pool-open error",
      "preCheck(): xchain_node pool host/port resolution @regression docker mode \u2192 127.0.0.1 with the live container port-forward",
      "preCheck: the CLI presents the sidecar HUB_API_KEY to the hub @regression hydrates process.env from the sidecar before the hub is installed or pushed to",
      "preCheck: the CLI presents the sidecar HUB_API_KEY to the hub @regression is a no-op on a host with no sidecar (a standalone install stays keyless)",
      "preCheck: the CLI presents the sidecar HUB_API_KEY to the hub @regression runs the hydration through the real sidecar reader with a host-env key left untouched",
      "preCheck: the hub is staged at the ref the command named passes null when the command named no ref, preserving the old behaviour",
      "preCheck: the hub is staged at the ref the command named passes the ref through to installHubModule"
    ],
    "bcfaa8d7327dcba5": [
      "DbCredentialDrift assertNoDbCredentialDrift ignores the container the caller is about to replace",
      "DbCredentialDrift assertNoDbCredentialDrift is a no-op when no decoder or indexer container is running",
      "DbCredentialDrift assertNoDbCredentialDrift proceeds with a warning when the override env is set",
      "DbCredentialDrift assertNoDbCredentialDrift resolves when the running containers agree with the config",
      "DbCredentialDrift assertNoDbCredentialDrift still flags a sibling that would be locked out, and names the excluded module in the remedy",
      "DbCredentialDrift assertNoDbCredentialDrift throws a tagged error naming the locked-out container",
      "DbCredentialDrift assertNoHubDbCredentialDrift ignores a container the caller is about to replace, and never inspects it",
      "DbCredentialDrift assertNoHubDbCredentialDrift proceeds with a warning when the override env is set",
      "DbCredentialDrift assertNoHubDbCredentialDrift resolves when every holder of the shared account agrees",
      "DbCredentialDrift assertNoHubDbCredentialDrift throws a tagged error naming a sibling hub under a different prefix",
      "DbCredentialDrift assertNoHubDbCredentialDrift treats an unreadable docker daemon as no drift",
      "DbCredentialDrift findDbCredentialDrift flags a decoder that disagrees on the shared decoder account",
      "DbCredentialDrift findDbCredentialDrift flags an indexer built from another install on both accounts",
      "DbCredentialDrift findDbCredentialDrift handles an empty container list and a missing intended map",
      "DbCredentialDrift findDbCredentialDrift ignores a key absent from either side",
      "DbCredentialDrift findDbCredentialDrift ignores env keys the module does not authenticate with",
      "DbCredentialDrift findDbCredentialDrift reports nothing when every container carries the intended password",
      "DbCredentialDrift findDbCredentialDrift treats an empty intended value as no claim rather than as a mismatch",
      "DbCredentialDrift findHubDbCredentialDrift does not flag an indexer whose hub connection uses its own account",
      "DbCredentialDrift findHubDbCredentialDrift flags a sibling install whose hub carries a different shared password",
      "DbCredentialDrift findHubDbCredentialDrift treats an absent or empty value on either side as no claim",
      "DbCredentialDrift formatDbCredentialDriftError leaks no credential value into the message",
      "DbCredentialDrift formatDbCredentialDriftError names the container and the remediation but never a password",
      "DbCredentialDrift isDbCredentialDriftError separates the drift refusal from an unrelated failure",
      "DbCredentialDrift readContainerEnv parses docker inspect output into a plain object",
      "DbCredentialDrift readContainerEnv returns null for a container that does not exist",
      "DbCredentialDrift readContainerEnv returns null on unparseable output rather than throwing"
    ],
    "bdea07439a6b5cec": [
      "HubConsensusEnvGuard assertNoHubConsensusEnvDrift() is a no-op (returns empty) on a fresh install with no running hub container",
      "HubConsensusEnvGuard assertNoHubConsensusEnvDrift() lets a mainnet recreate unset a stale, already-ignored derivation override",
      "HubConsensusEnvGuard assertNoHubConsensusEnvDrift() proceeds and logs when the override env is set",
      "HubConsensusEnvGuard assertNoHubConsensusEnvDrift() resolves when the running hub agrees with the new deploy",
      "HubConsensusEnvGuard assertNoHubConsensusEnvDrift() still refuses a regtest recreate that would drop a honored derivation override",
      "HubConsensusEnvGuard assertNoHubConsensusEnvDrift() throws a tagged error when the running hub would lose a value",
      "HubConsensusEnvGuard covers the four regtest-only XCHAIN/BTC derivation overrides too",
      "HubConsensusEnvGuard covers the named consensus-shaped var groups",
      "HubConsensusEnvGuard describeConsensusEnvSupply() does not report a regtest-only override as missing on a mainnet deploy",
      "HubConsensusEnvGuard describeConsensusEnvSupply() does report a regtest-only override as missing on a regtest deploy",
      "HubConsensusEnvGuard describeConsensusEnvSupply() splits supplied from defaulted",
      "HubConsensusEnvGuard describeConsensusEnvSupply() takes an explicit network, for the caller that resolved it from the live container",
      "HubConsensusEnvGuard describeConsensusEnvSupply() treats a null/undefined intended object as everything defaulted",
      "HubConsensusEnvGuard describeConsensusEnvSupply() treats an empty string the same as unset (defaulted)",
      "HubConsensusEnvGuard findHubConsensusEnvDrift() DOES flag a dropped override on a regtest venue, where the hub honors it",
      "HubConsensusEnvGuard findHubConsensusEnvDrift() DOES flag a dropped override when only the RUNNING container says regtest",
      "HubConsensusEnvGuard findHubConsensusEnvDrift() compares the price-indexer DB password without ever needing to log it",
      "HubConsensusEnvGuard findHubConsensusEnvDrift() does NOT call it drift when a mainnet deploy CHANGES an override the hub ignores",
      "HubConsensusEnvGuard findHubConsensusEnvDrift() does NOT call it drift when a mainnet deploy unsets an override the hub already ignores",
      "HubConsensusEnvGuard findHubConsensusEnvDrift() does NOT call it drift when a testnet or standalone deploy unsets one either",
      "HubConsensusEnvGuard findHubConsensusEnvDrift() flags a key the deploy would silently CHANGE (present but different)",
      "HubConsensusEnvGuard findHubConsensusEnvDrift() flags a key the deploy would silently DROP (present live, absent from the new deploy)",
      "HubConsensusEnvGuard findHubConsensusEnvDrift() flags every drifted key in one pass, not just the first",
      "HubConsensusEnvGuard findHubConsensusEnvDrift() reports nothing when the deploy agrees with the running container",
      "HubConsensusEnvGuard findHubConsensusEnvDrift() reports nothing when the running container never carried the key",
      "HubConsensusEnvGuard findHubConsensusEnvDrift() reports nothing when there is no running container",
      "HubConsensusEnvGuard formatHubConsensusEnvDriftError() names the drifted keys and the override escape hatch, never a value",
      "HubConsensusEnvGuard isHubConsensusEnvDriftError() separates this guard's refusal from an unrelated failure",
      "HubConsensusEnvGuard logConsensusEnvSupplyState() logs (not warns) the supplied keys and stays silent about warning when all are supplied",
      "HubConsensusEnvGuard logConsensusEnvSupplyState() warns naming every defaulted key when nothing is supplied",
      "HubConsensusEnvGuard network gating falls back to the running container when the invoking shell dropped HUB_NETWORK",
      "HubConsensusEnvGuard network gating honors the derivation overrides on regtest and nowhere else",
      "HubConsensusEnvGuard network gating leaves the ungated keys, including the per-operator DB source, honored on every network",
      "HubConsensusEnvGuard network gating resolves standalone (unset everywhere) to the empty network",
      "HubConsensusEnvGuard network gating resolves the network from this deploy's own HUB_NETWORK first",
      "HubConsensusEnvGuard network gating scopes the walked key list to the network"
    ],
    "cdda2d3e70c102b1": [
      "utils/dockerMariadb dockerMariadbArgs() builds a docker exec invocation forwarding MYSQL_PWD by name only",
      "utils/dockerMariadb dockerMariadbArgs() inserts -i before the env forward when interactive",
      "utils/dockerMariadb dockerMariadbArgs() never embeds a password value in argv",
      "utils/dockerMariadb mariadbEnv() does not mutate process.env",
      "utils/dockerMariadb mariadbEnv() returns process.env plus MYSQL_PWD"
    ],
    "d1b47eeb57e49143": [
      "scan-nine-field-files CLI argument reading does not match a flag name appearing as another flag value",
      "scan-nine-field-files CLI argument reading reads a flag value out of the vector",
      "scan-nine-field-files CLI argument reading returns the fallback for an absent flag",
      "scan-nine-field-files CLI argument reading returns the fallback when a trailing flag has no value",
      "scan-nine-field-files CLI corpus measurement reports the payload row count",
      "scan-nine-field-files CLI corpus measurement reports unknown for an empty result set",
      "scan-nine-field-files CLI corpus measurement reports unknown rather than zero when the count cannot be read",
      "scan-nine-field-files CLI keyset paging accumulates hits across pages",
      "scan-nine-field-files CLI keyset paging advances the cursor to the last tx_index of each page",
      "scan-nine-field-files CLI keyset paging breaks on a non-numeric tx_index instead of paging from NaN",
      "scan-nine-field-files CLI keyset paging breaks rather than spinning when the cursor does not advance",
      "scan-nine-field-files CLI keyset paging carries the limit into the paging SQL",
      "scan-nine-field-files CLI keyset paging counts the stalled page before stopping, so its rows are not lost",
      "scan-nine-field-files CLI keyset paging reports a clean store as scanned, not as unexamined",
      "scan-nine-field-files CLI keyset paging starts the cursor below zero so tx_index 0 is scanned",
      "scan-nine-field-files CLI keyset paging stops on the first empty page",
      "scan-nine-field-files CLI the entrypoint guard exports the helpers without running the scan on require"
    ],
    "d966ad5aa2291a4b": [
      "StatusService: getInstalledCoinsAndNetworks() ignores coins that are not in the Coin enum",
      "StatusService: getInstalledCoinsAndNetworks() returns coins and networks filtered by known Coin/Network values",
      "StatusService: getInstalledCoinsAndNetworks() returns empty object when no modules are installed",
      "StatusService: getStatus() DB not ready returns empty object when db is not ready",
      "StatusService: getStatus() branch column includes branch column in output when any module is on a non-master branch",
      "StatusService: getStatus() branch column omits branch column when all modules are on master",
      "StatusService: getStatus() cache hit prints cached status when printStatus=true and cache is hot",
      "StatusService: getStatus() cache hit returns cached status immediately when isStatusUpdated is true",
      "StatusService: getStatus() commit column never shows the module checkout as a container commit",
      "StatusService: getStatus() commit column omits the commit column when no image carries a source stamp",
      "StatusService: getStatus() commit column prints the commit each container was built from, per container",
      "StatusService: getStatus() with installed modules annotates the STATUS cell with restart count and non-healthy health status",
      "StatusService: getStatus() with installed modules builds status for a running container and returns installedModules",
      "StatusService: getStatus() with installed modules calls checkRemoteNodeVersion when checkVersions=true and version absent",
      "StatusService: getStatus() with installed modules cleans up empty coin slot when all containers are confirmed gone",
      "StatusService: getStatus() with installed modules does NOT prune or touch the registry on a transient inspect failure (daemon unreachable)",
      "StatusService: getStatus() with installed modules formats port bindings into a comma-separated string",
      "StatusService: getStatus() with installed modules handles node module versions via getLocalNodeVersion / getContainerNodeVersion",
      "StatusService: getStatus() with installed modules leaves a clean container (no restarts, healthy) unchanged and green",
      "StatusService: getStatus() with installed modules prints status output when printStatus=true on fresh call",
      "StatusService: getStatus() with installed modules removes module from installedModules AND reconciles the registry when docker confirms the container is gone",
      "StatusService: getStatus() with installed modules sets local_version and container_version on module",
      "StatusService: getStatus() with installed modules sets remote_version on module from remoteModuleVersions",
      "StatusService: loadInstalledModules() calls checkRemoteNodeVersion when checkVersions=true",
      "StatusService: loadInstalledModules() handles multiple modules for same coin/network",
      "StatusService: loadInstalledModules() populates installedModules from db.getAllModuleContainers",
      "StatusService: statusChanged() reports the hub failure first when both pushes reject",
      "StatusService: statusChanged() sets statusUpdated to false and calls updateHub + updateExplorer",
      "StatusService: statusChanged() still pushes explorer config when updateHub rejects, then rethrows",
      "StatusService: the advisory remote-version check never aborts the command getStatus completes and returns the table when the per-coin version check is rate-limited",
      "StatusService: the advisory remote-version check never aborts the command loadInstalledModules still registers containers when the version check is rate-limited",
      "StatusService: the advisory remote-version check never aborts the command still fills the remote version column when GitHub answers",
      "StatusService: the advisory remote-version check never aborts the command warns once per pass while still attempting every coin"
    ],
    "da1b4dbe85ee8518": [
      "commandLock acquires the lock and writes pid + command to the lock file",
      "commandLock creates the lock directory when missing",
      "commandLock getLockFilePath defaults to ~/.xchain-node/command.lock",
      "commandLock getLockFilePath honors XCHAIN_NODE_LOCK_DIR",
      "commandLock isPidAlive: our own pid is alive, pid 0/negative/junk are not",
      "commandLock refuses a second acquire while the first (live pid) holds the lock",
      "commandLock release does not clobber a successor lock owned by another pid",
      "commandLock release is idempotent",
      "commandLock stale reclamation is serialized a contender acting on a stale observation cannot delete a successor lock",
      "commandLock stale reclamation is serialized a reclaim marker abandoned by a dead pid does not deadlock acquisition",
      "commandLock stale reclamation is serialized a reclaim marker held by a LIVE pid stops the steal instead of racing it",
      "commandLock stale reclamation is serialized leaves no reclaim marker behind after a successful steal",
      "commandLock stale reclamation is serialized release keeps a successor lock that reuses our pid under a different nonce",
      "commandLock steals a lock file with unparseable contents",
      "commandLock steals a stale lock left by a dead pid",
      "commandLock waitMs blocks for roughly waitMs against a live holder, then throws",
      "commandLock waitMs still steals a stale lock immediately (no needless wait)"
    ],
    "dd6e4f59d7ec2e9a": [
      "check-rebase-completeness CLI (deploy-informing script) connection options carries the store host, port and user, coercing the port to a number",
      "check-rebase-completeness CLI (deploy-informing script) connection options defaults a bare store to loopback, 3306 and root",
      "check-rebase-completeness CLI (deploy-informing script) connection options falls back to 3306 on an unparseable port instead of connecting to NaN",
      "check-rebase-completeness CLI (deploy-informing script) connection options takes the target database from the caller, not from the store record",
      "check-rebase-completeness CLI (deploy-informing script) credentials falls back to the fleet-wide password",
      "check-rebase-completeness CLI (deploy-informing script) credentials never reads the password out of the config file",
      "check-rebase-completeness CLI (deploy-informing script) credentials prefers the per-store password over the fleet-wide one",
      "check-rebase-completeness CLI (deploy-informing script) credentials throws, naming both variables, when neither is set",
      "check-rebase-completeness CLI (deploy-informing script) credentials treats an empty per-store value as unset and keeps falling back",
      "check-rebase-completeness CLI (deploy-informing script) label slugs folds punctuation and spacing into single underscores",
      "check-rebase-completeness CLI (deploy-informing script) label slugs strips leading and trailing underscores so the env var name is usable",
      "check-rebase-completeness CLI (deploy-informing script) label slugs survives a missing label rather than throwing on undefined",
      "check-rebase-completeness CLI (deploy-informing script) store lookup finds a store by its database name",
      "check-rebase-completeness CLI (deploy-informing script) store lookup returns null for an unknown database and for an empty config",
      "check-rebase-completeness CLI (deploy-informing script) the CREATE_TIME read binds the database name ahead of the table list",
      "check-rebase-completeness CLI (deploy-informing script) the CREATE_TIME read closes the connection even when the read throws",
      "check-rebase-completeness CLI (deploy-informing script) the CREATE_TIME read does not let a failed close change the verdict",
      "check-rebase-completeness CLI (deploy-informing script) the CREATE_TIME read pins the session to UTC BEFORE reading create times",
      "check-rebase-completeness CLI (deploy-informing script) the CREATE_TIME read propagates a missing password rather than connecting anonymously",
      "check-rebase-completeness CLI (deploy-informing script) the CREATE_TIME read reads information_schema, not the store database",
      "check-rebase-completeness CLI (deploy-informing script) the CREATE_TIME read returns the driver rows unchanged",
      "check-rebase-completeness CLI (deploy-informing script) the CREATE_TIME read throws for a database the config never inventoried",
      "check-rebase-completeness CLI (deploy-informing script) the config file parses a config from disk and resolves a relative path",
      "check-rebase-completeness CLI (deploy-informing script) the config file throws on malformed JSON so the run exits 2 rather than sweeping nothing",
      "check-rebase-completeness CLI (deploy-informing script) the entrypoint guard exports the helpers without running the sweep on require",
      "check-rebase-completeness CLI (deploy-informing script) the epoch-marker read coerces a non-string tag rather than leaking a driver type",
      "check-rebase-completeness CLI (deploy-informing script) the epoch-marker read degrades every fault to \"no marker\" instead of to a verdict",
      "check-rebase-completeness CLI (deploy-informing script) the epoch-marker read reads the store database itself and returns the newest batch tag",
      "check-rebase-completeness CLI (deploy-informing script) the epoch-marker read returns null for an unknown database without opening a connection",
      "check-rebase-completeness CLI (deploy-informing script) the epoch-marker read swallows a missing password here, because a marker is only corroboration",
      "check-rebase-completeness CLI (deploy-informing script) the sweep wiring end to end feeds both readers into sweepFleet and fails a surviving store",
      "check-rebase-completeness CLI (deploy-informing script) the sweep wiring end to end passes a store whose tables were all rebuilt inside the window"
    ],
    "de606d67691f176b": [
      "Bootstrap signing checkBootstrapSignature() policy fails closed BY DEFAULT when no public key is pinned",
      "Bootstrap signing checkBootstrapSignature() policy fails closed when the key is pinned but the signature file is missing and REQUIRE is set",
      "Bootstrap signing checkBootstrapSignature() policy fails closed without a pinned key when XCHAIN_NODE_REQUIRE_SIGNED_BOOTSTRAP is set",
      "Bootstrap signing checkBootstrapSignature() policy throws when pinned key + signature are present but the signature is bad",
      "Bootstrap signing checkBootstrapSignature() policy verifies and passes when pinned key + valid signature are present",
      "Bootstrap signing checkBootstrapSignature() policy warns but proceeds when enforcement is explicitly disabled (=0)",
      "Bootstrap signing downloadBootstrap(): companion signature fetch downloads and stores the .sig next to the archive",
      "Bootstrap signing downloadBootstrap(): companion signature fetch removes a stale local .sig when the server has none (404)",
      "Bootstrap signing refusals are classified as BootstrapIntegrityError names a bad signature",
      "Bootstrap signing refusals are classified as BootstrapIntegrityError names a malformed signature file",
      "Bootstrap signing refusals are classified as BootstrapIntegrityError names an unsigned-bootstrap refusal",
      "Bootstrap signing refusals are classified as BootstrapIntegrityError still carries the operator-facing reason in the message",
      "Bootstrap signing rejects a malformed signature file",
      "Bootstrap signing rejects a signature from the wrong key",
      "Bootstrap signing rejects a tampered archive",
      "Bootstrap signing sign \u2192 verify round-trip passes"
    ],
    "e12b9a311ad07cac": [
      "cli commandRepairsHub() says no when the command needs a hub it cannot fix for `all` on the verbs whose expansion leaves the hub out",
      "cli commandRepairsHub() says no when the command needs a hub it cannot fix for `stop`, which takes the hub down rather than bringing it back",
      "cli commandRepairsHub() says no when the command needs a hub it cannot fix for `uninstall all` without the shared flag, which leaves the hub standing",
      "cli commandRepairsHub() says no when the command needs a hub it cannot fix for every verb that only reads or writes THROUGH the hub",
      "cli commandRepairsHub() says no when the command needs a hub it cannot fix tolerates a missing args array rather than throwing inside the hook",
      "cli commandRepairsHub() says no when the command needs a hub it cannot fix when a repairing verb targets some OTHER service",
      "cli commandRepairsHub() says no when the command needs a hub it cannot fix when resolveArgs refuses the argument shape",
      "cli commandRepairsHub() says yes when the command reaches the hub container for `uninstall all --include-shared`, the teardown that removes the hub",
      "cli commandRepairsHub() says yes when the command reaches the hub container for `update` at a named ref, where the ref is not a service",
      "cli commandRepairsHub() says yes when the command reaches the hub container for `update` with no service, which is the documented whole-node upgrade",
      "cli commandRepairsHub() says yes when the command reaches the hub container for autoheal, whose whole job is restarting containers that are unhealthy",
      "cli commandRepairsHub() says yes when the command reaches the hub container for the hub named outright on every verb that replaces or restarts it",
      "cli commandRepairsHub() says yes when the command reaches the hub container through the short alias operators actually type",
      "cli refForPreCheck() applies to update as well, which also takes a ref",
      "cli refForPreCheck() finds the ref regardless of argument order",
      "cli refForPreCheck() reads a release TAG, so a pinned install stages the pinned hub",
      "cli refForPreCheck() reads a release branch, which is what the ceremony freeze gate installs",
      "cli refForPreCheck() reads the branch out of a full install invocation",
      "cli refForPreCheck() returns null for an install with no arguments at all",
      "cli refForPreCheck() returns null for every command that does not install at a ref",
      "cli refForPreCheck() returns null when every argument is a known service/chain/network",
      "cli refForPreCheck() swallows a resolveArgs refusal rather than aborting before the action can report it"
    ],
    "e2d431011abfd746": [
      "publish-bootstraps.sh: forced republish after a reindex --no-forced-due falls back to the schedule alone",
      "publish-bootstraps.sh: forced republish after a reindex defers a due tracker but reports it on every run",
      "publish-bootstraps.sh: forced republish after a reindex does not fail the run when the node cannot list due combos",
      "publish-bootstraps.sh: forced republish after a reindex drops a due line that is not a plain combo triple",
      "publish-bootstraps.sh: forced republish after a reindex keeps the scheduled plan when nothing was reindexed",
      "publish-bootstraps.sh: forced republish after a reindex pulls a due combo into a plan that would have skipped it",
      "publish-bootstraps.sh: forced republish after a reindex pulls in a due combo the resolved plan never contained",
      "publish-bootstraps.sh: forced republish after a reindex republishes a due tracker when the operator accepts the downtime"
    ],
    "e3665c51c64c9661": [
      "HubService.buildCheckpointConfig hub endpoint defaults the endpoint to the hub container on the docker network",
      "HubService.buildCheckpointConfig hub endpoint emits a hub_url alongside every self_sync it advertises",
      "HubService.buildCheckpointConfig hub endpoint honours an operator HUB_API_URL from the host env",
      "HubService.buildCheckpointConfig hub endpoint keeps the mirror schema and indexer credentials it already carried",
      "HubService.isCheckpointSelfSyncEnabled is not opted in when neither the env nor the container says so",
      "HubService.isCheckpointSelfSyncEnabled is not opted in when there is no explorer container to ask",
      "HubService.isCheckpointSelfSyncEnabled is opted in when the host env carries the flag",
      "HubService.isCheckpointSelfSyncEnabled reads the flag itself off the container when it is there",
      "HubService.isCheckpointSelfSyncEnabled stays opted in when the env is gone but the explorer container remembers",
      "HubService.isCheckpointSelfSyncEnabled treats an empty env value as unset rather than as an opt-in",
      "HubService.isHubAnswering asks once, with no retry loop of its own",
      "HubService.isHubAnswering is false when nothing answers",
      "HubService.isHubAnswering is true when the hub answers a ping",
      "HubService.updateHub network attachment rejects naming the unreachable network instead of reporting success",
      "HubService.updateHub network attachment retries a failed attach once and succeeds on the second try",
      "HubService.updateHub network attachment returns true when every shared-container attach succeeds",
      "HubService.updateHub skipConfigPush attaches the shared containers but never reaches the config push",
      "HubService.updateHub skipConfigPush defaults to pushing, so no existing caller changes behaviour",
      "HubService.updateHub skipConfigPush pushes as it always has when not told to skip"
    ],
    "e7b8ff5f97f1f7f3": [
      "BootstrapRepublishLedger getReindexLedgerPath() defaults to the per-user ~/.xchain-node dir, not the data dir",
      "BootstrapRepublishLedger isRepublishDue() is due with a reindex and no publish",
      "BootstrapRepublishLedger isRepublishDue() is not due when the publish is at or after the reindex",
      "BootstrapRepublishLedger isRepublishDue() is not due without a reindex at all",
      "BootstrapRepublishLedger isRepublishDue() treats an unparseable publish timestamp as no publish",
      "BootstrapRepublishLedger isRepublishDue() treats an unparseable reindex timestamp as no reindex",
      "BootstrapRepublishLedger reading a damaged ledger drops entries that are not objects",
      "BootstrapRepublishLedger reading a damaged ledger drops keys that are not publishable combos",
      "BootstrapRepublishLedger reading a damaged ledger starts clean on a well-formed file of the wrong shape",
      "BootstrapRepublishLedger reading a damaged ledger starts clean on unparseable JSON rather than throwing inside a reset",
      "BootstrapRepublishLedger recordBootstrapPublished() clears a due combo once a newer archive exists",
      "BootstrapRepublishLedger recordBootstrapPublished() clears only the combo it names",
      "BootstrapRepublishLedger recordBootstrapPublished() does not clear a combo whose newest archive predates the reindex",
      "BootstrapRepublishLedger recordBootstrapPublished() does not grow the ledger for a combo that was never reindexed",
      "BootstrapRepublishLedger recordReindex() -> listRepublishDue() is silent for a box that never reindexed",
      "BootstrapRepublishLedger recordReindex() -> listRepublishDue() keeps the newest reindex when a combo is wiped twice",
      "BootstrapRepublishLedger recordReindex() -> listRepublishDue() makes a reindexed combo due, with its reason",
      "BootstrapRepublishLedger recordReindex() -> listRepublishDue() refuses a combo that is not publishable",
      "BootstrapRepublishLedger recordReindex() -> listRepublishDue() returns a stable, sorted list across several combos",
      "BootstrapRepublishLedger reindexAffectedModules() marks all three for a reset all, through their own wipes",
      "BootstrapRepublishLedger reindexAffectedModules() marks exactly the wiped service",
      "BootstrapRepublishLedger reindexAffectedModules() marks nothing extra for a node datadir wipe on its own",
      "BootstrapRepublishLedger reindexAffectedModules() marks nothing when nothing was wiped",
      "BootstrapRepublishLedger the bootstrap-republish-due command --json carries the timestamps the operator needs to judge the gap",
      "BootstrapRepublishLedger the bootstrap-republish-due command answers while another command holds the lock and Docker is unreachable",
      "BootstrapRepublishLedger the bootstrap-republish-due command prints nothing and succeeds when no combo is due",
      "BootstrapRepublishLedger writeReindexLedger() replaces the file atomically and leaves no temp file behind",
      "BootstrapRepublishLedger writeReindexLedger() reports failure instead of throwing when the target is unwritable"
    ],
    "eb89a6dc1945c342": [
      "SERVICE_REGISTRY buildModuleDockerArgs() conditional port omitted when a key is missing",
      "SERVICE_REGISTRY buildModuleDockerArgs() decoder: conditional port + unconditional bootstrap volume",
      "SERVICE_REGISTRY buildModuleDockerArgs() encoder: port only, no volume",
      "SERVICE_REGISTRY buildModuleDockerArgs() explorer: singleton with two unconditional port mappings",
      "SERVICE_REGISTRY buildModuleDockerArgs() hub: a mount refusal from ValidatorService fails the build instead of silently dropping the config",
      "SERVICE_REGISTRY buildModuleDockerArgs() hub: mounts the capability config DIRECTORY (ro) when HUB_CAPABILITY_CONFIG is present",
      "SERVICE_REGISTRY buildModuleDockerArgs() hub: mounts the generated DOGE signer (ro) with this package's node_modules beside it",
      "SERVICE_REGISTRY buildModuleDockerArgs() hub: singleton, unconditional single port, no static volumes when unconfigured",
      "SERVICE_REGISTRY buildModuleDockerArgs() sync: singleton, conditional port present",
      "SERVICE_REGISTRY buildModuleDockerArgs() unknown / docker-less module yields empty args (e2e-test, node, database)",
      "SERVICE_REGISTRY buildModuleDockerArgs() utxo-tracker: port + dynamic data volume + bootstrap volume + nofile ulimit",
      "SERVICE_REGISTRY coverage parity with canonical service enums has a docker facet for every buildAndUp-installed service",
      "SERVICE_REGISTRY coverage parity with canonical service enums has a hubConfig facet for every module the hub reports on",
      "SERVICE_REGISTRY coverage parity with canonical service enums marks exactly hub/explorer/sync as singletons",
      "SERVICE_REGISTRY coverage parity with canonical service enums the one-shot e2e-test runner has no docker facet (no ports/volumes)",
      "SERVICE_REGISTRY coverage parity with canonical service enums the singleton services contribute no hub config"
    ],
    "f23059ddff16822e": [
      "bundled crypto-node conf files every rpcuser/rpcpassword line carries the placeholder token, not a literal credential",
      "bundled crypto-node conf files finds conf files for every coin/network combo"
    ],
    "f3c42962f4a597a9": [
      "rebase-completeness sweep fleet sweep a passing report says so plainly",
      "rebase-completeness sweep fleet sweep fails the fleet when any single store fails, and keeps sweeping the rest",
      "rebase-completeness sweep fleet sweep refuses an empty store list rather than reporting an empty pass",
      "rebase-completeness sweep fleet sweep refuses to run without a window_open, which would pass everything",
      "rebase-completeness sweep fleet sweep the report names the survivor and refuses to bless the window",
      "rebase-completeness sweep it cannot say yes by accident a NULL CREATE_TIME FAILS rather than comparing as ancient or as now",
      "rebase-completeness sweep it cannot say yes by accident a missing marker does not fail a store whose tables all postdate the window",
      "rebase-completeness sweep it cannot say yes by accident an empty replayed list FAILS instead of reporting green",
      "rebase-completeness sweep it cannot say yes by accident an epoch marker does NOT rescue a stale table (the vacuous-marker case)",
      "rebase-completeness sweep it cannot say yes by accident an inventoried table absent from the store FAILS",
      "rebase-completeness sweep it cannot say yes by accident an unreadable store FAILS rather than skipping",
      "rebase-completeness sweep the SQL shape asks the SERVER for epoch seconds, never a zone-less DATETIME",
      "rebase-completeness sweep the SQL shape binds one placeholder per table plus the schema, so no name is interpolated",
      "rebase-completeness sweep the SQL shape exports the UTC session pin the read depends on",
      "rebase-completeness sweep the sweep says yes only when every replayed table postdates the window FAILS the store and names the survivor when one table predates the window",
      "rebase-completeness sweep the sweep says yes only when every replayed table postdates the window a table created exactly at the window opening counts as replayed",
      "rebase-completeness sweep the sweep says yes only when every replayed table postdates the window passes a store whose replayed tables were all recreated inside the window",
      "rebase-completeness sweep timestamp handling accepts Date, ISO, MariaDB DATETIME text, seconds and milliseconds",
      "rebase-completeness sweep timestamp handling reads DATETIME text as UTC, not local time",
      "rebase-completeness sweep timestamp handling returns null for what it cannot establish, never 0 and never now"
    ],
    "fc6518fe14036c8c": [
      "HubConnector _call() prefers a healthy endpoint over a degraded one",
      "HubConnector _call() returns null when an endpoint is truly unreachable (no err.response)",
      "HubConnector _call() surfaces the JSON-RPC body of a 503 \"degraded\" response instead of discarding it",
      "HubConnector constructor builds urls array with http://<host>:<port> from host+port args",
      "HubConnector constructor handles IP addresses in urls array",
      "HubConnector constructor stores a single-element urls array when constructed with host+port",
      "HubConnector ping() returns false on network error",
      "HubConnector ping() returns false on timeout",
      "HubConnector ping() returns false when response has no result",
      "HubConnector ping() returns true (reachable) for a 503 \"degraded\" hub rather than masking it as down",
      "HubConnector ping() returns true when response contains a result",
      "HubConnector ping() sends correct JSON-RPC payload",
      "HubConnector updateConfig() returns false on network error",
      "HubConnector updateConfig() returns false when response has no result",
      "HubConnector updateConfig() returns true when response contains a result",
      "HubConnector updateConfig() sends correct JSON-RPC payload with config in params"
    ]
  },
  "scripts": {
    "test": {
      "fileCount": 70,
      "titleCount": 2322,
      "files": {
        "test/unit/AutohealService.test.js": "5cd927f17b924ef8",
        "test/unit/BootstrapArchiveMeta.test.js": "2fa482502d5d1dbe",
        "test/unit/BootstrapHealthGate.test.js": "8b127ec2a1140cac",
        "test/unit/BootstrapNodeTipGuard.test.js": "3916386139147a8b",
        "test/unit/BootstrapRepublishLedger.test.js": "e7b8ff5f97f1f7f3",
        "test/unit/BootstrapService.test.js": "ba304d2c23bb7a06",
        "test/unit/BootstrapSigning.test.js": "de606d67691f176b",
        "test/unit/ConfigService.hubNetwork.test.js": "6e3e97a6bec46f56",
        "test/unit/ConfigService.test.js": "2be99405409c0a53",
        "test/unit/CredentialsService.test.js": "013fb4868c83ee37",
        "test/unit/DatabaseService.test.js": "a5c2a73d73682ccc",
        "test/unit/DbCredentialDrift.test.js": "bcfaa8d7327dcba5",
        "test/unit/DiscoveryService.test.js": "14bd28e1e4b711a9",
        "test/unit/DockerService.test.js": "482a3259eebf8fa8",
        "test/unit/EncoderMaintenanceWindow.test.js": "5125fdb96876c007",
        "test/unit/ExplorerConnector.test.js": "b9b75c9f2ae1b837",
        "test/unit/ExplorerService.test.js": "40db4f1a28173b1f",
        "test/unit/GatedFileFieldScan.test.js": "82171a751987af6e",
        "test/unit/GitHubDownloader.test.js": "26da132c6adf5f9c",
        "test/unit/GoLiveGate.test.js": "a67cabacd0112216",
        "test/unit/HubConnector.failures.test.js": "2c16774921b9b66c",
        "test/unit/HubConnector.test.js": "fc6518fe14036c8c",
        "test/unit/HubConsensusEnvGuard.test.js": "bdea07439a6b5cec",
        "test/unit/HubInstallPinning.test.js": "13499f2ff51c6f3c",
        "test/unit/HubService.test.js": "e3665c51c64c9661",
        "test/unit/InstallTargetService.test.js": "3b04cc05990c4f9a",
        "test/unit/MariaDbStore.test.js": "54cd8fb7321c2356",
        "test/unit/MemoryLimitService.test.js": "99bd35e26fa0632d",
        "test/unit/MigrationPreconditionService.test.js": "88431f058ac97635",
        "test/unit/ModuleService.test.js": "8d3d114fcc0b254f",
        "test/unit/NodeService.test.js": "40fdfec30fa58f39",
        "test/unit/RebaseCompletenessSweep.test.js": "f3c42962f4a597a9",
        "test/unit/ReleaseManifestService.test.js": "774bd66498878fa9",
        "test/unit/ReleaseSignatureService.test.js": "0b0e062a08751d6c",
        "test/unit/SelfUpdateService.test.js": "33a5dd7bc655d47e",
        "test/unit/ServiceRegistry.test.js": "eb89a6dc1945c342",
        "test/unit/SkewGuardService.test.js": "66a744498ed159be",
        "test/unit/StatusService.test.js": "d966ad5aa2291a4b",
        "test/unit/StopBudgetService.test.js": "a67181a3796fad54",
        "test/unit/TelemetryConnector.test.js": "31b05a9177135645",
        "test/unit/TelemetryService.test.js": "842ea67df5e152c0",
        "test/unit/ValidatorService.test.js": "2ac8a00f5806cacb",
        "test/unit/ValidatorStakeService.test.js": "0226ac96a43d54d4",
        "test/unit/VersionService.test.js": "9778dd0f1c360aef",
        "test/unit/checkRebaseCompleteness.test.js": "dd6e4f59d7ec2e9a",
        "test/unit/cli-error-handling.test.js": "2093a3bb7f34dc89",
        "test/unit/cli-rollback.test.js": "4e86adb287c39e1d",
        "test/unit/cliRefForPreCheck.test.js": "e12b9a311ad07cac",
        "test/unit/cliSelfUpdate.test.js": "79cc27613f3e7980",
        "test/unit/cloneIntegrity.test.js": "2e8ada203563571b",
        "test/unit/coins-conformance.test.js": "2442df610ebb6911",
        "test/unit/commandLock.test.js": "da1b4dbe85ee8518",
        "test/unit/coverage-thresholds-sync.test.js": "4405034a199528ec",
        "test/unit/crossChainNetworks.test.js": "2ae188b9157fab7b",
        "test/unit/cryptoNodeBuildScaffold.test.js": "12558605c0d0415c",
        "test/unit/cryptoNodeConfPlaceholders.test.js": "f23059ddff16822e",
        "test/unit/deploySourceIdentity.test.js": "a04c8cc3113456d6",
        "test/unit/dockerMariadb.test.js": "cdda2d3e70c102b1",
        "test/unit/helpers.test.js": "152f5f77b38555af",
        "test/unit/identifyDeployedRef.test.js": "9b210872d17a3ffa",
        "test/unit/moduleOperations.test.js": "a88dc187e71ed005",
        "test/unit/mutatingLockWait.test.js": "1ea3450bb4fcc6a7",
        "test/unit/precheck.test.js": "bb7e5453878cf8dd",
        "test/unit/publishBootstrapsForcedRepublish.test.js": "e2d431011abfd746",
        "test/unit/scanNineFieldFiles.test.js": "d1b47eeb57e49143",
        "test/unit/secret-env.test.js": "37d41880660612cd",
        "test/unit/security/configuration/dependency-advisories.test.js": "5cc2b8143bdd5808",
        "test/unit/state.test.js": "a40634de4e012c02",
        "test/unit/statusReorgHalt.test.js": "4fed77ea7d38ca97",
        "test/unit/ui-menu.test.js": "113421dff9a3e19e"
      }
    }
  }
}
